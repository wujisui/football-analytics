# 产品完善路线

从 **2026-08-24** 起，本文件按日记录产品边界、管理员运维与配额相关的完善过程。  
写入前先按代码核对是否已落地：已落地的写进当日记录并注明真源，未落地的才留在「待办」。

---

## 待办

- **手动更新单场盘口后不重算每日推荐**。详情「更新盘口」目前只刷新本场盘口并重载详情（`Detail/index.vue → onRefreshOdds`），不触发日推重排；只有定时盘口批次会在盘口刷新后调 `sync_daily_auto_favorites`（`fixtures_sync.py`）。要么让这个接口顺带重算当天日推，要么明确产品上就不重算。

---

## 2026-08-24（周一）

当天完成了三块：亚盘特征升到 `ah_v2`、订阅/调度边界重构、管理员运维与开关的展示收敛。以下按主题记录，并附真源，便于后续回溯。

### 一、亚盘学习：`ah_v1` → `ah_v2`

原始需求：一场比赛盘口有多个让球，每个让球数都有对应赔率，用赛果回溯这些赔率做机器学习来预测盘口胜负；同时记录初盘与即时盘（第一次拉盘口记初盘，之后记即时盘）。

已落地（`backend/app/services/ah_features.py`、`ah_predictor.py`）：

- `iter_ah_quotes` 读出完整亚盘报价 `(line, home_odd, away_odd)`，主盘在前，不再只看主盘一条线。
- 新增副盘特征：`ah_has_level_line`、`ah_level_water_diff`、`ah_level_away_hot`、`ah_has_aux_lines`、`ah_aux_away_hot_share`。让 0 与其它档只作主盘参考，**不改训练标签**。
- 新增初盘 → 即时盘同线升降水特征：`ah_opening_same_line`、`ah_home_odd_drift`、`ah_away_odd_drift`、`ah_water_drift`、`ah_away_steam`。
- 初盘/即时盘本身由同步批次维护：明天首次可用盘口冻结为初盘，成为今天后刷新即时盘。

### 二、订阅与定时任务边界

「打开免费配额」改名为「订阅」，开关轨道内写明已订阅 / 未订阅。已订阅视为至少 Pro（日配额 7500），并据此重排全部官方请求。真源 `backend/app/tasks/scheduler.py`、`fixtures_sync.py`。

- **已订阅**：11:00 跑当天唯一完整批次；11:55 与 14/16/18/20/22/00/02 只刷新今天未开赛热门盘口并重算日推；04/06/08/10 由默认开启的早间开关控制。
- **未订阅**：08:05（UTC 00:05）只拉当天赛程；11:00 跑昨天赛果 + 今天赛程/热门盘口 + 训练 + 日推 + 清理，跳过积分榜与详情预拉；22:00 只轻刷今天未开赛热门盘口并重算日推。
- **赛程窗口**：已订阅本地保留今天起 8 天滑动窗口，每天只补窗口末端缺失的一天，不重复请求重叠的 7 天（`missing_subscribed_fixture_days`）。这条同时解决了旧记录里「开启全量点同步没有拉未来 7 天赛程」。
- **详情预拉不再限量**：`run_scheduled_full_detail_enrich` 以 `limit=None` 跑全部候选，统计里保留 `unlimited: True`。旧记录里「全量获取不要限制 10 场」已不复存在。
- **删除「定时全量获取详情」平行开关**，能力收敛到订阅状态。
- **单场盘口**：无论是否订阅，管理员都可在详情「我的预测」点「更新盘口」，只请求本场未开赛热门比赛的盘口，不补其他详情（`POST /fixtures/{id}/odds/refresh`，限管理员 + `pending` + 未开赛 + 热门联赛）。旧记录里「后台管理新增只拉盘口按钮、并与全量开关关联」按此实现，位置从管理员页改到详情页，关联对象从全量开关改为订阅边界与热门/未开赛校验。
- **立即同步**：两种状态下，当天完整批次尚未成功都可补跑；成功后当天禁用，避免重复消耗。

### 三、管理员运维与开关展示

目标：管理员一眼看出配额还剩多少、这次花了多少、上次同步是什么时候；开关状态写在轨道里。

1. **官方剩余**移到「管理员运维」卡片标题右侧（`n-card` 的 `header-extra`）。
2. **本次消耗**：每次官方响应在缓存里累加请求数（`CacheService.note_api_response`），批次结束用结束计数减开始计数，与剩余并排展示。跳过的批次（今日已同步、未配 Key）不改写记录。
3. **上次同步时间**：结束时刻、批次类型（完整批次 / 盘口轻刷 / 当天赛程）、成败写入 `app_settings.last_sync_run`，刷新页面或换浏览器仍在。文案跟在「同步官方 API 数据」标题后面，如「上次同步 2026-08-24 19:19 · 完整批次」，失败时同位置标失败。进程刚启动、内存还没有剩余值时，接口回退到上次批次落库的剩余数。
4. 这条同时补上了旧记录里「同步中刷新网页，按钮加载态被重置、看不出是否完成」：`useAdminSync.hydrateStatus` 已从后端 `active_tasks` 恢复进行中状态，现在加上持久化的上次同步结果，刷新后两种情况都有交代。
5. **订阅开关**对齐亚盘/竞彩：轨道内「已订阅 / 未订阅」，删掉开关下方重复的状态小字。
6. **早间盘口刷新**轨道内「已开启 / 已关闭」。抽出公共 `TextSwitch`，约定以后此类开关都把状态写进轨道；深色模式开关一并改成「深色 / 浅色」。

### 四、更早的零散反馈（已核对，均已落地）

- **赛果列表进行中的场次排前面**：`utils/fixtureSort.ts` 的 `sortFixturesFavoritesFirst` 第一优先级即 in-play，其次日推、关注，最后开赛时间。
- **我的方案新增中奖统计**：`views/Mine/plans/index.vue` 的 `PLAN_STAT_ITEMS` 给出中奖 / 已结算 / 全部三档，配色对齐赛程「历史统计」。
