# 用户鉴权 · VIP · API 配额方案（设计稿）

> 状态：**设计已定方向，代码未落地**（前端登录仍为本地 stub）  
> 更新日期：2026-08-05  
> 关联：[PROJECT_PLAN.md](../PROJECT_PLAN.md) §1.3 / 里程碑 M6+；产品边界见 `.cursor/rules/project-scope.mdc`  
> 目的：官方 API 配额有成本；定时公共池拉赛程/盘口/赛果，详情按点击补缺落库后多人复用；列表路径不放大官方请求。

---

## 1. 目标与原则

### 1.1 要解决什么

- 六个固定同步批次消耗了多少官方配额，要可审计。
- 赛程/盘口/赛果由平台统一拉取；详情缺包时按点击补拉并落库，后续读者复用。
- 官方配额用尽时产品仍可读取最近一次落库数据。

### 1.2 原则

| 原则 | 说明 |
|------|------|
| 单一公共池 | 每天 00/06/11/16/19/22 六个固定同步批次统一更新赛程、一级联赛盘口与赛果。 |
| 列表本地、详情按需 | 页面、F5、下拉刷新、筛选只读本地；打开详情缺包时可打官方并落库，不按列表在线人数放大。 |
| 先身份、后 VIP、再支付 | 真登录 → 动作级计量（可先只记日志）→ 档位限额 → 支付/订阅。 |
| Key 永不进前端 | 与现网一致；前端只调本项目 `/api/v1`。 |
| 非直播站 | 不做滚球轮询；VIP 不提供「手动刷官方」入口；日后可把详情补拉或全量预拉纳入用户/VIP 额度。 |

---

## 2. 角色与权益（产品分层）

额度数字为**示意**，上线前按套餐与官方日配额再定。

| 档位 | 身份 | 公共数据 | 其它 |
|------|------|----------|------|
| 游客 | 未登录 | 本地已有列表 / 详情 / 赛果（只读） | 收藏等仅本机；不云同步 |
| 登录免费 | 已注册登录 | 同上 | 收藏云同步、计算器 |
| VIP | 有效订阅 / 权益标记 | 同上，不增加官方刷新权 | 更长历史窗口、账号侧增强等 |

**不建议**把 VIP 做成「每人一套 ML 模型」；训练、公共推理和官方数据同步继续服务端共享。

---

## 3. 配额计量模型

### 3.1 计量动作（建议枚举）

| `action` | 含义 | 典型入口 | 池 |
|----------|------|----------|----|
| `scheduled_fixtures_sync` | 全量赛程 + 一级联赛盘口 + 赛果 | 每天六个固定时点 | **公共** |
| `detail_package_enrich` | 单场展示包按需补拉 | 用户打开详情 | **公共落库 / 日后可计用户额度** |
| `scheduled_full_detail`（预留） | 窗口内全量详情预拉 | `ENABLE_SCHEDULED_FULL_DETAIL`（默认关） | **公共 / 订阅后开启** |

公共池动作**不扣**用户额度，但应写入**平台配额日志**（监控日余量）。

### 3.2 账本（建议表）

`quota_ledger`（名可再定）每条至少含：

- `user_id`（可空 = 系统/公共）
- `action`、`scope`（如 `date` / `league_id` / `fixture_id`）
- `estimated_requests` / `actual_requests`（若可知）
- `status`：`accepted` / `denied` / `deduped`（命中 running 或已新鲜未打官方）
- `created_at`

用户侧日汇总：`user_quota_daily(user_id, day, used, limit, plan)`。

### 3.3 扣费规则（建议）

1. 请求进入需打官方的路径前：`can_spend(user, action, cost)`。  
2. 若全局已有同类 sync `running` → 返回现有 `running`，记 `deduped`，**不扣**或扣「触发尝试」的极低成本（产品二选一，推荐不扣）。  
3. 若本地已满足 TTL / 已有初盘且策略为「只补缺」→ 可不打官方，不扣或少扣。  
4. 真正发起官方 HTTP 前扣减（或预留 + 完成后结算），失败可回滚预留。  
5. 额度用尽：HTTP `402` 或 `429` + 业务码 `quota_exhausted`；前端提示「本地仍可用，无法再向官方拉取」。

---

## 4. 鉴权与会话

### 4.1 现状

- 前端：`LoginModal` 等为**本地会话 stub**，无后端 `/auth`。  
- 官方 Key：仅后端 `secrets.local.env`。

### 4.2 目标形态（尚未实现）

- 后端：注册 / 登录 / 刷新令牌（或 session cookie）；`users`、`sessions`（或 JWT + refresh）。  
- VIP：`user_entitlements` 或 `subscriptions`（`plan`、`expires_at`）。  
- 不提供用户官方同步路由；列表和已落库详情可继续匿名只读。

密码与密钥规范遵循现有 secrets 规则；生产再定 OAuth（可选，非必须首版）。

### 4.3 用户私有数据（留坑 · 无痛衔接）

收藏、计算器方案等**按用户隔离**的数据，在真登录落地前也要按「以后有 user_id」来写，避免全民共用一张表。

| 约定 | 说明 |
|------|------|
| `user_id` 可空 | `NULL` = 登录前的本机单租户桶（当前行为）；登录后写入真实用户 id |
| 查询必带作用域 | `owner_is(column, user_id)`（`app/services/user_scope.py`）；禁止无过滤全表扫私有数据 |
| 鉴权依赖 | `CurrentUserId`（`app/api/deps_auth.py`）现恒为 `None`；接 JWT/session 时只改这一处 |
| 首登迁移 | 可选：把该设备上 `user_id IS NULL` 的收藏/方案认领到当前账号一次，再禁止匿名写 |
| 收藏 PK | 现为 `fixture_id`（单租户够用）；多用户时改为 `(user_id, fixture_id)` 或代理主键 |

已挂钩的表/接口：`favorite_fixtures.user_id`、`bet_plans.user_id`，以及 `/favorites`、`/bet-plans`。

**原则**：未登录时**不假装**做云同步或跨设备私有；新功能可以先落地业务，但私有数据路径必须过 `CurrentUserId` + `owner_is`，登录完善后只接身份、不改业务语义。

---

## 5. 与现有同步链路的对齐

| 现网行为 | VIP 方案下 |
|----------|------------|
| 打开 / F5 / 下拉刷新 | 只重读本地库 |
| 勾选非一级联赛 | 只改变本地筛选，不补拉盘口 |
| 详情 | 本地优先；缺展示包时按需补拉并落库（日后可计额度） |
| 定时 00/06/11/16/19/22 | 公共池同步赛程/一级联赛盘口/赛果；积分榜**只刷新 `config/leagues.json` 热门联赛**（日期条上的其它联赛只读本地、不烧官方配额） |
| 全量详情预拉 | 默认关；管理员可在「我的 → 管理员设置」开关（`app_settings` / `ADMIN_API_KEY`）；订阅/规模上来后再真正接线预拉逻辑 |
| 初盘冻结 / 即时盘展示去重 | 数据语义不变；与鉴权正交 |

**固定边界**：列表不触发官方；定时批次不做全量详情；详情补拉结果落库共享。用户身份与 VIP 不改变六个固定批次的频率。

---

## 6. 前端体验要点

- 「我的」：登录态、当前档位、VIP 到期。
- 不展示官方刷新入口或用户官方配额；PC 与手机刷新都只重读本地。
- 游客与登录权益差异在 UI 上可发现（勿静默失败）。

UI 细则后续写入 [frontend/FRONTEND_UI_SPEC.md](../frontend/FRONTEND_UI_SPEC.md)；本文件以业务与后端契约为主。

---

## 7. 后端落地顺序（实施清单）

代码按此顺序迭代，**当前仓库尚未开始**：

1. **用户表 + 真登录 API**（替换前端 stub；填充 `CurrentUserId`）  
2. **私有数据认领**：`favorite_fixtures` / `bet_plans` 的 `NULL → user_id` 一次性迁移；收藏 PK 升级为复合键  
3. **平台配额日志**：只记录六个固定同步批次的消耗与剩余量
4. **VIP 权益配置**：仅接非官方调用能力
5. **支付 / 订阅**（最后）：发卡或第三方支付写 `entitlements`

---

## 8. 风险与约束

| 风险 | 应对 |
|------|------|
| 固定批次超出官方日配额 | 监控每批消耗与官方日余量，按需缩小盘口预算 |
| 用户以为「VIP=实时比分」 | 文案与产品边界写清：赛前分析，非直播 |
| 调度失败导致数据偏旧 | 页面继续读最近版本；任务状态与日志明确失败批次 |
| 在线用户过多拖垮后端 | 只增加本地读压力，不放大官方请求 |

---

## 9. 验收标准（本方案「完成」时）

- [ ] 列表 / F5 / 筛选入口均不触发官方 API
- [ ] 每天只注册六个固定赛程/盘口/赛果批次；全量详情预拉默认关
- [ ] 详情缺包时可按需补拉并落库；重复打开不重复打满官方
- [ ] VIP 与免费权益不提供「手动刷官方」入口
- [ ] 平台日志能回答每批消耗与剩余配额
- [ ] Key 仍仅存后端；前端无官方 Key  

---

## 10. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-09 | 管理员可在「我的」页开关定时全量详情（落库覆盖 env） |
| 2026-08-09 | 澄清：定时批次只做赛程/盘口/赛果；详情按点击补拉；全量详情预拉预留默认关 |
| 2026-08-09 | 官方调用收口为每天六个固定公共批次；删除用户 sync / 列表补盘 |
| 2026-08-05 | 初稿：公共池/用户池、档位、计量动作、落地顺序；代码未改 |
