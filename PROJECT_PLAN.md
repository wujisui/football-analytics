# Football Analytics 项目计划书

> 更新日期：2026-08-05  
> 仓库：`football-analytics`（`backend/` + `frontend/`）  
> 定位：**赛前分析产品**，不是实时比分站

---

## 1. 产品目标（已明确）

### 1.1 要做什么

面向联赛与杯赛的**赛前分析**：在开赛前准备并展示决策相关数据，给出胜平负倾向与推荐。

核心数据（开赛前准备、写入本地库）：

| 数据项       | 说明                      |
|-----------|-------------------------|
| 赛前赔率      | 开赛前赔率变化，用于辅助判断          |
| 历史交锋      | 双方近期交手记录                |
| 近期战绩 / 近况 | 用于概率与展示                 |
| 阵容        | 首发阵型与名单                 |
| 伤病        | 双方伤停信息                  |
| 替补        | 替补席名单                   |
| 分析结果      | 主胜 / 平局 / 客胜概率、置信度、推荐方向 |

预测依据边界：**赛前 1X2 盘口是所有玩法的推断依据**。缺盘口的场次一律不出结论——胜平负 / 比分 / 大小 / 双进全部标「待分析」，让球标「缺少盘口数据分析」，即使近况模型已给出非均势概率也不落倾向（真源 `prediction.get_recommendation` / `derive_prediction_leans`）。均势盘口（三路都贴近 1/3）是真实读数，照常出双选。

数据价值边界：未开赛赛程与刚延期场次一律保留（盘口可能尚未开出）。已完场却没有赛前 1X2 盘口的场次物理删除，**库里冻结过预测也删**：无盘口的预测没有依据，留着只会污染赛果准确率与训练标签。训练样本同样只使用有盘口的完场场次。

结算依据留存：赛果页比赛列表只允许追溯近 7 天；准确率走势图默认 30 天且可选「全部」，因此冻结推荐、概率与盘口长期保留在 `pre_match_data`，`match_features` 训练样本也长期保留。超过 `CLEANUP_DAYS` 只清理阵容 / 伤停 / 交锋 / 近况 / 积分榜 / 简报等详情大字段，不删除统计与训练依据。

### 1.2 明确不做

| 不做            | 原因                                       |
|---------------|------------------------------------------|
| 实时比分 / 滚球直播   | 产品不是直播站，配额与架构都不按此设计                      |
| 开赛后轮询官方比分     | 赛果按日一次性回写；未完场比分只在用户点开详情时补一次，不做滚球                            |
| 开赛后改写赛前预测快照   | 预测审计字段冻结；展示包可按详情点击补缺落库                      |
| 前端直连官方并暴露 Key | Key 只放后端库内（管理员配置）；前端只调自有 API |

### 1.3 数据与配额原则

```
开赛前：定时同步批次只为管理员勾选的热门联赛刷新盘口，首次可用盘口冻结为初盘。管理员「订阅」开关是官方套餐边界：未订阅默认每天 **00:05 UTC / 08:05 北京时间**只同步当天赛程、**11:00** 完整批次 + **22:00** 盘口轻刷；已订阅（至少 Pro、日配额 7500）在 **11:00** 跑每日定时完整批次，其余盘口批次只刷新当天未开赛热门场次并重算日推
开赛后：预测快照审计字段冻结；已有冻结快照的场次详情只读本地展示包，官方只补比分与状态（开赛 12 小时内 60 秒缓存；此后到 7 天内仍未结算的按 30 分钟缓存补一次终场，不轮询）；按点击补缺展示包只发生在未开赛或从未分析过的场次
赛果：免费配额批次优先回写昨天终场比分（非滚球），再拉今天赛程与盘口；赛果列表与联赛筛选统一使用持久化的场地当地 `match_day`，不再按 UTC 日期拆散同一轮比赛
未订阅范围：官方请求固定为「00:05 UTC（北京时间 08:05）当天赛程 + 11:00 昨天赛果/今天比赛/热门盘口 + 22:00 今天热门盘口」。08:05 只多一次全球 `fixtures?date=` 请求，**不拉盘口、积分榜或详情**；11:00 跳过积分榜与详情预拉、不请求未来；普通用户详情点击完全只读本地。管理员「立即同步」随订阅关闭而禁用；管理员仍可点「只更新赛果」按日回写终场比分（未订阅含昨天与今天），不拉盘口；也可在运维页显式点「批量更新盘口」，仅接收前端【比赛】当前筛选已勾选联赛所对应的未开赛场次名单，已有盘口刷新即时盘、缺盘口补齐，不拉赛程、赛果、积分榜或详情；还可在任意未开赛场次详情「我的预测」点击「更新盘口」，不受热门联赛范围限制
已订阅范围：每天 11:00 跑定时完整批次；管理员「立即同步」不限每日次数，可在新增热门联赛后再次拉取数据，但每次都会消耗官方配额且同一时刻只允许一个官方批次。赛程在本地始终保留今天起 8 天，但每天只增量拉窗口末端新一天，不重复请求重叠的未来 7 天；近 4 天赛果仍按日回写。11:00 / 立即同步盘口覆盖今天及未来三天：未来三天缺盘补齐并冻初盘，今天刷新即时盘；详情预拉仍只覆盖今天、明天热门未开赛缺包，打开详情仍可按需补整包。盘口轻刷有两套独立完整方案：默认方案为 02:00、11:55、14:00、16:00、18:00、20:00、21:00、21:30、22:00、22:30、23:00、23:30、00:00；密刷方案为 02:00、11:55、14:00、16:00、16:55、17:25、17:55、18:25、18:55、19:25、19:55、20:25、20:55、21:25、21:55、22:25、22:55、23:25、23:55、00:25、00:55、01:25、01:55。运维开关只在两套方案间切换；04/06/08/10 由默认开启的独立早间开关叠加到任一方案；「只更新赛果」仍可按近 4 天回写比分
前端：始终只调本项目后端。列表页、F5、下拉刷新、切换筛选只读本地库；进入详情走 `/fixtures/{id}/analysis`。未订阅时详情完全只读本地公共数据；已订阅时热门联赛缺包可按需打官方并落库复用。已移除「定时全量获取详情」独立开关，能力收敛到订阅状态。每次定时同步盘口后，对**已有赛前盘口的未开赛场次**按后端持久化的**比赛所在地日历日**各自做**单选价值排序**：先用冻结历史按玩法做时间留出 Platt 概率校准（最近 20% 只验证，Brier 与 log-loss 未同时改善的玩法保持原概率）；`|让球线| ≤ 0.5` 的盘口一律按 1X2 概率估值并计入返还（±0.25 平局退半、平手盘平局全退），其中 `±0.5` 与胜平负构成同一结算事件时共用概率并按实际报价择优，避免平行模型重复估值；其余候选将高于市场基线的模型概率向市场公平概率保守收缩，再按期望收益折算凯利型风险收益分，使收益与稳定性共同参与排序。历史命中反馈只参与综合分调整、不设置历史分位淘汰门槛，每天按综合分最多选 4 场写入自动推荐池（`source=auto`，只替换自动推荐）。入选后在同一比赛日内部按综合分评级，最高质量锚定 5 星，其余每降一个得分档位减 0.5 星，同分同星。自动推荐只用于【比赛】/【赛程】置顶、`[荐]` 与质量星展示，不点亮关注星，也不进入【关注】；【关注】只展示用户点亮星标并经收藏接口写入数据库的 `source=manual` 记录。概率校准与风险调整只改变排序、不减少每日数量；拒绝胜/平、让胜/负等多选；精确比分因无独立校准概率且历史命中率低，只作详情提示，不参与自动主推；窗口内有多少个有赛日就有多少组推荐，禁止把整周场次压成全局 Top4。不做前端轮询或 SSE。**让球胜平负的展示与结算**只在「我的 → 偏好设置」用同一开关切换：默认**亚盘**（让胜/让负；整数盘打成让球平走水，四分盘出赢半/输半）；**竞彩**先把盘口按绝对值向上取整成整数（-0.25/-0.5/-0.75 → -1，-1.25~-1.75 → -2）再判让胜/让平/让负，不出半结果，展示的盘口同步显示取整后的线。口径只作用于让球胜平负，大小球、双进等其余玩法一律按亚盘拆盘。只改本机展示与结算，不回写冻结预测或已存方案；机器学习与每日推荐激励仍按产品默认亚盘。真源：`ah_features.jc_handicap_line`。
自动推荐按产品默认亚盘筛选：除多选外，单独「让平」只代表退本走水、不是可投注方向，也不得写入自动推荐池；竞彩开关只改变展示与结算，不另起一套推荐排序。让球倾向本身也不得与同卡片的胜平负推荐互斥（详见阶段 M-AH「与 1X2 关系」）。
详情「我的预测 → 盘口解释」由后端唯一真源根据已落库的初盘 / 中盘（T-6h 最近）/ 临场（T-3h 内最新）/ 即时盘生成：按采集时间去重，只在同庄家、同玩法、同档位间比较水位与去水概率；主盘升降、同档共振、1X2、大小球、算法倾向与按亚盘返还计算的期望收益分开说明。禁止把不同档位或不同庄家的报价直接串成走势，也不把盘口变化表述成无法从报价证明的「资金流 / 机构意图」。中间态来自现有刷新，不新增官方 API 请求；前端只展示后端结构化文案，不维护第二套盘口判断。
上句「不做前端轮询」特指不轮询比赛列表或官方 API。后台批次完成后写入本地 `data-revision`；仅【比赛】/【赛果】在前台且未息屏时每 30 秒对一次版本（息屏、切走或离开这两页停表），变化才刷新当前页。运维操作成功后直接发全局通知。

联赛范围：可勾选目录、分类与热门状态以数据库 `leagues` / `league_categories` 为唯一真源；`backend/config/leagues.json` 只在首次建库时导入种子。系统保护联赛仅五大联赛、荷甲、葡超、欧冠、中超，不可删除也不可改官方 ID；其余目录联赛可改可删。管理员可在「我的 → 管理员设置 → 热门联赛」新增分类、按官方稳定 `league.id` 新增联赛、修改中文名/国家/分类与勾选热门；后台新增的非保护联赛可改正写错的官方 ID（错误 ID 下已拉赛程会丢弃），也可在管理员密码二次确认后连同该联赛全部赛程/盘口/特征/推荐等历史数据删除，之后允许重新添加。非热门目录联赛仍入库赛程但不定时补盘；删除的非保护联赛退出目录并停止新赛程准入。未勾选的目录联赛及额外白名单赛事进入「其他」。`competition_scope.py` 只保留目录外已核对的各国顶级联赛、主要足球地区高级别杯赛及友谊赛；青年、预备队、乙级及以下和低级别杯赛不入库。筛选弹窗里热门默认勾选，切换日期后仍默认勾选（只有用户点过确认的选择才落 localStorage，加载时推导的勾选不落库）；筛选勾选只影响本地显示。已开赛详情只补比分与状态。
注：文中“11:00 / 22:00 批次”均指赛果与盘口主批次，不包含独立的 08:05 单次赛程补录。
```

| 时点 | 盘口与推荐刷新策略 |
|---|---|
| 11:00 完整批次 | 未订阅处理昨天赛果、今天赛程/热门盘口；已订阅处理近 4 天赛果、8 天赛程滑动增量、今天及未来三天热门盘口、今天/明天详情及积分榜；首次可用盘口冻为初盘；随后训练、日推与清理 |
| 盘口轻刷批次 | 只刷新今天未开赛热门盘口并重算日推；未订阅仅 22:00；已订阅在完整默认方案与完整密刷方案间二选一，04/06/08/10 早间时刻可独立叠加 |
| 管理员「批量更新盘口」 | 只处理前端明确传入的【比赛】当前筛选未开赛场次；已有盘口刷新即时盘、缺盘口补齐，未勾选的「其他」赛事不进入名单；不拉赛程、赛果、积分榜或详情，完成后重算日推 |
| 同步后自动推荐 | 未开赛且已有盘口的场次先按玩法做时间留出 Platt 校准；`±0.5` 等价结算事件统一概率并按实际赔率择优，其余模型优势向市场公平概率保守收缩，再以凯利型风险收益分叠加历史软权重排序；按后端持久化的**比赛所在地日历日**每天最多 4 场写入自动推荐池（`source=auto`），数量策略不变；不是整周全局 Top4。自动推荐在【比赛】/【赛程】置顶并显示 `[荐]`/质量星，但不点亮关注星且不进入【关注】；【关注】只展示 `source=manual` 的接口落库记录。标 `[荐]` 的胜平负标签展示日推单选方向 `auto_lean`，不显示分析器的双选文案 |
| 勾选其他联赛 | 只筛选数据库中已有赛程，不触发官方盘口请求 |
| 用户打开详情（未开赛 / 从未分析过） | 免费配额开启时完全只读本地，缺盘口/缺包均不打官方；关闭后仅热门联赛恢复盘口、阵容、伤病、交锋、近况、简报等全量按需获取并落库 |
| 用户打开详情（已开赛） | 展示包只读本地，不再补包/补盘；官方仅一次比分 + 状态（12 小时内 60 秒缓存跟比分，12 小时~7 天按 30 分钟缓存追终场结算）。真源 `ttl_policy.should_stop_api_refresh` |
| 已开赛或已结束 | **预测快照审计字段不再刷新**；赛果随固定批次回写 |

列表归属以**服务器当前 UTC 时间与开赛时刻**为主：开赛时刻未到进入 **比赛**，开赛时刻已到进入 **赛果**；不依赖定时批次回写、可能仍为 `pending` 的本地状态。**例外**：状态为 `postponed` 且原定开赛已过超过 1 天的场次移出赛果列表（避免延期场长期占位；官方改期后按新开赛日再进列表）。比赛列表请求 `/fixtures/today?scope=prematch` 时由后端 SQL 直接过滤，前端不再二次判断。**【比赛】列表「今天 / 明天」分组与每日推荐分桶**统一读取后端持久化的场地当地比赛日：依次按官方 `venue.city`、联赛国家、主队已知所在地推导 IANA 时区，无法确定时才回退 UTC，并保存 `match_timezone` / `match_day_source` 供审计；前端不再从 `fixture_date` 二次换算。预测卡片内附带让球主盘一行（多盘口悬停展开）；赛果列表不做滚球刷新。开赛归属边界真源：`backend/app/services/results_capture.py`；比赛日真源：`backend/app/services/match_day.py`。

读取顺序：`Redis` → `SQLite（api_snapshots / pre_match_data）` → `API-Sports`（本地缺展示数据时，含完场复盘详情）。

鉴权：只用 **API-Sports 官方 Key**（库内管理员配置）。

### 1.4 用户 · VIP · 配额（规划中）

官方日配额有成本；后续能力向**用户身份 + VIP 额度**靠拢。核心约定：

- **公共池**（平台）：六个固定批次统一更新赛程、初盘/即时盘与赛果 —— 一次拉、多人读。
- **详情按需**：用户打开详情时缺包才打官方，结果落库后复用；列表路径不分配官方调用。
- **落地顺序**：真登录 → 账本打点 → 档位限额 → 支付。当前登录仍为前端 stub，**代码未实施**。

详细设计（计量动作、表结构建议、验收清单）见 **[docs/AUTH_VIP_QUOTA.md](docs/AUTH_VIP_QUOTA.md)**。改产品边界时先改该文档与本节，再改代码。

---

## 2. 当前进度总览

| 模块               | 进度      | 说明                                          |
|------------------|---------|---------------------------------------------|
| 后端骨架与配置          | ✅ 完成    | FastAPI、配置、日志、CORS、健康检查                     |
| 数据模型与 SQLite     | ✅ 完成    | 联赛 / 球队 / 赛程 / 赛前分析 / API 快照                |
| 官方数据拉取           | 🟡 部分完成 | 联赛/球队/赛程/H2H/近况/统计已接；赔率/阵容/伤病已接并落库（视官方是否有数据） |
| 本地优先与 TTL        | ✅ 完成    | 落库、开赛后冻结、按开赛时间刷新                            |
| 赛前概率分析           | 🟡 持续优化 | 训练只使用冻结赛前特征/盘口与终场标签；1X2 必须优于盘口基线才启用，否则直接采用盘口隐含概率；比分/大小球/BTTS 使用独立 Poisson 进球分布模型；让球独立训练并受基线门禁 |
| 业务 API           | ✅ MVP   | leagues / today / sync（含按日批量赔率） / results / analysis / admin |
| 定时任务             | ✅ 完成    | 免费模式 **08:05 单次赛程 + 11:00 全量 + 22:00 盘口**；关闭后恢复六个整点及未来窗口 |
| 密钥管理             | ✅ 完成    | 管理员库内配置；多 Key 自动切换 |
| 前端展示             | 🟡 持续完善 | 日期赛程、联赛筛选、详情、收藏、赛果复盘与命中统计已接；模型来源标注及管理页面仍缺 |
| 赛前详情页（赔率/阵容/伤病等） | ✅ 主链路完成 | API `package` + 前端分区已接；官方 `/predictions`→「赛前简报」Tab；空态取决于官方开盘/公布阵容/简报覆盖 |
| 官方 Widgets       | ❌ 未做    | 可选增强，非必须                                    |
| 用户鉴权 / VIP / 配额 | 📋 设计中  | 见 [docs/AUTH_VIP_QUOTA.md](docs/AUTH_VIP_QUOTA.md)；登录仍为前端 stub |
| 测试 / 部署          | 🟡 部分完成 | 已有预测、进球模型、让球特征与数据清理单测；接口/前端/CI 与生产部署仍未完成 |

**整体判断：M4 主链路已完成；阶段 M 与 M-AH 已形成「冻结赛前输入 → 终场标签 → 时间验证 → 基线门禁 → 自动重训」闭环，当前重点是积累样本、校准和工程化上线。**

---

## 3. 已完成（Done）

### 3.1 工程与基础设施

- [x] Monorepo：`backend/` + `frontend/`
- [x] FastAPI 应用入口、CORS、生命周期内启动调度器
- [x] SQLAlchemy 异步 + SQLite（`data/football.db`）
- [x] Redis 缓存，不可用时降级 fakeredis
- [x] 官方 Key：只用管理员库内配置（`app_settings`）+ 多 Key 配额切换
- [x] API-Sports 直连鉴权（`x-apisports-key`）
- [x] 根目录 / 后端 / 前端 README；密钥说明见 [docs/API_SPORTS_KEYS.md](docs/API_SPORTS_KEYS.md)

### 3.2 数据与联赛覆盖

- [x] 模型：`League` / `Team` / `Fixture` / `PreMatchData` / `MatchFeature` / `ApiSnapshot`
- [x] 配置联赛：英超、西甲、德甲、意甲、法甲、欧冠、欧罗巴、亚冠、中超、美职联、巴甲、日职联(J1)、韩K联(K1)
- [x] CLI：`init-db`、`fetch-leagues`、`fetch-today`、`check-quota`、`test-api`、缓存与任务相关命令

### 3.3 本地优先与赛前冻结策略

- [x] 官方响应写入 `api_snapshots`，业务优先读本地
- [x] 分析结果写入 `pre_match_data`
- [x] 六个固定时点刷新赛程/盘口/赛果；**开赛后 / 结束后预测快照冻结**；详情缺包按点击补拉
- [x] 回溯已结束比赛：读本地库

### 3.4 分析与 API（MVP）

- [x] 胜平负概率：拟合模型仅在时间验证集优于盘口基线时启用，否则使用去水后的盘口隐含概率
- [x] 比分/大小球/双方进球：由赛前 1X2、大小球、亚盘及近况训练 Poisson 进球分布，不再从旧预测回灌训练
- [x] 置信度、推荐方向
- [x] `GET /api/v1/health`
- [x] `GET /api/v1/leagues`
- [x] `GET /api/v1/fixtures/today`
- [x] `GET /api/v1/fixtures/{id}/analysis`
- [x] 管理接口：任务状态 / 手动触发（`X-Admin-Key`）

### 3.5 调度

- [x] 免费配额调度：每日 08:05 用 1 次请求只同步当天赛程，11:00 同步昨天赛果与今天赛程/盘口，22:00 轻量刷新盘口并重算每日推荐；关闭后恢复六个整点与未来窗口；首次盘口冻为初盘，新增标签后条件自动 train
- [x] `clean_old_data`：每天 11:00 全量同步（及管理员「立即同步」）之后执行；物理删除「永远无法结算」（取消，或开赛已过 1 天仍无比分的完场 / 延期 / pending / live）与「已完场且无赛前 1X2 盘口」（含有预测但无盘口的无依据预测）的场次及关联数据；清理空联赛行、孤立球队与过期展示分析/日志
- [x] `train_model`：可手动触发训练
### 3.6 前端 MVP

- [x] Vue 3 + Vite + TypeScript + Naive UI + ECharts
- [x] 页面：联赛列表 → 今日赛程 → 单场概率分析
- [x] 开发代理：`/api` → 后端 `8000`

### 3.7 赛前数据包（M4 主链路）

- [x] 官方 `/odds`、`/fixtures/lineups`、`/injuries` 拉取（本地优先缓存）
- [x] `pre_match_data` 扩展 JSON 字段并落库
- [x] `GET /fixtures/{id}/analysis` 返回 `analysis.package`
- [x] 前端详情页分区：赔率、交锋、近况、阵容/替补、伤病、官方赛前简报
- [x] 今日列表分析不拉完整包（省配额）；计算器筛选展示本地未完赛联赛（未开盘仍可见）；不再靠全球发现扫出一堆无价值联赛选项
- [x] **ML 概率模型（带基线门禁）**：赛前特征落库、赛果打标、时间切分验证；1X2 未优于盘口时不部署
- [x] **进球分布模型**：持久化 `goal_features_json` 与终场进球标签；Poisson 模型驱动比分、大小球、BTTS，且验证 MAE 必须优于常数基线

---

## 4. 未完成 / 缺口（Not Done）

下列项属于**已明确需求但尚未闭环**（或仅有字段/拉取未用于产品）。

### 4.1 赛前数据闭环（高优先级）

| 需求     | 现状                                       | 缺口                        |
|--------|------------------------------------------|---------------------------|
| 赛前赔率   | 任一定时、补齐或管理员单场刷新路径首次成功获取的可用盘口冻为**初盘**（`odds_opening_json`），同次也写**即时盘**（`odds_json`）；后续刷新只更新即时盘并重算 1X2。单份盘口只取一家庄（`BOOKMAKER_PRIORITY` 中覆盖核心玩法最多者，只有主庄没开的玩法才回退次级庄）；初盘若冻在次级庄，主庄开盘后按主庄首盘替换，开赛后不再改写 | 官方未开盘仍空态 |
| 阵容（首发） | 已接 lineups，落库并展示                         | 临场前官方可能无数据               |
| 替补     | 已随 lineups 解析 `substitutes` 并展示           | 同上                      |
| 伤病     | 已接 `/injuries` 并展示                         | 部分联赛覆盖不全                 |
| 历史交锋展示 | 已进 API `package` + 详情页                      | 已入多因子概率（`h2h_*`）           |
| 近况展示   | 已进 API `package` + 详情页                      | 已入多因子 + 连胜/连败均值回归特征     |
| 球队统计   | 已拉取；详情已展示近况胜率、场均进失球与赛事排名             | 官方赛季统计尚未完整进入模型和展示         |
| ML 推荐  | ✅ 赛前特征/盘口入库 → 赛果打标 → 时间验证 → 优于基线才部署；预测快照仅用于审计，不作为训练输入 | 继续积累分联赛与置信度样本；见 §5 阶段 M |

### 4.2 产品能力增强

| 需求                 | 现状                      |
|--------------------|-------------------------|
| 未来 N 日赛程（非仅今日）     | ✅ 已做：后端日期窗口 + 前端日期条；同步范围按联赛配置与窗口控制 |
| 积分榜 / 场地 / 教练      | 积分榜/赛事排名已展示；场地、教练未做（可选）  |
| 赛果查询页 / 终场比分落库    | ✅ 已做：`/results` + 固定同步批次 |
| 分析模型升级             | ✅ 1X2 基线门禁 + Poisson 进球分布 + 独立 AH 模型链路已接；继续积累样本与校准 |
| 球队 Logo            | 库中有 `logo_url`，API/前端未用 |
| API-Sports Widgets | 未集成（可选，注意 Key 暴露）       |

### 4.3 工程化

| 需求                   | 现状                         |
|----------------------|----------------------------|
| 自动化测试                | 🟡 已有预测/ML/AH/清理单测；fetcher、analyzer、TTL、API 与前端测试仍缺 |
| 生产部署（Docker / Nginx） | 无                          |
| 本机数据迁云 / 备份策略       | 无；数据在 `backend/data/football.db`（gitignore），需手动拷或云上重拉（见阶段 D.2） |
| CI                   | 无                          |
| `pandas` / `numpy`   | `numpy` 已用于概率模型训练；`pandas` 仍可清理 |

---

## 5. 将来要做（Roadmap）

按优先级分阶段；完成前一阶段再开下一阶段。

### 阶段 M — 本地训练、基线门禁与自动重训（进行中 · 当前重点）

> **前提**：本地库从现在才开始持续入库，不以历史赛季回灌为前提。  
> **策略**：训练输入只允许冻结赛前特征/盘口，标签只允许终场赛果；预测快照只负责审计。模型达到样本阈值后自动训练，但必须在时间验证集优于对应基线才部署。

#### M.1 数据闭环（已实现，持续跑）

```
赛前分析 → 写 pre_match_data（展示）+ match_features（冻结训练特征/预测审计快照）
     ↓
固定同步批次回写赛果 → 写 1X2 / AH / 主客进球标签
     ↓
样本数 ≥ ML_MIN_TRAIN_SAMPLES（默认 30）且有新增标签
     ↓
自动 train → 时间验证并与盘口/常数基线比较 → 合格 artifact 才部署
不合格时 → 1X2 使用盘口隐含概率；进球分布保留旧规则兜底
```

| 能力 | 状态 | 说明 |
|------|------|------|
| 赛前特征入库 | ✅ | `match_features`（近况/回归/H2H/盘口等） |
| 预测快照入库 | ✅ | 同表记录当时胜平负概率与 `model_source` |
| 赛果打标 | ✅ | `capture_finished_results` 后写 `label` |
| 价值数据清理 | ✅ | 每天 11:00 全量同步后跑 `clean_old_data`：删「永远无法结算」（取消，或开赛超 1 天仍无比分的完场/延期/pending/live，含有赛前包的）与「完场且无赛前盘口」（有预测也删，无盘口的预测没有依据）的场次；未开赛赛程与刚延期场次保留 |
| 盘口基线输出 | ✅ | 1X2 拟合模型未胜过盘口时直接采用去水盘口概率 |
| 进球分布输出 | ✅ | Poisson 模型独立评估主客进球、比分、O/U 与 BTTS；各目标单独战胜基线才输出，否则待分析 |
| 自动训练 | ✅ | `scheduled_fixtures_sync` 回写标签后可 `maybe_auto_train_model` |
| 自动切换推断 | ✅ | 有合格 artifact 时才切 ML，不以“样本够”代替“效果好” |
| 运维命令 | ✅ | `backfill-features` / `train-model` / `train-goals-model` / `model-status` |

配置：`ML_MIN_TRAIN_SAMPLES`（默认 30）、`ML_AUTO_TRAIN`（默认 true）。

#### M.2 积累期要做的事（产品侧）

1. 保持日常 sync / 赛前分析 / 赛果回写正常跑，让标签自然增长  
2. 用 `python manage.py model-status` 看 `match_features_labeled` 与 `inference_mode`  
3. **不要**在样本很少时强行调低阈值冒充「已上 ML」；阈值可配，但建议 ≥30  

#### M.3 样本够了之后（自动完成，无需手切）

1. 首次达到阈值 → 自动训练 → 仅验证优于基线的新分析走 `ml`  
2. 之后有新增赛果标签 → 自动重训（`labeled > last_trained_n`）  
3. 人工可随时：`manage.py train-model` 或 `trigger-task --name train_model`  

#### M.4 后续完善（有数据后再做，不挡当前）

| 项 | 说明 |
|----|------|
| 命中率对比看板 | 多因子 vs 拟合模型 vs 盘口基线（复用 `results_accuracy`） |
| 特征增强 | 主客分拆近况、阵容质量、联赛强度编码 |
| 模型升级 | 样本数百场后可换 GBDT + 概率校准 |
| 历史回灌（可选） | 配额允许时按赛季补历史，缩短积累时间；非必须 |
| API/前端标注 | 展示当前 `market_baseline` / `ml` / `poisson_ml` 来源 |

#### M.5 串关推荐（**门禁：ML 就绪后再做**）

> **不做时机**：模型尚未稳定战胜盘口基线时——单场优势不足，2～8 串会放大误差，产品价值低。  
> **开门条件**（建议同时满足）：  
> 1. 推断已稳定 `source=ml`（`model-status` 显示合格 artifact）  
> 2. 标签样本明显超过训练阈值（建议再攒一批赛果后再开，避免刚切 ML 就串关）  
> 3. 赛果页已能对照评估单场命中（便于串关复盘）

| 项 | 说明 |
|----|------|
| 产品 | 独立「串关」页：2～8 串 1；展示算法推荐组合（非滚球跟单） |
| 优化目标 | 先定一种主目标（最高联合命中 / 相对盘口期望价值 / 带约束最优），可多档 |
| 算法 | 候选场次上近似搜索（贪心 / beam / ILP）；默认场次独立乘积；注明偏乐观 |
| 数据 | **只读本地** ML 概率与盘口；不为串关额外打官方接口 |
| 约束 | 开赛后冻结；联赛分散、单场概率下限等可配 |
| 验收 | 给定日期 + \(k\) 返回 Top N；与单场 `ml` 概率一致可复算 |

列入里程碑 **M7**（见 §6）；**不**挤占当前阶段 M 积累与自动切换。

### 阶段 M-AH — 让球穿盘 ML（已实现主链路 · 样本积累与门禁中）

> **动机**：1X2 与让球是不同市场；同一天可既有「主胜 + 让球负」（主让 -0.5 主侧高水）又有「客胜 + 让球胜」（主受让 +0.5 主侧低水）。写死水位规则无法稳定覆盖，需单独学 **「主队是否穿盘」**。  
> **原则**：与 1X2 ML 同架构——本地积累标签 → 样本够自动训练 → 推断切换；**不足样本时保留极简规则兜底**（仅结构性双选 + 无盘口空态）。  
> **不做**：滚球让球、多庄家套利、实时水位跟踪。
>
> **当前状态**：AH 特征与标签、回填/训练命令、自动训练钩子、ML/启发式推断及赛果页让球命中统计均已接入；是否切到 ML 仍由样本量与验证集基线门禁决定。

#### M-AH.1 问题定义

| 项 | 约定 |
|----|------|
| 预测目标 | 赛前快照时刻 **主盘 Asian Handicap** 上，主队是否 **穿盘**（cover） |
| 标签 `ah_label` | `cover` \| `no_cover`；整球线走水 `push` **不入训**（或单独三分类，首版二分类+过滤） |
| 结算 | 90 分钟比分 + 赛前冻结 `ah_line`：`x.25/x.75` 拆成相邻两档半盘，明确全赢/赢半/走水/输半/全输；保存方案按拆分后的返还系数精确计算奖金。整数非零盘保留让胜/让平/让负三项，`line=0` 且平局统一走水 |
| 主盘选取 | 与列表/详情展示一致：取 `pre_match_data.odds_json`（或 `odds_opening_json`）中 **第一条有效 AH 线**；分析时冻结写入特征行。主盘只认**全场盘**：`prematch_package._is_full_match_bet` 会挡掉半场 / 组合 / 角球等变体（曾把 `Goals Over/Under First Half` 当大小主盘，卡片推荐 小(2.25) 却在玩法区给出半场 0.5） |
| 与 1X2 关系 | **独立模型**。普通详情展示由 `ah_predictor._agree_with_recommendation` 约束 `|让球线| ≤ 0.5`；标 `[荐]` 的日推在 Top-N 前执行更严格的联合闸：以页面可见 1X2 为准，只能在真实盘口线与去水水位支持时修正让球侧，比分只能从已有候选中重选。`胜/平 + 主让-0.75` 等无法唯一映射、修正侧水位不支持或无匹配比分候选的场次跳过并由后续候选补位，绝不改盘口线或凭空造比分。真源 `recommendation.consistency.validate_pick_consistency` |

典型分歧（必须靠模型/特征学，不宜写死）：

- 1X2 主胜，主让 -0.5 但 **主侧水位 > 客侧** → 市场看「难穿盘」  
- 1X2 客胜，主受让 +0.5 但 **主侧水位 < 客侧** → 市场看「主队不败穿盘」  

#### M-AH.2 特征（`AH_FEATURE_VERSION = ah_v3`）

在现有 `FEATURE_NAMES`（1X2）基础上 **追加** 让球专用列（训练/推断向量分开或拼接，推荐 **拼接同一行** 便于 join）：

| 分组 | 特征 | 说明 |
|------|------|------|
| 盘口 | `ah_line` | 有符号让球线（主让为负），归一化到约 [-2.5, +2.5] |
| 盘口 | `ah_home_odd`, `ah_away_odd` | 主盘两侧欧赔 |
| 盘口 | `ah_water_diff` | home − away（正 → 主侧相对高水） |
| 盘口 | `ah_implied_cover` | 由两侧赔率反推的穿盘概率（去 margin 后） |
| 盘口 | `ah_line_abs`, `ah_line_shallow`, `ah_line_deep` | 浅/中/深盘 one-hot 或 tier 标量 |
| 交叉 | `mx_home_prob`, `mx_draw_prob`, `mx_away_prob` | 当时 1X2 模型输出（或赔率隐含） |
| 交叉 | `mx_vs_ah_gap` | 1X2 主胜概率 − AH 穿盘隐含（市场分歧强度） |
| 联赛 | `league_tier_top5`, `league_tier_asia`, … | 与 `league_names`/配置 ID 桶一致，避免每联赛 one-hot 过稀 |
| 元 | `has_ah_market` | 是否有有效 AH（无则 whole-head 不输出） |
| 副盘 | `ah_has_level_line`, `ah_level_away_hot`, `ah_aux_away_hot_share` | 让 0 / 其它档只作参考：客水更低则压低主盘穿盘倾向；标签仍只结主盘 |
| 走水 | `ah_home_odd_drift` 等 | 同线初盘→即时盘 |
| 分段走水 | `ah_mid_*`、`ah_late_*` | 中盘−初盘、临场−中盘；缺档 `present=0` 且变化量为 0 |

**不新增官方 API 调用**；中盘 / 临场从已有刷新里按开赛倒计时打标（中盘：T-10h～T-3h 最接近 T-6h；临场：T-3h～开赛取**最新一次**，晚间半小时刷新即封盘）。写入 `odds_mid_json` / `odds_late_json`。旧 `ah_v2` 权重因版本不匹配自动忽略，需重新训练。

#### M-AH.3 数据与表结构

**方案（推荐）**：扩展 `match_features` 同表同行，避免双表 join。

| 字段 | 类型 | 说明 |
|------|------|------|
| `ah_line` | Float, nullable | 冻结主盘线 |
| `ah_home_odd`, `ah_away_odd` | Float, nullable | 冻结主盘水位 |
| `ah_features_json` | Text, nullable | `AH_FEATURE_NAMES` 有序 JSON |
| `ah_label` | String(12), nullable | `cover` / `no_cover` / `push` |
| `ah_cover_prob` | Float, nullable | 推断时 P(cover) |
| `ah_model_source` | String(32), nullable | `rules` / `multifactor` / `ml` |

`feature_version` 仍为 1X2 的 `v1`；让球版本用 **`ah_feature_version=ah_v3`** 独立演进。

**写入时机**（与 1X2 对称）：

```
赛前分析 / 赔率入库后
  → extract_ah_features(package, probs_1x2)
  → persist_match_features(..., ah_*)
固定同步批次回写赛果
  → settle_ah_label(home_goals, away_goals, ah_line)
  → 写 ah_label（push 可写 NULL 或 push 字符串）
```

**回填**：`manage.py backfill-ah-features` — 从已有 `pre_match_data.odds_json` + 完场比分补历史行（本地库有则做，**不**为回填打官方 API）。

#### M-AH.4 模型与训练

| 项 | 首版 | 样本多后 |
|----|------|----------|
| 算法 | **二元 Logistic**（与 1X2 softmax 并列） | 可选 GBDT + Platt 校准 |
| artifact | `data/models/ah_v3_weights.npz` + `ah_v3_meta.json` | 旧 v2 文件不再加载 |
| 阈值 | `ML_AH_MIN_TRAIN_SAMPLES` 默认 **80**（二分类，且需覆盖多档盘口） | 可调 |
| 切训条件 | `ML_AH_AUTO_TRAIN=true` 且 labeled 行数 ≥ 阈值且较上次训练有新增 | 同 1X2 |
| 验证 | **按日期 hold-out**（禁止随机打乱同联赛同日） | 分 league_tier、分 line tier 报准确率 / Brier |
| 基线对比 | ① 永远跟低水一侧 ② 当前规则层 ③ 仅 `ah_implied_cover` | 赛果页加 AH 命中统计 |

**推断优先级**：

```
无 AH 盘口 → handicap_lean = 「缺少盘口数据分析」
有 artifact 且 source=ml → P(cover) → 让球胜/负 + 「模型穿盘 xx%」
样本不足 → multifactor 启发（相对水位 + 1X2 分歧，规则极简）
```

**产品输出示例**：

```
让球负（-0.5）· 模型穿盘 38%（与胜平负「主胜」分歧）
让球胜（+0.5）· 模型穿盘 61%
```

#### M-AH.5 代码落点（已落地）

| 模块 | 职责 |
|------|------|
| `backend/app/services/ah_features.py` | `extract_ah_features`, `settle_ah_label`, `AH_FEATURE_NAMES` |
| `backend/app/services/ah_predictor.py` | 训练/推断/持久化；镜像 `ml_predictor.py` 结构 |
| `backend/app/services/prediction.py` | `handicap_lean` 改为 **调 ah_predictor**；删除或降级冗长水位 if-else |
| `backend/app/services/ml_predictor.py` | 不变；1X2 与 AH **解耦** |
| `backend/app/models/match_feature.py` | 新增 AH 列（迁移） |
| `backend/app/services/fixtures_sync.py` | 固定同步批次回写赛果后 `maybe_auto_train_ah_model` |
| `manage.py` | `backfill-ah-features`, `train-ah-model`, `model-status` 展示 AH 段 |
| 前端 | `handicap_lean` 展示理由；可选 badge「ML 让球」 |

#### M-AH.6 里程碑与门禁

| 步骤 | 交付 | 状态 / 门禁 |
|------|------|-------------|
| AH-0 | 标签结算函数 + 单元测试（含 ±0.25/±0.75 拆盘、0 盘走水及整数盘让平） | ✅ 已完成 |
| AH-1 | 特征提取 + 分析/赔率入库时写入 + backfill 命令 | ✅ 已完成；持续积累 `ah_label` |
| AH-2 | 训练 CLI + 自动训练钩子 + `model-status` | ✅ 能力已完成；训练门槛 labeled ≥ 80 |
| AH-3 | `handicap_lean` 切 ML 推断；启发式仅兜底 | 🟡 已接入；须验证集优于基线才启用 artifact |
| AH-4 | 赛果页 **让球命中率**（cover 预测 vs 实际） | ✅ 已完成，与 1X2 命中率并列 |

**与阶段 M 关系**：1X2 ML 继续积累；**不等待** 1X2 切 `ml` 才开 AH——两者标签同源、可并行攒样本。  
**与 M.5 串关关系**：串关仍门禁在 1X2 ML；让球 ML 稳定后可把 AH 作为串关第二市场（**M7 扩展**，另议）。

#### M-AH.7 积累期操作（现在起）

1. 保持六个固定同步批次运行，确保 **`odds_json` 含 AH 主盘** 且完场比分齐全
2. 按需运行 `backfill-ah-features`，用 `model-status` 查看 `ah_labeled`、artifact 与推断来源  
3. **未达门槛或未胜基线前**：使用启发式兜底，不把样本不足包装成 ML 输出  
4. 达标后用「同日主客分歧盘」及赛果页命中率持续回归验收  

### 阶段 A — 赛前数据包（主链路已完成）

目标：一场比赛详情页能看到「分析 + 交锋 + 近况 + 赔率 + 阵容/伤病/替补」，且全部来自本地库。

1. 后端接入并落库  
   - `/odds`（赛前赔率）  
   - `/fixtures/lineups`（首发 + 替补）  
   - `/injuries`（伤病）  
2. 扩展 `pre_match_data`（或关联表）持久化上述结构化数据  
3. 扩展 `GET /fixtures/{id}/analysis`（或拆分子资源）返回完整赛前包  
4. 前端详情页分区展示：概率、赔率、H2H、近况、阵容、伤病、替补  
5. 开赛后预测快照只读冻结值；详情展示包本地优先，缺数据时按点击补拉并落库（定时不做全量预拉）

> 主链路已通；空态取决于官方是否开盘/公布阵容。

### 阶段 B — 准备窗口与调度对齐

1. ✅ 支持按日期窗口拉取和浏览「未来 N 天」赛程（世界杯等提前准备）  
2. 调度与 TTL 策略对齐：远期少拉、临场多拉、开赛后停拉  
3. 将 H2H / 赔率 / 伤病等作为冻结赛前特征；拟合模型必须通过时间验证并战胜盘口基线才接管  
4. 置信度与数据完整度更真实地反映「数据包是否齐全」  

### 阶段 C — 体验与可选增强

1. 积分榜、场地、教练等辅助信息（按需）  
2. 球队 Logo、更完整的联赛/比赛列表筛选  
3. 评估是否嵌入官方 Widgets（仅展示层；Key 需域名限制或代理）  
4. 管理后台简易页（触发任务、看配额）  

### 阶段 D — 工程化与上线

1. 单元测试 / 接口测试（fetcher、analyzer、TTL、API）  
2. Docker Compose（backend + nginx 静态前端；Redis 默认关闭）— 见 `docker-compose.yml` 与 [docs/ALIYUN_ECS.md](docs/ALIYUN_ECS.md)  
3. 清理无用依赖；完善生产环境变量说明  
4. 监控：配额剩余、任务失败告警、**ML 样本数 / inference_mode**  
5. **本地数据 → 云服务器迁移与持久化（勿漏）** — 见下方 D.2  

#### D.2 数据落盘位置与迁云策略

> 产品当前**没有**内置「本机 ↔ 云」自动同步。持久化是单机文件；上线前必须显式处理，否则云上是空库、ML 标签也会丢。

**本机落盘（开发默认）**

| 路径 | 内容 | 是否进 Git |
|------|------|------------|
| `backend/data/football.db` | 联赛/赛程/`pre_match_data`/`match_features`/`api_snapshots` | ❌（gitignore） |
| `backend/data/models/` | 1X2 / AH / 进球分布三套模型的权重与元数据 | ❌（gitignore） |
| `backend/logs/` | 运行日志 | ❌ |

热缓存默认 `REDIS_ENABLED=false`（内存），**不落盘**，迁云时不必拷。

**首次上云（二选一，写进发布清单）**

| 方案 | 做法 | 适用 |
|------|------|------|
| A. 整库 + 模型拷贝 | `python manage.py export-sqlite` 后把导出库与整个 `data/models/` 拷到 ECS 的 `backend/data/`（`scripts/deploy-aliyun.ps1`）；Key 已在库内 | 首发需与本机模型、历史推荐基线和推断结果对齐 |
| B. 云上重拉 | 只部署代码；`init-db` → 管理员配置 Key → fetch/sync → 调度自行积累 | 干净环境；本机标签不迁移 |

约束：

- **禁止**把 `football.db` / Key 提交进 Git 当「同步」手段  
- 仅部署 Git 代码不会带上本机数据库、`app_settings` 激励态或模型产物；要求首发对齐时，`football.db` 与整个 `data/models/` **缺一不可**
- 服务器必须使用相同代码提交、锁定依赖、联赛目录，并对齐 `ML_*`、`SCHEDULER_TIMEZONE`、`API_HISTORY_MODE` 与详情预拉开关；固定随机种子只能保证相同输入下可复现，不能弥补训练样本、赛前数据或依赖版本差异
- 本机与云**不要长期双写**同一逻辑库；上线后以**服务器为权威源**，本机仅作开发或单向备份  
- 拷库前尽量停掉两边写库进程，避免 SQLite 半写入损坏  

**上线后日常（阶段 D 交付时应具备）**

1. 服务器数据目录挂持久卷（Docker volume / 主机目录），重启不丢库  
2. 定期备份 `football.db`（及 `data/models/`）到对象存储或异地；保留保留点  
3. 文档化：从备份恢复的步骤；`DATABASE_URL` / `ML_*` / Key 的生产配置清单  
4. （推荐演进）生产改 **PostgreSQL**（或同等），减少单文件拷贝与并发限制；SQLite 可留作本地开发  

**M6 验收补充**：发布说明中必须写清「权威数据在哪、如何备份、首次是方案 A 还是 B」。

---

## 6. 里程碑建议

| 里程碑        | 完成标准                         | 状态     |
|------------|------------------------------|--------|
| M0 工程骨架    | 服务可启动、健康检查通过                 | ✅      |
| M1 数据 MVP  | 联赛 + 今日赛程入库，前端可浏览            | ✅      |
| M2 分析 MVP  | 单场胜平负概率 + 推荐可展示              | ✅      |
| M3 本地优先    | 落库、开赛后冻结、密钥本地化               | ✅      |
| M4 赛前数据包   | 赔率 + 阵容 + 伤病 + 替补 + H2H 展示闭环 | ✅ 主链路完成；空态取决于官方覆盖 |
| M5 模型与调度增强 | 本地积累闭环 + 自动训练切换 + 未来赛程准备 | 🟡 能力已接入，持续积累样本与校准 |
| M5-AH 让球 ML | 穿盘标签 + AH 特征 + 二元模型；`handicap_lean` 按门禁切 ML（见 M-AH） | 🟡 主链路已完成，等待样本/基线门禁 |
| M6 可上线     | 测试 + 部署 + 配额可控 + **数据迁云/备份策略落地（D.2）** | ⬜      |
| M6-U 用户/VIP | 真登录 + 收藏/方案/历史窗口等账号权益；不提供官方刷新权（见 [docs/AUTH_VIP_QUOTA.md](docs/AUTH_VIP_QUOTA.md)） | 📋 设计已写入，代码未开 |
| M7 串关推荐   | 2～8 串 1 最优组合页；**仅 ML 稳定输出后**（见阶段 M.5） | ⬜ 门禁未开 |

---

## 7. 技术栈与关键路径

| 层   | 技术                                                                        |
|-----|---------------------------------------------------------------------------|
| 后端  | FastAPI · SQLAlchemy · SQLite · Redis/fakeredis · APScheduler · httpx     |
| 前端  | Vue 3 · Vite · TypeScript · Naive UI · ECharts · Axios                    |
| 数据源 | API-Sports（`v3.football.api-sports.io`），Key 存库内管理员配置 |

本地启动见根目录 [README.md](README.md)；后端细节见 [backend/README.md](backend/README.md)；前端见 [frontend/README.md](frontend/README.md)。

---

## 8. 风险与约束

| 风险           | 应对                                   |
|--------------|--------------------------------------|
| 官方 API 日配额有限 | 每天六个固定公共批次 + 全前端只读本地；见 [docs/AUTH_VIP_QUOTA.md](docs/AUTH_VIP_QUOTA.md) |
| Key 泄露       | 库内管理员配置（界面只显示末 4 位）；勿提交 Git；勿写前端 |
| 阵容/赔率临场才稳定   | 临场窗口提高刷新频率，但仍在开赛前停止                  |
| 分析模型过简       | 1X2 设盘口基线门禁；比分/O-U/BTTS 使用独立进球分布；让球单独训练 |
| 串关过早上线       | 门禁见 M.5：须 `source=ml` 且样本充足后再做；乘积假设偏乐观 |
| 上云丢库 / 双写冲突  | 明确权威源；整库拷或云上重拉（阶段 D.2）；禁止用 Git 同步 db |
| SQLite 单文件 concurrent | 单实例部署；生产演进 Postgres + 卷备份           |

---

## 9. 文档维护

- 本文档随阶段推进更新「已完成 / 未完成 / 将来要做」勾选状态。  
- 产品边界变更（例如是否做直播）须先改本文第 1 节，再改代码。  
- 接口细节以运行中的 `/docs` 与代码为准；本文描述进度与规划，不替代 OpenAPI。
