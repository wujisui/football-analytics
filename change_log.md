# 产品完善路线

从 **2026-08-24** 起，本文件按日记录产品边界、管理员运维与配额相关的完善过程。  
写入前先按代码核对是否已落地：已落地的写进当日记录并注明真源，未落地的才留在「待办」。

---

## 待办

- **用户管理页未落地**。后端没有 `admin/users` 一类接口，前端也没有入口。计划：表格 + 分页展示用户名、账号、注册时间、角色、VIP，管理员可手动授权；**先定角色有几种**（至少普通用户 / 运维 / 管理员，默认普通用户），再动手。角色与 VIP 的划分口径见 `docs/AUTH_VIP_QUOTA.md`。
- **滚动时隐藏浏览器地址栏 / 底部操作区**。用户在其他网页见到向下滚隐藏、反向滑再出现。本项目未做；实现依赖移动浏览器对 `overflow` / 全屏滚动链的限制，成本未评估，先不改。

---

## 2026-08-24（周一）

当天完成了三块：亚盘特征升到 `ah_v2`、订阅/调度边界重构、管理员运维与开关的展示收敛。以下按主题记录，并附真源，便于后续回溯。

### 一、亚盘学习：`ah_v1` → `ah_v2`

原始需求：一场比赛盘口有多个让球，每个让球数都有对应赔率，用赛果回溯这些赔率做机器学习来预测盘口胜负；同时记录初盘与即时盘（第一次拉盘口记初盘，之后记即时盘）。

已落地（`backend/app/services/ah_features.py`、`ah_predictor.py`）：

- `iter_ah_quotes` 读出完整亚盘报价 `(line, home_odd, away_odd)`，主盘在前，不再只看主盘一条线。
- 新增副盘特征：`ah_has_level_line`、`ah_level_water_diff`、`ah_level_away_hot`、`ah_has_aux_lines`、`ah_aux_away_hot_share`
  。让 0 与其它档只作主盘参考， **不改训练标签**。
- 新增初盘 → 即时盘同线升降水特征：`ah_opening_same_line`、`ah_home_odd_drift`、`ah_away_odd_drift`、`ah_water_drift`、
  `ah_away_steam`。
- 让球特征升到 `ah_v3`：从已有盘口刷新里打标中盘（T-6h）和临场（T-3h 起到开赛的 **最新一次**），特征为中盘−初盘、临场−中盘（
  `ah_mid_*` / `ah_late_*`）。中盘窗口 T-10h～T-3h 取最接近 6h；临场在窗口内每次刷新都覆盖成更新的一份。不新开定时任务、不额外打官方。缺档
  `present=0`。展示仍用初盘/即时盘，后续再优化。
- 初盘/即时盘本身由同步批次维护：未来三天首次可用盘口冻结为初盘，成为今天后刷新即时盘。
- 拉盘范围已统一为三条：场次仍在【比赛】；轻刷、批量与详情更新只处理【比赛】列表的「今天」即尚未开赛场次里最早的场地比赛日（不是浏览器/调度时区日历日，避免凌晨翻日后无法更新仍显示为今天的盘口）；联赛必须出现在「热门联赛」配置页（
  `leagues.is_catalog=true`），是否勾选热门不影响拉盘。目录外赛事即使出现在【比赛】也不拉盘口。
- 未来三天只补缺盘，不刷新已有盘；成为比赛日后轻刷才覆盖即时盘，因此 `odds_json` 保留开赛前最近一次成功采集。详情非比赛日隐藏「更新盘口」。
- `odds_snapshot.py` 仍在写入与读取两端校验 `scraped_at < match_start_time`；开赛后不请求盘口，旧冻结数据不回写修复，缺采集时钟的历史盘按既有赛前盘兼容读取。
- 一份盘口只取一家庄家（`parse_odds_payload` 先按庄归集，再选覆盖核心玩法最多的那家，平手按 `BOOKMAKER_PRIORITY`
  ；只有主庄没开的玩法才回退次级庄）。初盘允许先由次级庄兜底，但主庄一开盘就用主庄首盘替换（`board_outranks`），开赛后不再改写。否则「初盘
  0 → 即时盘 -1」只是换庄，不是真实盘口移动，升降水特征会失真。

### 二、订阅与定时任务边界

「打开免费配额」改名为「订阅」，开关轨道内写明已订阅 / 未订阅。已订阅视为至少 Pro（日配额 7500），并据此重排全部官方请求。真源
`backend/app/tasks/scheduler.py`、`fixtures_sync.py`。

- **已订阅**：11:00 跑每日定时完整批次（当前比赛日刷新目录盘口、未来三天只补目录缺盘，详情预拉仍只今天/明天）；轻刷只刷新当前比赛日仍在【比赛】的目录盘口并重算日推。07:00 / 「只更新赛果」回写当前比赛日的昨天和今天终场比分。
- **未订阅**：08:05（UTC 00:05）只拉当天赛程；11:00 跑昨天赛果 + 今天赛程/目录盘口 + 训练 + 日推 +
  清理，跳过积分榜与详情预拉；22:00 只轻刷当前比赛日仍在【比赛】的目录盘口并重算日推。07:00 同样按比赛日昨天+今天回写赛果。
- **赛程窗口**：已订阅本地保留今天起 8 天滑动窗口，每天只补窗口末端缺失的一天，不重复请求重叠的 7 天（
  `missing_subscribed_fixture_days`）。这条同时解决了旧记录里「开启全量点同步没有拉未来 7 天赛程」。
- **详情预拉不再限量**：`run_scheduled_full_detail_enrich` 以 `limit=None` 跑全部候选，统计里保留 `unlimited: True`
  。旧记录里「全量获取不要限制 10 场」已不复存在。
- **删除「定时全量获取详情」平行开关**，能力收敛到订阅状态。
- **单场盘口**：无论是否订阅，管理员只可在详情「我的预测」更新今天仍在【比赛】的目录联赛盘口，不补其他详情（
  `POST /fixtures/{id}/odds/refresh`）。非比赛日或目录外赛事隐藏按钮，后端仍按同一边界与 `require_admin` 强制拦截。
- **立即同步**：只在已订阅时可用，不限每日次数；新增热门联赛后可再次同步，每次仍消耗官方配额并受请求锁限制。

### 三、管理员运维与开关展示

目标：管理员一眼看出配额还剩多少、这次花了多少、上次同步是什么时候；开关状态写在轨道里。

1. **官方剩余**移到「管理员运维」卡片标题右侧（`n-card` 的 `header-extra`）。
2. **本次消耗**：每次官方响应在缓存里累加请求数（`CacheService.note_api_response`），批次结束用结束计数减开始计数，与剩余并排展示。未配
   Key 等未实际执行的批次不改写记录。
3. **上次同步时间**：结束时刻、批次类型（完整批次 / 盘口轻刷 / 当天赛程）、成败写入 `app_settings.last_sync_run`
   ，刷新页面或换浏览器仍在。文案跟在「同步官方 API 数据」标题后面，如「上次同步 2026-08-24 19:19 ·
   完整批次」，失败时同位置标失败。进程刚启动、内存还没有剩余值时，接口回退到上次批次落库的剩余数。
4. 这条同时补上了旧记录里「同步中刷新网页，按钮加载态被重置、看不出是否完成」：`useAdminSync.hydrateStatus` 已从后端
   `active_tasks` 恢复进行中状态，现在加上持久化的上次同步结果，刷新后两种情况都有交代。
5. **订阅开关**对齐亚盘/竞彩：轨道内「已订阅 / 未订阅」，删掉开关下方重复的状态小字。
6. **早间盘口刷新**轨道内「已开启 / 已关闭」。抽出公共 `TextSwitch`
   ，约定以后此类开关都把状态写进轨道；深色模式开关一并改成「深色 / 浅色」。

### 四、更早的零散反馈（已核对，均已落地）

- **赛果列表进行中的场次排前面**：`utils/fixtureSort.ts` 的 `sortFixturesFavoritesFirst` 第一优先级即
  in-play，其次日推、关注，最后开赛时间。
- **我的方案新增中奖统计**：`views/Mine/plans/index.vue` 的 `PLAN_STAT_ITEMS` 给出中奖 / 已结算 / 全部三档，配色对齐赛程「历史统计」。

---

## 2026-08-25（周二）

### 悬停信息收敛

- 对阵名称过长时，原来的完整队名浮层会与外层详情 tab 浮层重叠。现改为同一可点区域只弹一个浮层：对阵悬停只显示完整「主队 vs
  客队」，不再显示「统计」。
- 让球主盘数字无论是否有副盘，悬停都显示完整让球盘口。只有一个盘时单列显示；初盘与即时盘都有时左右并排，
  **左侧初盘、右侧即时盘**，每列按主队水位 / 盘口 / 客队水位展示全部让球档。是否算两份盘口 **只看采集时间**
  ：初盘严格早于即时盘才并排，采集时间相同（初盘刚由当前这份冻结或替换）只显示「即时盘」一列。判定真源是 `isOpeningDistinct`
  ，列表与详情「我的预测」共用，不逐档比水位、不要另写一套。
- 列表接口新增冻结初盘摘要 `odds_opening_snippet`，与即时盘 `odds_snippet` 分开返回；只读本地
  `pre_match_data.odds_opening_json` / `odds_json`，不增加官方 API 请求。
- 让球浮层是 **一张卡片装两个盘口表**：外层卡片一层底色（`--fa-bg-elevated`），两张盘口表另一层底色（`--fa-bg-soft`
  ），靠底色层次区分，不再是两张带间隔的独立卡片。详情页 `PreMatchOddsTable` 的同款浮层共用这层外壳。
- 初盘 / 即时盘标题后用次级文字标各自的采集时间，格式 `08-25 09:31`（`formatLocalMonthDayMinute`）。时间来自摘要新增的
  `captured_at` 字段，同样只读本地。
- 摘要只有 `_odds_snippet_from_package` 一个构造点，列表与详情接口都走它。详情响应会经 `mergeDetailIntoListFixture`
  覆盖列表行，详情曾另建一份不带 `captured_at` 的摘要，导致点开详情再返回后列表把初盘与即时盘并成一份。新增摘要字段务必只改这一处。

### 圆角统一

- 新增 token `--fa-radius-card`（`src/styles/base.css` 的 `:root`），值 `3px`，与 Naive `common.borderRadius` 一致。浅色主题后来补了表面色
  `theme-overrides`（见 2026-08-27），但未覆盖圆角，全站 `n-card`（含比赛卡 `.predict-card.standalone`）仍用这个值，自定义表面跟着它就不会再出现「浮层比卡片圆」的割裂。
- 已把卡片 / 面板 / 浮层 / hover 高亮块的字面圆角（原先散落的 2/4/6/8/10/12px）全部改成 `var(--fa-radius-card)`
  ：让球浮层、比赛卡分区与让球行、详情基础信息与 tabs 容器、关注页手机卡片、空态卡、联赛菜单图标块、H2H 徽章、投注截图预览等。
- 保留不动的只有形状本身决定的圆角：日期圆点 `50%`、回到顶部胶囊、滚动条 `999px`、1~2px 宽指示条，以及显式取消圆角的 `0`。
- 约定写入 `.cursor/rules/frontend-ui.mdc`「圆角」小节：以后禁止再写字面圆角值，改全站圆角只动这一个 token。

### 只更新赛果

- 管理员设置新增「只更新赛果」。只走 `scheduled_fixtures_sync(mode=results)`：按日全球 `fixtures?date=`
  回写终场比分并打训练标签，不拉盘口/赛程/详情、不重算日推。当时未订阅回写昨天+今天（2 次请求），已订阅近 4 天；**08-29 起已订阅也改为昨天+今天**，见当日记录。
- 与「立即同步」共用 `_sync_lock`，不能同时跑；接口在锁占用时返回 409。早间盘口刷新（04/06/08/10）只动今天仍在【比赛】的目录盘口，关了也不会挡住赛果。

### 已订阅晚间半小时盘口

- 已订阅 21:00 至 24:00（含 00:00）每隔半小时轻刷今天仍在【比赛】的目录盘口并重算日推：21:00、21:30、22:00、22:30、23:00、23:30、00:
  00。白天仍是 11:55、02/14/16/18/20；04/06/08/10 继续由早间开关控制。未订阅仍只有 22:00。
- 管理员「订阅」说明跟着开关走：正文分已订阅/未订阅两套，并列出后端 `sync_times`；早间开关开/关也会改一句。

---

## 2026-08-26（周三）

### 联赛目录改到数据库

- 可勾选目录、分类与热门状态以数据库 `leagues` / `league_categories` 为唯一真源；`config/leagues.json` 只在首次建库导入种子。系统保护联赛仅五大联赛、荷甲、葡超、欧冠、中超，不可删、不可改官方 ID。其余目录联赛可改正官方 ID，也可在密码确认后连同该联赛全部历史删除再重加。真源 `league_catalog.py`、管理员「热门联赛」页。
- 新增/改正官方 ID 前可点「核对」：`GET /admin/settings/leagues/{id}/lookup`，有 7 天缓存则不打官方，未命中才请求一次 `GET /leagues?id=`，不拉赛程盘口。

### 批量更新盘口

- 运维页「批量更新盘口」使用 `prematch_odds_sync`：前端只提交【比赛】当前筛选中 `match_day` 为今天的场次 ID；后端按持久化比赛日与 `leagues.is_catalog=true` 再过滤，明天及目录外赛事即使被提交也排除。已有盘口刷新即时盘，缺盘口补齐，不拉赛程、赛果、积分榜或详情。真源 `fetcher.sync_odds_for_prematch_fixtures`。
- 定时刷盘覆盖「热门联赛」配置页里的全部目录联赛，`is_hot` 只控制默认展示筛选。【比赛】列表当前筛选只改本机显示，不改变定时拉盘范围。

### 初盘：第一次可用盘口即冻结

- 原先只有 11:00 完整批次 / 补齐任务带 `set_opening=True` 才写初盘；轻刷和详情「更新盘口」只写即时盘。后来加入热门、或管理员手动首拉的场次会一直没有初盘。
- 现改为写入侧自动判定：任一定时、补齐或管理员单场刷新，首次成功拿到可用盘口同时写初盘和即时盘；后续只更新即时盘。次级庄初盘仍允许主庄开盘后替换；开赛后不再改写。旧数据只有即时盘时，下次刷新先把旧即时盘收成可恢复的初盘。真源 `should_write_opening`、`_upsert_odds_and_recompute`。

### 让球推荐与命中不再对冲刷绿

- `负/平` + `+0.25` 这类四分盘不再结构性输出「让胜/负」。首选比分若落在胜平负允许范围内，按该比分结算成单选（例：`1-1` + 主队 `+0.25` → `让胜(+0.25)`）。整数盘仍可出「让胜/平」「让负/平」。真源 `ah_predictor._structural_pick` / `predict_handicap`。
- 预测准确率只认方向对：全赢、赢半算中；输半、全输算不中；走水不进分母。已冻结的「让胜/负」对冲推荐不再评分。个人方案资金结算仍按输半 0.5 倍返还，与预测命中分开。真源 `asian_result_counts_as_hit`、`results_accuracy.evaluate_fixture_prediction`。

### 运维「更新赛果」后赛果页自动重载

- 赛果列表把按日场次和历史走势缓存在内存；同步成功后若不作废，回到赛果页仍显示旧比分。
- 完整批次或「只更新赛果」完成后走全局 `notifyLocalDataChanged` / `clientDataEpoch`；只有当前打开的赛果页才重拉。让球规则切换同样作废缓存，回到赛果页再读。真源 `useClientDataRevision.ts`、`clientDataEpoch.ts`。

### 详情展示

- 未开赛场次在「历史交锋」顶部插入本场，比分 `VS`、赛果「未开赛」；不计入胜率/进失球统计，开赛后不再插入。面包屑比赛名前面加本地开赛时间（`formatTime`）。真源 `H2HTab.vue`、`BasicInfo.vue`。

### 界面入口收敛

- 桌面顶栏右上角亚盘/竞彩开关删除，只留「我的 → 偏好设置」。手机日期选择器加 `input-readonly`，点日历不再唤起键盘。

### 晚间密刷盘口

- 运维新增「晚间密刷盘口」持久开关（默认关）。已订阅盘口轻刷维护两套独立完整时刻表：默认方案为 02:00、11:55、14:00、16:00、18:00、20:00、21:00 至 00:00 每半小时；密刷方案为 02:00、11:55、14:00、16:00、16:55、17:25 … 01:55（开赛整点/半点前 5 分钟）。开关只在两张表间切换，04/06/08/10 早间时刻可独立叠加；未订阅仍只有 08:05 / 11:00 / 22:00。真源 `scheduler.SUBSCRIBED_DEFAULT_ODDS_SLOTS` / `SUBSCRIBED_DENSE_ODDS_SLOTS`、`app_settings.subscription_dense_odds`。

### 球队译名范围

- 默认只翻管理员勾选的热门联赛球队，不再按目录全量或当日【比赛】列表扩范围。手工 `BY_ID` 必须跑 `audit-team-names`。规则写入 `.cursor/rules/team-name-translation-scope.mdc`。

---

## 2026-08-27（周四）

### 管理设置本地缓存

- 热门联赛目录、订阅/调度开关和官方 Key 摘要写入当前**标签页**的 `sessionStorage`：关标签或关浏览器即丢，同一标签 F5 仍在。本标签第一次打开该子页才打接口，之后再进只读缓存；「恢复」以及保存/新增/修改/删除成功后用接口返回值覆盖缓存，与库一致。退出登录或鉴权失效时清除管理员缓存。
- 运维任务状态只在本次前端会话首次进入时恢复一次；本机触发任务后继续用模块级状态与轮询跟踪，避免每次切换路由都额外请求 `/admin/tasks`。

### 管理入口与同步边界

- 「主题与玩法」更名为「偏好设置」。
- 管理员「立即同步」取消每日一次限制：仅已订阅时可用，可在新增热门联赛后再次执行；未订阅时前端禁用且后端拒绝，运行中的官方批次仍由请求锁防重。
- 已订阅 11:00 / 立即同步的盘口窗口从「今天+明天」扩大为今天刷即时盘、未来三天补缺盘并冻初盘；轻刷与详情预拉不变。真源 `FULL_BATCH_FUTURE_ODDS_DAYS`。
- 每次后端定时任务更新盘口会重新计算推荐，所以每次更新盘口后让前端跟着刷新，不然新计算的推荐无法及时给到用户。
- 后端批次完成后写入 `app_settings.client_data_revision`。前端只有一条全局通知：`notifyLocalDataChanged` 作废缓存并 bump `clientDataEpoch`；当前页才重拉。30 秒版本检查只在【比赛】/【赛果】激活且未息屏时运行，息屏、切到后台或进入关注/我的/详情即停表。运维按钮和详情更新盘口成功后直接通知。

### 亚盘让平：显示走水、不进日推

- 亚盘规则下独立「让平」渲染为「走水」，竞彩不变。真源 `ah_features.adapt_handicap_lean_for_ruleset`，前端 `utils/handicapDisplay.ts` 同步。
- 独立让平不再成为日推主推：`auto_favorites._score_handicap` 直接拒绝该候选；`favorites.list_auto_pick_responses` 过滤掉历史已冻结的让平自动记录，老数据不用回刷。

### 联赛目录保护：只保护九个

- 保护名单收敛成常量 `PROTECTED_CORE_LEAGUE_IDS`（英超、西甲、德甲、意甲、法甲、荷甲、葡超、欧冠、中超），管理端 `_hot_leagues_payload` / `create_catalog_league` 与建库种子都读它，不再各写一份。真源 `league_catalog.py`。
- 老库里多余的保护位由启动时 `init_db → sync_catalog_protection` 一次性对齐（期间重建 `prevent_protected_league_unlock` 触发器），不需要手工改库。
- 「改了后端重启仍没有删除按钮」是前端 `sessionStorage` 旧缓存：缓存键升到 v3。关标签后缓存自然没了，同一标签内不再每次进入都核对。顶栏 keep-alive 只缓存【比赛】壳和【关注】，不缓存【我的】；PC 点「我的」会回到离开前的子页，手机仍进个人主页。

### 联赛 ID 名称对照与译名补全

- 新增 `docs/LEAGUE_ID_NAMES.md`：按库导出目录内 / 目录外联赛的官方 ID、中文名、国家与热门 / 保护标记，供对号入座。
- `league_names.py` 补齐此前仍是英文的目录外赛事译名，仍按「官方 ID > 英文名+国家 > 全球唯一名称」优先级写，禁止纯名称兜底歧义赛事。
- 新增 `python manage.py backfill-league-names`，把译名回写本地 `leagues.name`，不消耗官方配额。

### 批量任务运行时的单场「更新盘口」

- 后台批次（同步赛果 / 批量更新盘口）运行时，详情点「更新盘口」原先直接 500。现在 `POST /fixtures/{id}/odds/refresh` 先查 `official_sync_busy()`，占用时返回 409 并带文案，不再打官方。
- 前端不再发这次无效请求：详情页接 `useAdminSync` 的 `officialSyncBusy`（进入详情时 `hydrateStatus` 恢复一次状态），按钮置灰并在「我的预测」顶部提示后台任务进行中。

### 比分提示与胜平负推荐对齐

- 比分示例经大小球、双方进球调整后，必须再落回胜平负推荐允许的结果集，杜绝「推荐负却给出主胜或平局比分」。真源 `prediction._align_score_with_outcomes`，回归用例按截图场景补在 `test_prediction_copy.py`。

### 让球倾向与胜平负不再打架

- 分析器（按最可能结果，允许双选，供竞彩参考）与日推（单选）是两条轨道，允许不同向，卡片上不混排。日推选场口径在 8 月 28 日改为校准置信度，见当日记录。
- Top-N 截取前过 `recommendation.consistency.validate_pick_consistency`：以日推自己的单选方向为唯一基准，配一条该方向不会输的真实让球侧，加一份按该方向重算的比分候选。无法表达的场次记录原因并从完整排序池补位，不改盘口线。
- 闸门不再校验「修正侧去水水位是否支持」：它表达的是日推自己的方向，不是修正分析器，市场偏向另一边不构成淘汰理由。
- 分析器侧：平手盘的平局是退本结算，不是推荐项。优先读取同庄家其他让球档位中最接近均水的非零盘判断主客方向，其次读取平手盘水位，最后用方向明确的胜平负推荐兜底；例如「胜/平 + 0盘 + 比分1-1」显示 `让胜(0)`，不能显示「走水(0)」。真源 `ah_predictor._level_ball_pick`。

### 详情「盘口解释」改由后端生成

- 新增 `backend/app/services/market_analysis.py` 作唯一真源：用初盘 / 中盘 / 临场 / 即时盘四阶段（按采集时间去重）生成结构化解释，主盘升降、同档共振、1X2、大小球、计入亚盘返还后的期望收益分开说明。
- 只比较同庄家、同玩法、同档位的水位与去水概率；跨庄换盘只作告警提示，不当成资金流或盘口移动。
- 详情响应新增 `market_analysis` 字段；前端删除 `utils/predictionExplanation.ts`，`PredictionResult.vue` 直接渲染后端结构化结果与告警，不再保留第二套判断。

### 护眼主题：暖米灰、拉开层级

- 为解决浅色大面积表面接近 `#fff`、观看刺眼的问题，新增暖调米灰（纸感）护眼配色：页面底色明显低于卡片一档，配暖阴影；不使用大面积纯白表面。
- 当前取值：内容带外背板 `#d2cbc1`，页面底色 `#e1dbd3`，卡片/弹层/表格 `#f3f0eb`，次级面板/表头/标签 `#eae5dd`，输入框 `#ebe6de`，边框 `#d0c9c0`。正文 `#3a352f`、强调 `#262220`，阴影 `rgba(74, 62, 48, …)`。
- 胜/平/负红蓝、推荐标签、链接蓝等业务强调色保持现有辨识度，不随表面一起改。
- Naive 组件表面一并覆盖，避免 `n-card` / 弹层 / 表格 / 输入框 / 禁用输入 / `n-tag` / `n-tabs` / 代码块仍走近白默认色。
- **真源** `frontend/src/styles/themes/eye-care.css`（`html[data-theme='eye-care']`）；Naive 组件表面色仍在 `theme/presets.ts` 的 `eyeCareOverrides`。规范写入 `frontend/FRONTEND_UI_SPEC.md`。深色与原始浅色不受该配色覆盖。
---

## 2026-08-28（周五）

### 三主题下拉选择

- 恢复原始浅色主题（页面 `#f5f6f8`、卡片 `#ffffff`、次级表面 `#fafafa`），保留 8 月 27 日新增的暖米灰配色并正式命名为「护眼」。
- 「我的 → 偏好设置」把原来的深浅 `TextSwitch` 改为下拉选项，固定顺序为深色、浅色、护眼；新用户默认护眼。
- 本地偏好存储升级为 `fa-theme-preset-v2`：已有深色偏好继续深色；旧版存成 `light` 的暖米灰用户迁到护眼，避免升级后突然变亮。真源 `theme/presets.ts`、`composables/useTheme.ts`、`views/Mine/theme/index.vue`。
- 切换主题只改 `html[data-theme]`，三套主题各自闭合，不再共用 `data-theme='light'`。

### 主题 CSS 归拢到 `src/styles`

- 删除 `src/style.css`。布局与共用 token（圆角、间距、滚动条、胜平负三色）进 `src/styles/base.css`；深色 / 浅色 / 护眼各自 `src/styles/themes/*.css`；`src/styles/index.ts` 统一导入，`main.ts` 只引这一处。
- 表面色以 CSS 为唯一真源，`presets.ts` 删掉 `lightShell` 等 hex 对象，不再 `setProperty`。旧版写在 `<html style>` 上的 `--fa-*` 在切换时清掉。
- `index.html` 在 `<head>` 同步读 localStorage 写 `data-theme`，避免 CSS 到达时先闪错主题。
- 护眼主题表面色后续微调：内容带外背板 / 页面底 / 卡片层级仍以 `frontend/src/styles/themes/eye-care.css` 为真源。

### 【我的】去掉 keep-alive，会话内缓存管理员设置

- 顶栏 `keep-alive` 只缓存【比赛】壳和【关注】，不再缓存【我的】：手机端【我的】只是二级入口，保活会让 Naive 布局重建时重复挂载、热门联赛接口打两次。
- PC 离开【我的】时把当前子路由写入 `sessionStorage`，再点回来打开上次菜单；手机仍进个人主页。登出时清掉记忆。真源 `views/Mine/sectionMeta.ts`、`composables/useAuthSession.ts`。
- 热门联赛 / 运维订阅 / 官方 Key：同一标签页内读 `sessionStorage`，关闭标签才丢；保存成功后立刻写回缓存。并发读用 `api/admin.ts` 的 `inflightAdminReads` 去重。不再每次进入都静默核对。

### 日推管线落地：独赢单选、排除平局、卡片三件套

- 日推主市场回到独赢 1X2，但**只在主胜 / 客胜里选**。亚盘庄家同时开独赢与让球盘，和局并非不可下注；平局概率低、方差大，即使算出正 EV 也会拖垮中奖率，因此不进候选池。真源 `strategy.DAILY_PICK_OUTCOMES`。
- 让 0 同样是让球盘：主胜配让0胜、客胜配让0负，赛果打平按走水退本、不算输，照常给方向。分析器 `_level_ball_pick` 不再把平手盘写成「走水(0)」。
- `[荐]` 卡片整行改用日推三件套（`auto_lean` / `auto_handicap_lean` / `auto_score_hint`），分析器双选文案只留在未入选场次与详情「我的预测」。真源 `useFavoriteFixtures.autoFavoritePick`、`PredictionRecommendationRow.vue`。
- 一致性闸在 Top-N 截取前跑：主胜遇深于主让 1 球、客胜遇深于客让 1 球、缺大小球结论等无法表达的场次记录原因后从完整排序池补位。三件套写入 `favorite_fixtures` 与 `auto_pick_snapshots`，**不回写** `pre_match_data`。

### 日推空池：正期望门槛不可用，改按校准置信度选场

推荐管线一度改用严格 `ev > 0` 后，日推四场与【比赛】`[荐]`、【赛果】推荐 tag 同时消失（两处都读 `fetchAutoPicks()`，池空即一起失效）。按本地库 195 场未开赛实测：

**直接原因**：`recommendation_calibration.json` 全局桶三个方向 `deployable` 全为 `false`——3255 场历史上 Platt 校准的留出 Brier 反而略差于原始市场赔率（home 0.20468 vs 0.20444，draw 0.18924 vs 0.18893，away 0.18726 vs 0.18720）。`_resolve_bucket` 退回 `"raw"`，校准概率 = 市场去水概率，于是

```
EV = 赔率 × 去水概率 − 1 ≡ −抽水 < 0
```

对每一场恒成立。178 场可算场次里最好的一场 EV 也只有 −0.0313；放开平局重算仍是 0，与排除平局无关。

**四条线的边际实测**（本地库，未打官方 API）：

| 玩法 | 模型状态 | 与市场的分歧 | 回测 |
|---|---|---|---|
| 独赢 1X2 | `inference_mode: market_baseline` | 中位 0.0000，467 场里 464 场偏差 < 0.01 | 模型验证 log-loss 0.9810 差于市场 0.9760，系统主动退回市场概率 |
| 亚盘 AH | `inference_mode: ml`，2921 样本 | 中位 0.0301 | Brier / log-loss / 命中率三项均输给市场；`edge>0.08` 整段 ROI +7.9% 但按时间切分为 7 月 +26.4%、8 月 −13.1%，分歧越大亏越多，典型过拟合 |
| 大小球 | Poisson，MAE 跑赢基线 | — | 453 注 ROI −5.3%，前后半段反号 |
| 双方进球 | 同上 | — | 465 注 ROI −3.9%，前后半段反号 |

**结论**：当前没有任何玩法具备可利用的市场边际。「正期望价值注」在模型能力达标前无法成立；旧 `auto_favorites` 每天必出四场靠的是「无正期望时按预期亏损最小排序」，本质是在抽水里挑最便宜的。

**处置**：日推定位为「模型最有把握的四场」，按校准置信度排序，以中奖率为目标；EV 与赔率照常逐场算出并落库供审计。`strategy.decide_match` 不再因 `ev <= 0` 返回空，`pipeline._recommendable_results` 只按方向过滤。`feedback.apply_feedback_to_pick` 的基数从 `pick.ev` 换成 `pick.score`（置信度）——EV 为负时乘奖励系数会把奖励变成惩罚。本地干运行：195 场输入、178 场可算、一致性淘汰 28 场后，4 个比赛日各 4 场共 16 场。回归 `test_negative_ev_candidates_still_fill_the_daily_four`。

**再引入 EV 门槛的前提**：某个玩法在时间留出上同时改善 Brier 与 log-loss 并稳定跑赢市场基线。否则推荐池会再次恒为空。口径已写入 `PROJECT_PLAN.md`、`.cursor/rules/project-scope.mdc`。

附带修掉测试夹具：`test_recommendation_calibration._synthetic_rows` 原先把标签按 home/draw/away 分块铺在时间轴上，拟合器取最近 20% 留出时整段单一类别，任何校准器都赢不了常数基线。改为固定种子打散后，`test_unknown_league_falls_back_to_global_bucket` 才真正考到全局桶回落。

### 日推再平衡：独赢与让球共同竞争风险调整回报

- 用户复核后确认「命中率优先」表述过于绝对：此前只按置信度会把四场集中到约 1.6 的低赔独赢，足球结果不确定，不能用低回报假设换取并不存在的 100% 把握。
- 日推候选扩为两类真实可下注项：独赢主胜 / 客胜，以及主让球盘的让胜 / 让负。让球不再只是独赢入选后的展示说明，而是拥有自己的置信度、赔率、EV、玩法历史权重与结算快照，可直接赢得 `[荐]`。
- 基础评分改为 `校准命中概率 × sqrt(赔率 - 1)`。净赔率平方根是凹收益函数：会明显奖励从 1.6 提升到约 1.9～2.0 的回报，但不会像直接乘赔率那样把长赔冷门推到前面。最低置信度为 40%，EV 保留为审计字段和同分参考，不恢复 `ev > 0` 空池门槛。真源 `strategy.risk_adjusted_return_score`。
- 浅盘 `|line| ≤ 0.5` 用 1X2 概率按真实结算拆分：±0.25 计入退半，0 盘计入走水；深盘读取冻结 `MatchFeature.ah_cover_prob`，经过每日玩法 Platt 校准后，将高于市场公平概率的差异收缩一半，避免把尚未通过时间留出验证的 AH 分歧当成免费优势。
- 所有独赢 / AH 市场候选先按综合分排序，再过同源一致性闸。AH 入选时 `auto_market=ah`、`auto_lean` 保存伴随胜负方向、`auto_handicap_lean` 保存实际让球投注项；冻结快照的 `market/lean` 保存真正用于赛果结算的 AH 选项。【比赛】整行读取日推三件套，`[荐]` 落在实际入选的独赢或让球标签上；【赛果】卡片改为独立的 `[荐]` 标签按快照自己结算（见 2026-08-29 段）。
- 同一场只允许一个玩法入选，防止同场高度相关的两个注占掉两个坑位。
- 本地库干运行（未打官方 API）：195 场输入、178 场可算、483 个市场候选，一致性闸淘汰 28 项后，4 个比赛日均补足 4 场；入选赔率 **1.83～2.12**，不再集中于约 1.6。新增回归覆盖「AH 高水位可胜过独赢」「同场只取一个玩法」。

### 退半只罚一次，玩法配额取消

用户以一张 -0.25 卡片提出质疑：主胜 46.1% / 平 27.0% / 客胜 27.0%，独赢胜 2.10、让胜(-0.25) 1.83，为什么推独赢而不是让胜？

先澄清一个常见误解：**让球水位低于独赢在浅盘上是必然的，不是数据错误**。让胜(-0.25) 与独赢胜的赢盘条件完全相同（主队赢），但平局时前者只输一半、后者全输；同样的中奖条件配更小的下行，价格必须更低。分水岭恰好是 **-0.5**（此时两者结算完全等价，水位应当接近），浅于 -0.5 必然更低，深于 -0.5 才会更高。用卡片上的去水概率反算，让球两侧抽水分别约 2.2% 与 1.6%，与独赢行同源自洽，落库数据无误。

真正的缺陷在打分函数。原式为 `p_cond × sqrt(净赔率 × at_risk)`：

- 退半的**好处**已经计入 `p_cond`（条件命中率由 46.1% 抬到 53.3%）；
- 退半的**代价**已经计入更低的赔率（0.83 < 1.10）；
- 再乘 `sqrt(at_risk)` 等于对「敢少亏」第三次定价，且方向为偏好高方差，与「风险调整」相反。

结果是让胜(-0.25) 得 0.451、独赢胜得 0.483，打分顺序与 EV 顺序相反（每单位本金期望分别为 −0.0219 与 −0.0322，让球明显更优）。去掉 `at_risk` 后为 0.4853 对 0.4833，与 EV 同向。`risk_adjusted_return_score` 随之删掉 `stake_share` 形参；`at_risk` 仍用于计算 EV，只是不再进入排序。

同时移除 `MAX_PICKS_PER_MARKET = 3`。玩法配额会让第 4 名的高分候选被分数更低的另一玩法顶掉，等于推翻综合分排序，与「兼顾命中率与回报」冲突。跨场次只按综合分竞争，仅保留「同一场一个玩法」。`select_daily_picks_by_match_day` 由两趟合并为一趟。

**改前 / 改后干运行对比**（同一份本机库，195 场输入，未打官方 API）：

| | 改前（罚 at_risk + 配额 3） | 改后 |
|---|---|---|
| 入选场次 | 16（4 天 × 4） | 16（4 天 × 4） |
| 玩法组合 | 12 AH + 4 独赢 | 16 AH |
| 赔率 中位 / 均值 | 1.94 / 1.952 | 1.93 / 1.932 |
| 每单位本金 EV 均值 | +0.0565 | +0.0734 |
| 最差一场 EV | −0.0388 | −0.0255 |

被换掉的正是配额硬塞进来的那 4 场独赢，替换者为 4 场分数更高的让球。赔率区间几乎不变，说明没有回退到「一味低赔保命中」。

**需要留意**：改后组合变成 100% 让球，其中若干为 -1.25 / -1.5 / -2 深盘，这类概率来自尚未通过时间留出验证的 `ah_cover_prob` 模型（已做向市场收缩一半）。原先的配额实际上在充当针对该模型的隐性对冲。正确的应对是继续改进 AH 校准与收缩系数，而不是恢复人为配额。

回归：`test_quarter_ball_refund_is_not_penalised_twice`（strategy 层同向对比）、`test_quarter_ball_refund_survives_adverse_market_feedback`（管线层，-0.25 的当场优势不得被玩法历史翻掉）、`test_daily_four_follows_pure_score_order_across_markets`、`test_same_fixture_cannot_occupy_two_slots_with_both_markets`。

---

## 2026-08-29（周六）

### 手机计算器：倍数去掉加减号

- 投注详情面板在手机底栏（`footerOnly`）隐藏 `n-input-number` 加减按钮，输入框收窄到 84px，避免小屏显示不全。真源 `Predictions/components/BetDetailsPanel.vue`。

### 关注列表按比赛日分组

- 【关注】日期条与列表分组改为读后端持久化的 `match_day`（场地当地日历日），不再用 UTC 开赛日。美职联凌晨场不再被分到「明天」。真源 `favorites.py` / `FavoriteFixtureResponse.match_day`、`useFavoriteFixtures.ts`、`Favorites/index.vue`。

### 方案列表手机可滚动

- 手机「我的方案」表头固定、列表在 `n-scrollbar` 内滚动：`.plans-gap` 接入 flex 高度链（`flex: 1; min-height: 0`）。底部再补 `safe-area-inset-bottom`，避免贴齐系统手势条。真源 `Mine/plans/index.vue`。

### 结算命中 tag：命中红、未命中可辨认且不可筛

- 命中 tag 用 Naive `error`（红底）；未命中一律 `default`，不用组件 `disabled`（那会丢掉点击态和自定义样式）。
- 未命中加 `.fa-tag-missed`：背景 `--fa-border`、文字 `--fa-text-secondary`，三个主题下都能看成 tag；光标默认，不可点筛选。命中仍可点筛同类场次。真源 `resultsDisplay.ts`、`betPlanSettle.ts`、`ResultHitTags.vue`、`Mine/plans`、`base.css`。

### 运维 HelpTip：手机居中、PC 仍在触发点上方

- 手机上 `n-tooltip` 会溢出屏幕。`HelpTip` 在手机改点开居中 `n-modal`，PC 仍用 tooltip 出现在正上方。真源 `components/HelpTip.vue`。

### 赛果回写昨天和今天，07:00 定时同范围

- 已订阅 11:00 /「只更新赛果」不再回写近 4 天。所有赛果路径（07:00 定时、11:00 完整批次、管理员「只更新赛果」）打两次全球 `fixtures?date=`：调度时区昨天 + 今天（周六回写周五晚场，下午完场当天可补）。不拉更早的日期。真源 `fixtures_sync.result_days_for_batch`。
- 新增 `scheduled_results_sync_07`：已订阅与未订阅都跑。与手动按钮共用 `mode=results`，不拉盘口/赛程/详情、不重算日推。

### 赛果卡片：`[荐]` 单独成标签，按日推自己的结算上色

- 用户报「推荐四场都命中，统计只算 2 场」。统计没错：08-28 四场冻结快照为 `1x2/胜`（命中）、`ah/让负(+0.75)`（命中）、`ah/让负(-0.5)`（未命中）、`ah/让负(-0.5)`（未命中），正好 2/4。
- 错在卡片混排两条轨道：`ResultHitTags` 把 `[荐]` 挂在分析器标签上借用分析器命中色。圣菲联合与博卡青年的分析器让球是 `让胜(-0.5)`（命中、红色），日推买的却是对面的 `让负(-0.5)`，于是屏幕上显示红色 `[荐] 让胜`。
- 改为 `[荐]` 独立一枚标签：文案取冻结快照 `auto_pick_lean`（AH 按偏好设置口径显示并保留盘口线），颜色与点击筛选取 `auto_pick_hit`；五枚分析器标签不再带 `[荐]`。后续按界面反馈将整枚 `[荐] + 投注项` 移到赛果卡片头部（时间后、状态前），命中时仍可点筛每日推荐命中；下方只保留五项分析器结算。真源 `components/ResultFixtureCard.vue`、`ResultHitTags.vue`。
- 半球盘的对面是「不败」双选：-0.5 盘的让负 = 客不败、+0.5 盘的让胜 = 主不败。日推按风险调整回报独立选边，因此分析器看主胜（主胜只是最大单一概率）时，只要不败侧的风险调整分更高，日推就会买让负。方向本身不算写反，但这类反向集中出现在赔率约 2.0 的五五开比赛上，成因见下方「赔率项幂次」段。

### 比赛 / 赛果每日推荐标签统一

- 【比赛】普通场次的五项预测标签统一使用 Naive `info`；每日推荐场次只把真正入选的玩法设为 `error` 红底并保留 `[荐]`，其余预测标签全部 `default` 灰色。该规则只由 `autoFavoritePick(fixtureId)` 判定，不按卡片位置或星标猜测。真源 `components/PredictionRecommendationRow.vue`。
- 【赛果】头部日推标签继续按冻结 `auto_pick_hit` 上色：命中为 `error` 红底，未命中为 `default` 作废态；因此赛前主推与赛后命中的视觉语言一致，但赛前红色只表示“主推”，不表示已命中。

### 浅盘与独赢先做当场取舍，再应用历史权重

- -0.5 盘的让胜与独赢主胜、+0.5 盘的让负与独赢客胜是同一结算事件，概率同源 1X2 校准，差别只在水位与玩法历史权重。此前玩法 EMA 能让低水位的独赢挤掉高水位的 AH。
- 浦和红钻 / 神户胜利船复现 ±0.25 同类问题：未加历史权重时让胜(-0.25) 基础分略高，日职联 `1x2` 软权重约 1.15、AH 约 1.01，叠加 AH EMA −0.1747 后反馈层翻成独赢，丢掉平局只输半的兜底。
- `pipeline._choose_best_shallow_market_per_direction` 统一处理 `|line| ≤ 0.5`：同一场、同一胜负方向先在独赢与 AH 中取一笔；±0.5 结算完全相同则按实际水位取高者；±0.25 / 0 按已计入退半/走水的未加权基础分取高者；同分优先 AH。**选完才**把历史 EMA 与联赛×玩法权重乘到胜出的玩法上做跨场排序，学习能力保留，但不得反向淘汰当场更好的本金保护结构。
- 回归 `test_equivalent_half_ball_keeps_the_higher_quote_despite_market_feedback`（AH 更高 / 同赔取 AH / 独赢更高时留独赢）；`test_quarter_ball_refund_survives_adverse_market_feedback` 用浦和红钻当时盘口与日职联权重，要求保留让胜(-0.25)。
- 删除 `auto_favorites` 已无生产引用的旧排序链：`_best_market` / `score_fixture_confidence` / `score_auto_pick_candidates` / `rank_auto_pick_candidates` / `select_auto_picks_by_match_day`（真源是 `recommendation.pipeline`，只有 `_market_candidates` 仍被 `probability_calibration` 复用）。`test_auto_favorites.py` 收敛为候选层断言。

### 命中率「断崖」核查：每天 4 场的正常波动

用冻结快照全量重放（`auto_pick_snapshots` × 终场比分，亚盘口径）：

- 合计 66 场已结算、33 场命中 = 50%。
- 按比赛日：08-24 100%(4/4)、08-25 25%、08-26 50%、08-27 40%、08-28 50%；改动之前也有 08-18 0%(0/3)、08-20 0%(0/4)、08-16 70%(7/10)。单日只有 4 场，25%~50% 落在正常波动内，看不出 08-28 改动引起的系统性下滑。
- 按玩法分桶样本仍太小（浅盘不败侧 4 场、浅盘单选侧 5 场、深盘各 9~10 场），不足以支撑任何调参结论，继续按日累积。

### 赔率项幂次：试过降到 1/3，已回退到 0.5

用户以圣菲联合、博卡青年两张卡片追问「推荐的和预测的打架」。查冻结快照：分析器给 `胜 / 让胜(-0.5)`，日推买 `让负(-0.5)`（= 客不败），赔率 2.01 / 2.05，主胜 51.3% / 52.2% 对客不败 48.7% / 47.8%。两条轨道目标不同（分析器取最大单一概率，日推取风险调整回报），一致性闸按设计不拦反向，所以既不是显示错位也不是闸漏。

但公式本身有一个固定甜点。1X2 概率来自 `inference_mode=market_baseline`，即去水后的市场隐含概率 `p ≈ 1 / 赔率`，代入基础分后正比于 `(赔率-1) ** e / 赔率`，极大值落在 **`赔率 = 1 / (1 - e)`**：

- `e = 0.5` → 甜点恰好 **2.00**，也就是主客五五开、方差最大、分析器最容易与日推反向的那批比赛；
- `e = 1/3` → 甜点移到 **1.50**（1.50 → 0.529、1.60 → 0.522、2.00 → 0.500、1.15 → 0.487）。**这条推导只描述单点分值，忽略了它对候选之间分差的影响，据此调参是错的**，理由见下。

这不是抽样问题，是公式的数学性质；66 场冻结快照的平摊 1 注回放也指向同一处（最常被推的 1.90~2.10 档 20 场、命中 40%、ROI −21.6%；1.70~1.90 档 33 场 −11.8%；<1.70 档仅 8 场但 +40.9%。与分析器反向的 21 场 ROI −14.5%，同向 45 场 −5.8%。低赔档样本太小，只作方向参考）。

**先按 `e = 1/3` 改过一版，随即回退**。用户指出入选上限被压到 1.88 不合理——历史记录里 2.0 档也有大量命中，「保命中率兼顾收益」不该等于封掉 2.0。复核确认这条反馈成立：

- 压幂次不是只把甜点平移，它会**整体抬高净赔率 < 1 的低赔候选**（0.52 的立方根比平方根高约 11%，0.93 只高约 1.2%），于是各候选原始分差被压平；
- 原始分一压平，历史 EMA 与联赛×玩法软权重（实测乘数约 0.9）就更容易反超。08-30 的 fixture 1557384（`ah/让负(-1.25)`，概率 62%、赔率 1.93、**EV +0.197**）在 `e=0.5` 下稳居当日第一，在 `e=1/3` 下原始分优势由 1.31 倍收窄到 1.19 倍，被历史权重压到第五名、掉出四强，让位给 1.52~1.65 档 EV 约 −0.05 的候选。

即 `e=1/3` 的实际效果是「牺牲高概率高水位候选去换低赔候选」，与目标相反。已回退 `PAYOUT_EXPONENT = 0.5`（保留常量与推导注释，不回到硬编码 `math.sqrt`），并在常量注释、`PROJECT_PLAN.md`、`project-scope.mdc` 三处写明：**要偏向命中率就调 `MIN_DAILY_CONFIDENCE` 下限，不要压这个幂次**。

只读网格实测（本地 115 场未开赛、204 个候选，未打官方 API）：

| 概率下限 | 幂次 | 入选赔率带 | 均概率 | 均 EV | 每日场次 |
|---|---|---|---|---|---|
| 0.40 | 1/3 | 1.52~1.88 | 0.570 | −0.0406 | 4/4/4 |
| 0.40 | 0.5（现行） | 1.52~2.09 | 0.548 | −0.0180 | 4/4/4 |
| 0.50 | 0.5 | 1.52~1.93 | 0.554 | −0.0180 | 4/4/4 |
| 0.55 | 0.5 | 1.52~1.93 | 0.587 | −0.0214 | 4/4/**2** |

下限抬到 0.55 会让候选不足（08-31 只凑到 2 场），0.50 三天仍满 4 场，且恰好淘汰 08-28 那两场概率 0.4849 / 0.4761 的不败侧候选——若后续要「每注都过半概率」，走这条参数，不要动幂次。

回归：`test_risk_score_keeps_even_money_reachable` 用一条公平赔率线钉住 2.00 仍是极大值、不被人为封顶；`test_low_exponent_would_flatten_the_score_gap_across_odds` 把「压幂次→原始分差收窄→历史权重反超」这条机制固化为 1.31 倍 / 1.19 倍的断言；`test_risk_score_still_rewards_payout_at_equal_probability` 保证同概率下高水位仍占优。

## 2026-09-01：订阅与密刷调度收敛

- 每日完整批次统一改为 10:55。已订阅批次维持 8 天赛程滑动窗口，每天只补缺失的末端日期；盘口收窄为今天刷新即时盘、明天补缺并冻结初盘，详情只预拉今天和明天。未订阅批次内容不变。
- 「早间盘口刷新」及旧默认/密刷两套表删除，只保留「开启密刷」。打开订阅自动开启密刷，关闭订阅同时关闭并禁用；已订阅仍可手动关闭。
- 密刷从 11:25 起按每小时 25/55 分全天循环，只刷新比赛日今天的未开赛目录盘口。10:55 由完整批次完成后顺序执行同刻密刷，避免官方请求锁让其中一项直接跳过。
- 已订阅但关闭密刷时采用 07:00、08:05、10:55、22:00 稀疏时刻表，但 10:55 仍执行已订阅完整批次，「立即同步」仍可用。
- 运维文案与按钮对齐：行标题为「同步数据 / 更新盘口 / 更新赛果」；上次同步类型、全局提示、确认框不再写「批量更新盘口」「同步官方 API 数据」「只更新赛果」。详情单场仍称「更新盘口」，与运维同名时用「详情 / 运维」区分。
- 「我的」二级页（关于 / 偏好设置 / 我的方案 / 拉盘联赛 / 运维 / 后台管理 / 账号说明 / 订阅记录 / 会员管理）带标题的卡片补上 `:segmented="{ content: true }"`，标题与正文之间有分隔线，与赛果页「当日统计 / 历史统计」一致。Naive 的 `content-segmented` 只对非首个子元素画线，无标题卡片不会多出一条，不必再写 `border-top`。
- 「我的方案」列表 PC / 手机共用同一张卡：标题「方案列表」，日期选择放 `#header-extra`；列表在卡内滚动、标题固定。PC 顶栏第二行只保留「已保存 N 个方案」，不再放日期选择。

### 详情「我的预测」丢失初盘

用户反馈：列表卡片悬停让球数能同时看到初盘与即时盘两张表，点进详情「我的预测」却只剩即时盘。

**根因是补包路径丢档。** 初盘 / 中盘 / 临场由同步批次按采集时刻冻结进 `pre_match_data`，本来就不会重拉。列表走 `_odds_snippet_from_stored` 直接读库，四档都在；详情在需要补包时走 `analyzer._collect_prematch_package`，它从 `_empty_prematch_package()` 空壳起手，只把本趟拉到的即时盘、交锋、近况等填回去，冻结的三档从头到尾没人带进来，于是 `analysis.package.odds_opening` 恒为 `{"available": false}`。以林肯城 vs 布莱克本（`fixture_id=1563120`）实测：库里初盘 08-31 03:00Z、即时盘 09-01 01:37Z 都在，接口返回的 `odds_opening` 却不可用，顺带 `odds_opening_snippet` 也是 `null`。

影响不止那张表：`market_analysis` 的主盘升降与同档共振、`persist_ah_fields` 的 `ah_opening_*` 升降水特征都读 `package["odds_opening"]`，补包一跑就等于把走势对比整块抹掉。

修复在 `_collect_prematch_package` 结束、所有拉取完成之后，从本地行把 `odds_opening / odds_mid / odds_late` 中可用的那几档覆盖回展示包——放在拉取之后是为了接住本趟刚由 fetcher 冻结的初盘。`_save_pre_match_data` 不写这三列，带回去不会反向污染库。回归 `test_odds_snapshot.CollectPackageFrozenBoardTests`。

详情「我的预测」里「算法原始预测」（及已开赛的「赛前结果预测」）和「盘口解释」两张卡，标题右侧 `#header-extra` 显示本场对局名。

### 单场更新盘口不重算日推（产品规则，不是缺口）

日推是同一批盘口之间的跨场排序。详情「更新盘口」只刷新一场：本场走 `_upsert_odds_and_recompute` 重算算法推荐，「我的预测」会更新；**不得**调 `sync_daily_auto_favorites`。拿一场新水位去跟其余场的旧盘口比没有可比性，整池重排也多半还是同一批 `[荐]`，却会让人以为已经按最新盘口重选过。已带 `[荐]` 的场次三件套等到下一次批量才对齐。

重算日推只发生在批量盘口之后：运维「更新盘口」、密刷、10:55 完整批次（`fixtures_sync` 在 `mode != results` 时调用）。真源 `POST /fixtures/{id}/odds/refresh` 与 `scheduled_fixtures_sync`；回归 `test_fixture_odds_refresh`、`test_prematch_odds_batch_only_refreshes_explicit_fixture_ids`。

### 让球日推不再买对面的低概率高水侧

用户以布拉加 1:0 吉马良斯（葡超，主让 -0.5）追问：分析器给 `胜 / 让胜(-0.5)`，`[荐]` 却是 `让负(-0.5)`。这次不是 08-29 记录的那种「概率不足五成才反向」——盘口解释页显示主胜 51.9% / 平 27.2% / 客胜 20.9%，让胜概率已经过半，日推仍买了 48.1% 的对面。

**根因是基础分在两选一市场上对概率免疫。** 把 `p × 净赔率 ** e` 平方并代入两侧结算关系可得 `score² = p × (1 - p + EV)`，其中 `p(1-p)` 关于 0.5 对称。按面板数字实算：

- 让胜 `p(1-p) = 0.249639`，让负 `p(1-p) = 0.249639`，**概率项分差 0.000000**；
- 抽水项分差 0.006912（让球价值 让胜 -3.0% / 让负 -1.8%）；
- 原始分 0.48381 对 0.49090，市场收缩后 0.48381 对 0.48933，让负两次都赢。

也就是说 08-29 记录里「不要压幂次、命中率偏好只调 `MIN_DAILY_CONFIDENCE`」这条结论只对了一半：幂次确实不该动（压它会同时平移甜点并压平分差），但 `MIN_DAILY_CONFIDENCE = 0.40` 也拦不住这种情况，因为 48.1% 远高于门槛。用户原始要求是「在保证命中率的前提下兼顾回报率」，即**先设命中率下限、再在下限之上比回报**；此前实现成两者在同一式子里加权，而该式子恰好在让球盘上把概率项抵消了。

**修法：同一块让球盘只保留条件命中率更高的一侧。** 可行性依据是两侧条件概率之和恒为 1（退半与走水已计入条件概率），按仓库结算规则实测各档：

```
line     让胜      让负      sum
-0.50    0.5190   0.4810    1.000000
-0.25    0.6007   0.3993    1.000000
+0.00    0.7129   0.2871    1.000000
+0.25    0.7581   0.2419    1.000000
+0.50    0.7910   0.2090    1.000000
```

所以「取命中率更高的一侧」等价于「只买过半的那一侧」，且每块盘仍贡献一个候选，候选池不缩小。闸设在 `pipeline._to_ah_picks` 计算基础分之前，比较对象是 Platt 校准后、市场收缩前的条件命中率（校准是单调变换，不改变两侧排序；市场收缩是价值折价，不是命中率估计，放在闸后）。命中率同档（差值 < `AH_SIDE_TIE_EPSILON`）时两侧都留下，交回基础分按水位决胜。

未动 `PAYOUT_EXPONENT` 与 `MIN_DAILY_CONFIDENCE`，也未动独赢候选生成，因此不存在「抬全局门槛把独赢候选清空、池子偏向让球」的副作用。

- 真源 `backend/app/services/recommendation/pipeline.py::_to_ah_picks`；新增常量 `AH_SIDE_TIE_EPSILON`。
- 回归 `test_shallow_board_never_buys_the_lower_probability_side`（布拉加当时盘口，要求选中 `ah / 让胜(-0.5)`）。既有 `test_ah_candidate_can_beat_the_lower_payout_1x2_candidate`（-0.75 深盘 cover 0.58）、`test_equivalent_half_ball_keeps_the_higher_quote_despite_market_feedback`（单选侧概率本就要求过半）、`test_quarter_ball_refund_survives_adverse_market_feedback`（-0.25 让胜 0.5165 > 让负 0.4835）方向均与新闸一致。
- `strategy.py` 的常量注释与 `risk_adjusted_return_score` 文档串补上「该分数无法区分两选一盘的两侧，方向闸在上游」，避免下次又把幂次当命中率旋钮。
- 同步 `PROJECT_PLAN.md` 第 1 节、`project-scope.mdc`，以及「我的 → 关于」里 `[荐]` 那段说明：不同向只在分析器看好的结果本身不到五成时才会出现。

### 分析器让球方向改为主盘去水概率

用户以西汉姆联 vs 狼队为例确认最终语义：1X2 为 `胜/平`，主让 -0.5 的报价为让胜 2.06 / 让负 1.84，旧分析器却因 1X2 与参考比分输出 `让胜(-0.5)`，与日推 `让负(-0.5)` 割裂。

分析器让球方向现收敛为这块盘口自己的信息：`_structural_pick` 读取真实主盘两侧赔率，按倒数归一去水后比较隐含概率，推荐概率更高的一侧；同价时显示 `让胜/负` 持平。西汉姆盘口去水后约为让胜 47.2% / 让负 52.8%，回归要求输出 `让负(-0.5)`。

- 删除 `_agree_with_recommendation` 整条 1X2 强制翻边路径，以及因此无引用的 `_loses_outright_on_recommendation`、比分覆盖、平手盘 1X2 兜底和相关辅助函数。
- 1X2 推荐与参考比分继续作为各自独立预测展示，但不再影响让球方向；主盘相同的情况下，换成相反的 1X2 / 比分输入仍必须得到相同让球结论。
- 浅盘和深盘统一按主盘去水概率决定分析器展示方向。原 AH ML / 多因子概率通过 `_model_prediction` 继续写入 `MatchFeature.ah_cover_prob/ah_model_source`，供深盘日推、训练与审计使用，不再决定分析器展示方向。
- 盘口解释文案由「算法让球倾向」改为「让球主盘倾向」，并在倾向说明中展示两侧去水概率，避免把市场隐含概率误称为模型预测。
- 回归：`test_structural_pick_uses_devig_main_handicap_prices`、`test_west_ham_regression_market_prices_choose_handicap_loss`、`test_handicap_direction_is_independent_of_1x2_and_score`、平手盘主盘与同价测试。

日推列表读时过滤陈旧独立让平行：原先只查 `auto_lean`，字段拆开后恒为假。已改为 `is_stale_ah_push_row`，独立「让平」出现在 `auto_lean`（拆字段前）或 `auto_handicap_lean`（拆字段后）任一列即隐藏。回归 `test_stale_ah_push_is_hidden_from_either_lean_column`：旧列 `让平(0)`、新列伴随 `胜` + `让平(0)` 均滤掉；`让胜(-0.5)`、`让胜(0)`、独赢、`让胜/平` 双选均保留。

### 详情饼图卡不再自称「算法原始预测」

承接上一条：让球方向改读盘口后，用户提出既然「每日推荐」和「盘口解释」都基于市场，这张卡叫「算法原始预测」就名不副实，建议改成「胜平负市场定价」。

核对下来，用户的判断比提法更准，但标题不能整张换成「胜平负市场定价」：

- **卡里不止胜平负。** 同一个 `<ul>` 还渲染 `goal_lean`、`both_score_lean`、`score_hint`，它们来自进球分布模型，不是市场定价；让球那栏才刚改成市场定价。整张卡贴「市场定价」会把这三项标错。
- **但那三个百分比确实恒为市场定价。** `published_match_probabilities` 在存在 1X2 主盘时**无条件**返回去水盘口概率，ML softmax 从不进展示值（`test_published_probabilities` 里 ML 给 0.999 仍等于去水盘口概率），模型只经 `working_match_probabilities` 喂 `get_recommendation`。没有主盘时 `ready=False`，卡片走空态不显示百分比。因此这条标注是静态恒真的，与 `inference_mode` 是否升到 `ml` 无关，**不需要**把 `model_source` 暴露进 `/analysis`。

改法：

- 卡片标题统一为「赛前结果预测」，与已开赛态同名；原先未开赛 / 已开赛两个 `n-card` 除 body 外完全重复，合并为一张卡加 `v-if` 切换 body。
- 胜平负三行上方固定一行 `.prob-source`：「胜平负概率为主盘赔率去水后的市场定价」，与让球的 `.handicap-note` 形成对称的出处说明，两栏都标明来源后割裂感消除。
- 同步 `frontend/FRONTEND_UI_SPEC.md` 第 4.3 节「我的预测」行。

### AH v3 补齐盘口基线门禁与模型介入

复核 `ah_v1` / `ah_v2` 后确认：两版都采用同一套二元 Logistic，v2 只增加副盘与初盘→即时盘特征；训练达到 80 条就切 `source=ml`，从未证明模型优于 `ah_implied_cover`。v3 虽增加中盘/临场特征，但沿用了这个门禁缺口。

本次补齐：

- AH 最近 20% 且至少 20 条按时间留作验证，禁止随机打乱；同时计算模型与主盘去水基线的 log-loss、Brier、accuracy。
- 只有模型 log-loss 与 Brier **同时严格优于**盘口基线才写 `deployable=true`；旧 v3 artifact 没有该字段时自动视为不合格，不再仅凭样本量启用。
- 合格模型不全盘替代盘口，统一使用 `盘口概率 + 0.5 × (模型概率 − 盘口概率)`。详情让球方向与落库 `ah_cover_prob` 使用同一修正值；未过门禁直接回退主盘去水概率。
- 深盘日推读取已修正并冻结的 `ah_cover_prob`，删除日推层原来的第二次单边收缩，避免同一模型偏差被重复打折。
- 删除已失去运行路径的 `multifactor_cover_prob` / `_clip_logit_term`，以及删除二次收缩后无引用的 `_two_way_fair_probs`。
- 全量重训改为新建 Logistic 实例，避免先在训练集拟合、再在同一实例上全量拟合导致早期样本被重复训练。
- `model-status` 与自动训练状态区分 `artifact_ready` 和 `deployable`；训练成功但未胜盘口时明确报告 `market_implied`。

### 胜平负双选过多：症结在「弱热门」的绝对阈值

用户反馈「现在的预测基本上都是双选，算法感觉越改越回去了」。先分方向核对：分析器 `handicap_lean` 的双选率在改读主盘去水概率后从 08-19 的 30%、08-24 的 20% 降到 08-26 之后的 0%，当前 35 场未开赛只剩 1 场 `让胜/负`（两侧同价）。**双选来自胜平负，不是让球**，而 `get_recommendation` 这轮唯一的改动是删掉 `features` 形参，核对删除前的版本确认它在函数体里从未被读过，判定逻辑一行没变。

把 35 场逐场跑分支：26 场触发 `weak_home/weak_away`，其中 17 场的 `m_gap` 在 0.12~0.24，远超 `_SINGLE_PICK_GAP = 0.1`，是有明确热门的盘。症结是 `model[fav] < 0.50` 为绝对阈值——足球热门本就大多落在 40%~50%，该条件近乎恒真；而 `contested` 排在「明确热门」分支之前，一触发就把单选路径整条短路。另有一层：35 场的 `d_spread` 与 `m_spread` 完全相等（1X2 模型未过门禁、处于 `market_baseline`，喂进去的模型概率就是去水盘口本身），所以 `weak_*` 名义上的「模型认为热门不强」已退化成「盘口热门不到五成」，不再是独立于市场的信号，却仍在否决市场自己给出的热门。

**回测后才改，不凭手感。** 新增 `backend/scripts/backtest_lean_thresholds.py`（只读本地库，不打官方 API），在 658 场已结算且有冻结盘口的样本上取「`m_gap >= 0.1` 却被 `weak_*` 单独拖进双选」的 178 场：

| 方案 | 命中率 | 市场覆盖度 | 超出覆盖度 |
| --- | --- | --- | --- |
| 维持双选 | 74.2% | 72.7% | +1.5% |
| 改为单选 | 47.8% | 45.2% | +2.5% |

关键是覆盖度那一列：双选覆盖两个结果，74.2% 里有 72.7% 是机械得来的，判断本身只贡献 +1.5pp，单选反而贡献 +2.5pp。两者差 1pp，在 178 场上标准误约 3.7pp，属噪声。**即单选并不更差，双选那个高命中率是虚的**，收紧没有可测量的准确率代价，纯属展示信息量的取舍。

改法：`weak_home/weak_away` 加 `m_gap < _SINGLE_PICK_GAP` 前置，只在市场自己也没选出热门时才成立。当前 35 场未开赛的双选占比从 74% 降到 26%（库里存量仍是旧值，下次批量或运维「更新盘口」重算后才刷新）。回归 `test_clear_favorite_below_half_still_gets_a_single_pick`（去水主 46.9%／平 27.1%／客 26.0%，领先 19.8 个点须出「胜」）、`test_weak_favorite_still_doubles_when_market_has_no_favorite`（领先仅 5.1 个点仍出「胜/平」）。

### 让球候选维持只取主盘：扩档位经回测不成立

顺着上一条追问「1X2 双选但实际按亚盘单选下注时，让球推荐是不是最优」。先确认范围：`_structural_pick` 与 `pipeline._to_ah_picks` 都只调 `extract_main_ah_line`，**两条路径都只看主盘一条线**。且 `_structural_pick` 按倒数归一去水，序关系与 `1/赔率` 一致，所以它恒等于「买主盘水位低的那一侧」，永远不会与盘口热门相左。

主盘确实会把 1X2 结论劈开：`胜/平` 在 -0.5 盘上分裂成「胜→让胜、平→让负」，平局落在让负那侧，所以主盘让负并不是「胜/平」的亚盘表达。当前 26 场双选里，每一场都存在命中率明显更高的浅盘档位（如 1563125 主盘让负 -0.5 命中 50.2%，而让胜 +0.5 命中 75.9%）。

但补上回报后这条路走不通。同口径 395 场（每块盘先按命中率留一侧，再选一注）：

| 策略 | 命中率 | ROI | t |
| --- | --- | --- | --- |
| 只看主盘（现状） | 50.9% | −0.24% | −0.06 |
| 全浅盘 · 按分数 | 51.4% | +0.62% | 0.15 |
| 全浅盘 · 按命中率 | 61.5% | −5.08% | −1.52 |
| 全浅盘 · 分数近则贴 1X2 | 56.2% | +5.67% | 1.50 |

- 单纯扩档位无差别（t=0.15）：风险调整分在各档位间本就分不出高下，主盘照样胜出，候选池变大而选择不变。
- 按命中率选档明确变差，高命中率被低水位吃光——「不追低赔」这条产品规则的实证。
- 「贴 1X2」看似最好，但并列窗口取 0.005/0.01/0.02/0.03/0.05/0.10 时 ROI 为 +1.20%/−0.74%/+5.67%/+1.39%/−1.75%/−2.44%，尖峰加正负横跳而非平滑，是参数过拟合。时间切分虽显示后段 ROI +20.82%（t=3.38），但**四种策略在后段全部转正**（现状也从 −2.90% 到 +5.95%），是行情差异不是策略能力，证明不了任何东西。

结论：维持只取主盘，并把上述数据写进 `project-scope.mdc`，避免下次再重新论证一遍。

### 运维「更新盘口」说明只回答「更新完会怎样」

用户反馈原文案通篇在讲比赛日、热门、明天排除、后端二次校验这些产品边界，唯独看不出「点完会不会重算推荐和日推」，而这正是管理员点之前唯一想确认的事。

文案压到两句：先说更新范围（比赛日今天、仍未开赛的热门场次，每场约一次官方请求），再说成功后的结果（按新盘口重算这些场次算法预测并重排当天日推 `[荐]`，不重训模型）。行标题下的常驻摘要补「并重算推荐」，不展开 `?` 也能看到答案。边界细则不再进文案，仍以 `PROJECT_PLAN.md` 与 `project-scope.mdc` 为准。

「不重训模型」是实际行为：`scheduled_fixtures_sync` 只在 `mode == "full"` 调三个 `maybe_auto_train_model`，`prematch_odds` 不训练；重算算法预测来自每场 `_upsert_odds_and_recompute`，重排日推来自 `mode != "results"` 时的 `sync_daily_auto_favorites`。

`HelpTip` 的 tooltip 与手机模态给文本加 `.help-text { white-space: pre-line }`，说明文案可用换行分条；tooltip 内容改为 `<span class="help-text">` 承载，避免模板空白被 `pre-line` 渲染成首行空白。

### 「今天只推荐了三场」：默认联赛筛选把一场 `[荐]` 整场藏掉

用户看到当天只有 3 场带 `[荐]`。查库当天实际写了 4 场：英冠 3 场（1563123 让负 -0.75、1563124 让胜 +0.25、1563119 让胜 0），外加德国杯 1550704 让胜 +8。少的那场是德国杯，`leagues.is_hot = 0`。

根因是两套范围不一致：日推候选池按 `is_catalog` 取（所有目录联赛都拉盘口），而 `/leagues/filter-options` 的 `default_checked` 只认 `is_hot`，前端 `resolveTrackedSelection` 完全按这个字段决定默认勾选。于是任何落在未勾热门目录联赛上的 `[荐]`，在默认筛选下连比赛本身都不显示，用户看到的推荐数少于实际选出数。

改法：`default_checked = 热门 ∪ 当日有可见 [荐] 的联赛`，新增 `_auto_pick_league_ids` 按筛选窗口的 `match_day` 查 `source=auto` 并复用 `is_stale_ah_push_row` 排掉隐藏的残留「让平」。`tier` 分组不动——德国杯仍归「其他」，只是默认勾上，不改动热门配置本身。回归 `backend/tests/test_league_filter_options.py`：非热门带 `[荐]` 默认勾上且 `tier` 仍为 `extra`、同为非热门但无 `[荐]` 的联赛不受影响、手动关注与残留「让平」都不撑开勾选。

### 六场阈值管的是「允许少推」，不是「可以多推」

顺着上面查库发现 09-03 写了 5 场，超出每日 4 场。`select_daily_picks_by_match_day` 原按

```python
day_limit = limit_per_day if day_total >= MIN_MATCHES_FOR_FULL_QUOTA else len(day_picks)
```

处理：当日进管线场次不足 6 时，上限被换成「全部候选」。09-03 只有 5 场有盘口，5 个候选全部入池。

这中间绕了一圈：先按「恒定最多 4 场」改掉，又因把用户「池子不低于 6 场就要有 4 场推荐」的原话误读成「不足 6 场则全选」而改回原样，最后由用户点明——**六场阈值是「几时允许少于 4 场」的豁免条件，不是「几时可以多于 4 场」的放宽条件**。两者不是一回事，原代码把前者实现成了后者。

定稿口径：

- 每个比赛日恒定最多 4 场，`day_limit = min(limit_per_day, len(day_picks))`。
- 池子 ≥ 6 场：应当给满 4 场，选不满写告警，供排查是被质量门还是自洽闸卡住。
- 池子 < 6 场：给不满 4 场属正常，不告警。

顺带修了告警本身：`log_sync_summary` 原先拿聚合 `total_matches` 与 `selected_count` 比较，而配额是按比赛日算的——多日窗口里 selected 通常是 `4×有赛日`，恒大于 4，告警实际永不触发。改为接收 `matches_by_day` / `selected_by_day` 逐日判断，`run_pipeline` 相应多返回一个 `matches_by_day`，`_count_matches_by_day` 因此仍有唯一用途（不再作为选场入参）。

回归 `test_quiet_day_may_pick_fewer_but_never_more_than_four`（5 个合格候选只出 4 场，2 个候选仍给 2 场）、`test_short_pool_is_the_only_excuse_for_fewer_than_four`（池子 6 场选 3 场触发告警，池子 5 场选 4 场不告警）。联赛默认勾选修复不受影响。

### 待查：深盘候选的置信度天然贴近 50%

同一轮排查发现德国杯那场的可疑之处：主盘 +8、两侧同价 1.92，去水后正好 50.0%/50.0%，是纯抛硬币加约 4% 抽水，却拿到 4.5 星；配套三件套还写成「主胜 / 比分 2-1」，而市场给主队 1.9%。自洽闸放过它，是因为 +8 让球在主胜时必然覆盖，「不会输」这个条件被巨型让球盘轻易满足。

更普遍的隐患：`_ah_side_probability` 对 `|让球线| > 0.5` 的深盘直接取 `result.ah_cover_prob`，而 AH 模型尚未过门禁（`inference_mode=market_baseline`），该值就是主盘去水概率——**而让球盘的定义就是让两侧接近 50%**。所以深盘候选的置信度天然都在 0.5 附近，`MIN_DAILY_CONFIDENCE = 0.40` 对它形同虚设，排序几乎只由抽水差决定。09-01 至 09-03 的 13 条推荐里有 4 条是深盘。尚未定改法（排除近似 50/50 的盘、深盘整体暂停入池、还是收紧三件套自洽闸），先记录。
