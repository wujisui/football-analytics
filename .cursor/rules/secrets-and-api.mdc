---
description: 密钥与 API-Sports 鉴权约定
globs: backend/**/*
alwaysApply: false
---

# 密钥与官方 API

## 密钥存放

- 真实 Key 只写在 `backend/secrets.local.env`（已 gitignore）
- 模板用 `secrets.local.env.example` / `.env.example`，禁止写入真实 Key
- 非密钥配置可放 `backend/.env`；启动时先 `.env` 再 `secrets.local.env`（后者覆盖）
- 不要把 Key 贴进聊天、README、前端代码或 commit

## 鉴权优先级

1. 有 `API_SPORTS_KEY` → 官方直连，请求头 `x-apisports-key`
2. 否则才用 `RAPIDAPI_KEY` + `X-RapidAPI-Key` / `X-RapidAPI-Host`
3. Host 默认 `v3.football.api-sports.io`

## 管理接口

- `/api/v1/admin/*` 使用 `ADMIN_API_KEY`，请求头 `X-Admin-Key`
- 普通业务接口不需要 Admin Key
