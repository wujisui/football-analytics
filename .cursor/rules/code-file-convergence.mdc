---
description: 代码收敛与文件收敛——迭代改需求时先收拢再加新
alwaysApply: true
---

# 代码收敛与文件收敛

需求常在「上一次不满意」上叠加。默认策略是**先收拢、再扩展**，禁止在半废弃路径上继续堆代码/堆文件。

## 改需求时

1. **先问**：现有函数/组件/接口能否改语义覆盖？能则改，不新开平行实现。
2. **再合**：新旧两套逻辑并存超过一次迭代 → 合并或删旧，禁止长期双轨。
3. **后加**：仅当职责清晰且无法并入现有单元时，才新增文件。

## 文件收敛

- 不为「这次方便」再拆一个几乎只被一处引用的薄封装（除非复用已明确出现第二次）。
- 新增 `.vue` / composable / endpoint 前：在同目录找可扩展的宿主；优先扩展 `Home.vue` 已有组件、`useHomeFixtures`、`leagues.py` 等。
- 删除或停止调用废弃导出后，**顺手删无引用文件**，不要留空壳。
- 配置也收敛：生效目录与参考目录职责写清；不要再复制第三份联赛清单。

## 代码收敛

- 同一数据不要多条加载链（例如首页不要同时 `catalog` + `leagues` + `filter-options` 拉同类信息）。
- 前端过滤 vs 官方同步边界固定后，不要再在勾选路径偷偷加 sync，除非产品明确要求。
- 改完做一次引用排查：`Grep` 旧函数名 / 旧路由；无引用则删。

## 交付自检

- [ ] 本次是否少文件或少平行 API，而不是只加不减？
- [ ] 是否留下 `@deprecated` / 注释掉的大块旧实现？有则删或当迭代内清掉。
- [ ] 文档（README / PROJECT_PLAN）是否与唯一真源一致？
