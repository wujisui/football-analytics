---
description: 未指定联赛时，只翻译数据库目录联赛的球队，禁止全量翻译冷门联赛
alwaysApply: true
---

# 球队译名范围：默认只做热门联赛

球队译名唯一真源是 `backend/app/services/team_names.py`（`BY_ID` 手工精校 + `data/team_names_auto.json` 机翻兜底）。

## 默认范围

用户**没有指定**要翻哪些联赛时，翻译范围 = 数据库中 `leagues.is_catalog=true` 的联赛；`config/leagues.json` 仅是首次建库种子。

- 判定“还缺哪些”只用这个口径：`team_translate.list_untranslated_catalog_teams` / `team_needs_zh`。
- **禁止**为了“全量/清零”去翻冷门联赛（U17/U20/预备队/各国乙丙级/杯赛外围等）的球队，白费时间和 token。
- **禁止**用单个自然日/单个时区日历日当核对窗口冒充“全量”；核对就按 catalog 联赛统计。

## 怎么补

1. 热门联赛的常见球队 → 手工写进 `BY_ID`（机翻常把短名译错，如 Hearts→心）。
2. 其余 catalog 球队 → `python manage.py translate-catalog-teams`（机翻 + 质量闸，不耗官方 API 配额）。
3. 写完 `backfill-team-names` 回写本地 `teams.name`。

## 手写 BY_ID 必须核对 ID

`BY_ID` 按官方 team id 写，**不能照着联赛名单顺序猜 ID**：曾经整段德/法/意/西被写偏一位，Holstein Kiel 显示成“海登海姆”、Köln 显示成“圣保利”，直接导致按错队名下注。

- 改完 `BY_ID` 必须跑 `python manage.py audit-team-names`：它用 `api_snapshots` 里的官方英文名反查，`BY_ID`（按 ID）与 `BY_NAME`（按英文名）互相独立，两者给出不同中文名即 ID 行写错。要求 conflicts = 0。
- 有意保留的简称（拜仁 / 多特 / 门兴 / 莱比锡 / 巴黎）是全名前缀，审计会自动放过；出现非前缀差异就是真错。
- 发现某个 ID 其实属于别的球队（如 761 是 Sporting CP B、750 是 Naftan）：删掉该行，并把本地 `teams.name` 复位成官方英文名，否则库里旧中文名会一直留着。
- 关键 ID 补一条 `backend/tests/test_team_names.py` 回归断言。

## 例外

仅当用户**明确点名**某个冷门联赛/球队时才翻它；不要主动扩大范围。
