---
description: 本地优先落库与赛前刷新策略
globs: backend/**/*
alwaysApply: false
---

# 本地优先与配额

## 读取顺序

列表类接口只读 Redis / SQLite（`api_snapshots`、`pre_match_data` 等）。  
详情 `/fixtures/{id}/analysis`：本地优先；展示包缺失或不完整时，后端可按需打官方并落库（`analyze_fixture`）。

## 刷新策略（赛前分析）

- **定时批次**：免费模式 07:00 回写昨天和今天赛果，08:05 用 1 次请求只拉当天赛程，11:00 先拉昨天/今天赛果再拉今天赛程/盘口，22:00 只刷今天热门盘口；不拉未来。已订阅另加盘口轻刷方案，赛果仍只回写昨天和今天
- **已开赛或已结束：预测快照审计字段冻结**；展示包仍可在用户点开详情时补缺落库
- **详情 `/fixtures/{id}/analysis`**：本地有完整包则直出；否则按需补拉并写入 `pre_match_data`
- **不做**定时窗口全量详情预拉（默认关；管理员可在「我的」页覆盖并持久化到 `app_settings`）
- 分析结果复用 `pre_match_data`；TTL 逻辑在 `app/services/ttl_policy.py`
- 不要改回「固定 1 小时过期」或「开赛后仍轮询比分」
- 赛果由固定批次回写 `fixtures.home_goals/away_goals`，不做滚球

## 赛前数据包（未完成能力）

优先补齐并落库：`/odds`、lineups（首发+替补）、injuries；再暴露 API / 前端。  
H2H、近况、球队统计已有拉取的，应尽量展示并逐步入模，避免只拉不用。
