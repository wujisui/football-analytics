---
description: 前端 Naive UI 组件优先与全屏布局约定
globs: frontend/**/*
alwaysApply: false
---

# 前端 UI 组件约定（Naive UI）

本项目已依赖 Naive UI。**能用组件库解决的布局与交互，禁止手写一套平行实现**，否则既浪费包体积，又容易出现侧栏随页滚动、样式不一致等问题。

页面交互规格见 `frontend/FRONTEND_UI_SPEC.md`；API 约定见 `frontend-api.mdc`。

## 必须优先使用的组件

| 场景 | 使用 | 不要做 |
|------|------|--------|
| 应用壳 / 全屏分区 | `n-layout`、`n-layout-header`、`n-layout-sider`、`n-layout-content` | 用 `div` + `flex` 再造整页骨架 |
| 首页左右栏 | `has-sider` + `n-layout-sider`（固定）+ 右侧 `n-layout-content` 滚动 | `position: sticky` 伪固定侧栏 |
| 侧栏导航 / 联赛筛选 | `n-menu`（可配 `NBadge`） | 手写一排 `<button>` 菜单 |
| 页面标题区 | `n-page-header` | 自绘 h1 + 返回链接当主标题栏 |
| 层级导航 | `n-breadcrumb` / `n-breadcrumb-item` | 纯文本「返回」代替路径 |
| 加载 / 空 / 错 | `n-spin`、`n-empty`、`n-alert` + `n-button` 重试 | 自定义转圈或裸文字 |
| 标签 / 按钮 / 卡片 / Tabs | `n-tag`、`n-button`、`n-card`、`n-tabs` | 重复造轮子 |
| 悬浮说明 | 仅当产品**明确要求**悬浮提示时用 **`n-tooltip`** | 原生 HTML `title` / `:title` 冒系统气泡；未要求时不要擅自加悬浮文案 |
| 区域滚动 | `n-scrollbar`，或 Layout 设 `:native-scrollbar="false"` | 手写 `overflow` + 自定义 `::-webkit-scrollbar` |
| 表单输入 | `n-input`、`n-form`（需要时） | 原生 textarea 无统一样式 |
| 数据展示 | `n-descriptions`、`n-progress`、`n-list`、**`n-data-table` / `n-table`** | **禁止**原生 `<table>` / 手写表格网格；对阵列表、赛果表一律 Naive 表格 |
| 分区栅格 | `n-grid` / `n-gi`、`n-flex`、`n-space` | 业务页再造一套 `display:grid/flex` 分栏（壳层 Layout 除外） |
| 全局配置 | 根节点 `n-config-provider`（中文 locale + `theme`）+ `n-message-provider` + **`n-modal-provider`**（命令式弹窗） | 各页各自套主题；在组件外调用 `useModal` |
| 业务模态框 | **`useModal().create(...)`**（见下节） | 页面里堆 `v-model:show` 的 `<n-modal>` |
| 明暗主题 | `darkTheme` + 顶栏切换，偏好写入 `localStorage` | 手写整站 CSS 变量换肤 |

按需自动引入已配置 `unplugin-vue-components` + `NaiveUiResolver`；根布局等需显式 import 的除外。

## 模态框（命令式优先）

业务弹窗统一用 **`useModal().create`**（须在 `App.vue` 的 `n-modal-provider` 内调用），参考【我的方案】`views/Plans/index.vue`。

```ts
const modal = useModal()

modal.create({
  preset: 'card', // 或 'dialog'
  title: '标题',
  autoFocus: false, // 必填，见下第 2 条
  style: { width: 'min(420px, calc(100vw - 32px))' },
  content: () => h(SomeContent, { id }),
  // dialog：positiveText / negativeText / onPositiveClick（返回 false 阻止关闭）
})
```

1. **新建**业务确认框、详情浮层、表单弹窗 → `useModal.create`，不要再加 `v-model:show` + `<n-modal>`。
2. **每个弹窗必须 `autoFocus: false`（声明式写 `:auto-focus="false"`）**：`display-directive` 默认 `if`，弹窗内容比 vueuc `FocusTrap` 激活晚一帧，`FocusTrap` 会退回聚焦自己那个 `aria-hidden="true"` 的 0 尺寸哨兵 div，Chrome 拦下并刷 "Blocked aria-hidden on an element because its descendant retained focus"。关掉初始自动聚焦即可，焦点陷阱与 Esc 关闭仍生效；**不要**改成 `trap-focus=false`（会丢焦点陷阱）。
3. **改名等带输入的 dialog**：用 `defaultValue` + `onUpdate:value` 收集草稿，勿依赖每次按键都重渲染 `content`。
4. **例外**：应用壳级、需跨路由长期挂着的会话态浮层（如 `LoginModal` 绑 `useAuthSession`）可暂时保留声明式；新业务弹窗仍走命令式。
5. 需要时 `const m = modal.create(...)` 后调 `m.destroy()`；同一业务同时只开一个时先 destroy 再 create。

## 路由与组件目录

目录按“父页面 → 专属子组件”组织，避免把所有业务组件平铺到全局 `components/`：

```text
src/views/Results/
├── index.vue
└── components/        # 仅 Results 页面及其后代使用
    ├── AccuracyStatistic.vue
    └── ResultsFilterPanel.vue
```

1. 路由页面使用 `src/views/<RouteName>/index.vue`。
2. 只属于一个路由功能树的组件放在该路由的 `components/`；即使不是页面直接引用，只要祖先唯一也归属该路由。
3. 被两个及以上互不从属的页面/功能树引用，才放 `src/components/`。公共目录可按稳定领域分组，禁止再次变成无层级的大杂烩。
4. 只属于布局的组件放 `src/layouts/components/`；应用级抽屉/浮层（如收藏）也按路由功能待遇放 `src/views/<Feature>/`，不要再堆 `components/app/`。
5. composable 遵循同一归属规则：单一功能树使用的放 `views/<Feature>/composables/`；布局树专属的放 `layouts/composables/`；只有跨功能复用的才留在全局 `src/composables/`。
6. 迁移组件或 composable 后必须扫描旧 import / 旧路径；空目录、薄包装页面、无引用文件当次删除。
7. 不为“一处引用”新增 re-export `index.ts` 或薄封装；直接导入实际文件，避免隐藏依赖。

## 全屏布局规则

1. **应用占满视口**：根 `n-layout` 使用 `position="absolute"` + `inset: 0`（或等价 100vh），不要用 `max-width` 把整站收成居中窄栏。
2. **滚动落在内容区**：侧栏 / 顶栏不随整页 `body` 滚动；列表、详情正文在对应 `n-layout-content` 内滚动（`native-scrollbar` 或组件滚动条）。
3. **首页**：`n-layout has-sider` → 左侧联赛 `n-layout-sider` + `n-menu`；右侧仅比赛列表区域滚动。
4. **详情**：顶栏面包屑 + `n-page-header`；下方 Tabs 卡片 `flex: 1` 铺满剩余视口；正文在 content 内滚动。
5. 内容区可设合理 `max-width` 提升阅读（如详情正文），但**外壳仍全屏**（白卡片区域应纵向撑满，勿只占上半屏）。

### 滚动条（必读）

Naive Layout **默认 `nativeScrollbar: true`**（浏览器原生条）。主内容区优先 Naive 滚动条；原生 `overflow` 区域用全局细条样式，禁止各组件各自写 `::-webkit-scrollbar`。

1. 外层 `n-layout` 用 `content-style="display:flex;flex-direction:column;height:100%;"` 把高度锁死（否则子内容撑开，外层仍会冒原生条）
2. 顶栏 `flex-shrink: 0`；可滚区域用 `n-layout-content`：`style="flex:1;min-height:0"` + **`:native-scrollbar="false"`**（可加 `:scrollbar-props="{ trigger: 'hover' }"`）
3. **原生 overflow 条**：统一在 `src/style.css`（`--fa-scrollbar-*`）；需要隐藏时用 `.fa-scrollbar-hidden`，不要再在业务组件里复制一套
4. **禁止卡片内嵌套滚动抢滚轮**：计算器玩法区等固定高度面板用 `overflow: hidden`，滚轮交给外层比赛列表；不要再给内层挂 `wheel` 拦截
5. 不要指望只写 `native-scrollbar=false` 却不约束高度
### 少写 CSS（优先依赖组件默认样式）

使用 Naive UI 的首要目的就是**用组件默认样式，少写甚至不写自定义 CSS**，从而减小体积、统一观感、便于维护。

1. **默认先信任组件**：间距、字号、标签色、按钮态、卡片内边距等，优先用 props（`size` / `type` / `depth` / `bordered` / `quaternary`）和插槽，不要再包一层同名 class 去复刻。
2. **禁止无必要的 class + scoped CSS**：能为 `n-tag` / `n-text` / `n-flex` / `n-card` 的默认外观解决的，不要手写 `padding` / `font-size` / `background` / `border-radius`。
3. **内联 style 仅作一次性约束**：如单处 `flex: 1; min-width: 0`、`padding: 10px`，不必再开 class；重复出现再抽。
4. **自定义 CSS 只留给复杂布局**：例如填满视口的 grid 壳、滚动区 `min-height: 0`、对阵行两侧 ellipsis + 中间 VS 不换行、底栏 sticky / safe-area。业务色用 `--fa-*`，禁止写死 `#fff`。
5. **改完自检**：本次新增的 class / `:deep` 是否可删？能否换成 `n-tag` / `n-text depth` / `n-flex` props？能则删。

反例：给已是 `n-tag` 的元素再写 `.selection-play { padding; border-radius; background }` —— 等于废弃组件默认样式。

## 主题

Naive UI **只内置两套全局主题**：默认浅色（`theme=null`）与 `darkTheme`。没有更多官方命名主题。

本项目做法：

1. 仅保留 Naive 内置两套：**浅色**（`theme=null`）/ **深色**（`darkTheme`），无 `theme-overrides`
2. 主题切换为**顶栏右上角图标按钮**（深色月亮 / 浅色太阳），放在导航分段控件右侧
3. 自定义页面壳用 `--fa-*`（非 Naive 变量），由预设同步；**禁止**写死 `#fff` / `#f5f6f8`
4. 偏好键：`fa-theme-preset`（`localStorage`，值为 `light` | `dark`；旧色系 id 会自动归一）

**禁止**在页面/卡片上写死 `#fff` / `#f5f6f8`；自定义区域用 `--fa-*`。

## 展示约定

- **格式真源在入库侧**（见 `data-format-at-source`）：盘口符号、队名翻译等由后端解析/落库定稿；前端原样展示接口值，不为后端装饰字符再写一层剥除逻辑
- 开赛时间：API 为 UTC；前端用 `parseApiDate` + 浏览器本地时区格式化（`utils/format.ts`）——仅此类依赖运行环境的展示可留前端
- 赛果：W/D/L 显示为 胜/平/负（`resultToZh` / `formCharsZh`）
- 队名：后端入库时翻译并写入 `teams.name`（`backend/app/services/team_names.py`）；前端直接展示接口返回名，不再维护前端对照表
- 联赛名：优先按 league_id / 国家消歧（`utils/leagueNames.ts`）；同名如 League One 在中国为中甲、英格兰为英甲。侧栏顺序由后端 `LEAGUE_IDS` 决定，暂不支持拖拽排序


## 面包屑约定

- 首页：`赛前赛事` / `全部` 或当前联赛名
- 详情：`赛前赛事` / `{联赛}` / `{主队 VS 客队}`
- 可点击项用路由跳转（回列表、按联赛筛选），最后一级不可点

## 何时可以不用组件库

- 业务特有可视化（如 ECharts 概率图、W/D/L 色块）
- 组件库没有对应能力、或包装成本明显高于收益的极小组件
- 此时仍应嵌在 Naive 的 Card / Layout / Space / DataTable 等容器内，保持视觉一致

**自检（改 UI 必做）**：新增列表/表格前先查 Naive 是否已有 `n-data-table` / `n-list` / `n-descriptions`；没有充分理由不得提交原生 `<table>`。

## 反例（已踩过的坑）

- 侧栏 `sticky` + 整页滚动 → 侧栏跟着滚；应改为 `n-layout-sider` 固定、content 滚动
- 整站 `max-width: 1100px; margin: 0 auto` → 两侧大片空白，不像后台/工具全屏
- 手写联赛菜单按钮 → 与 `n-menu` 选中态、键盘与主题不一致

新增页面或改布局时：先查 [Naive UI 文档](https://www.naiveui.com/) 是否已有 Layout / Navigation / Data Display 组件，再写自定义结构。
