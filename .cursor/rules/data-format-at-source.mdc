---
description: 数据展示形态在入库源头定稿——禁止写入装饰字符再让前端剥掉
alwaysApply: true
---

# 数据格式：源头定稿

凡涉及**展示用数据格式**（符号、翻译、舍入、单位、标签文案），先排查：是不是**我们自己在解析/入库时加的**？

## 强制顺序

1. **先查写入路径**：官方原文 → 本项目 `parse_*` / 入库字段（如 `odds_json`、`teams.name`）→ API 响应 → 前端。
2. **若装饰是我们加的**：在**解析或落库的唯一真源**改掉，库里与接口直接给出可展示值；**禁止**「入库带装饰 + 前端再剥」。
3. **若官方原文即如此、且产品要另一套展示**：仍优先在后端归一后再存；仅当形态依赖浏览器环境（如时区）才留在前端。
4. 源头改完后：删除前端对应的「擦屁股」格式化；用 `Grep` 确认无残留平行处理。

## 禁止

- 入库写 `+0` / 带号盘口，前端再 `formatXxx` 剥成 `0`
- 入库写英文队名，前端再维护一份队名对照表（队名真源：`team_names.py`）
- 同一字段前后端各做一遍归一，长期漂移成双轨屎山

## 正例

- 让球指数：`prematch_package._line_token` / 标签归一，零盘存 `"0"`，前端原样展示
- 队名：入库译入 `teams.name`，前端直接显示

## 交付自检

- [ ] 本次展示格式问题是否已在写入/解析侧处理，而不是只加了前端补丁？
- [ ] 是否删除了因此产生的前端（或第二处后端）平行格式化？
