---
description: 官方 API 配额：先文档后实测，禁止调试空耗
alwaysApply: true
---

# 官方 API 配额保护

本项目对接 API-Sports / RapidAPI，**免费日配额很紧**。Agent 调试不得把配额打光。

## 回答「API 能不能 / 怎么用」类问题

1. **先查文档**（官方 docs、仓库内 README / `providers.py` 注释 / 既有代码路径）
2. 文档已能说明的，**直接回答，禁止为验证而实打官方接口**
3. 仅当文档缺失、矛盾，或用户**明确要求**实测时，才可打官方 API
4. 若必须实测：先说明会消耗配额、预计请求次数；能 1 次说清的不要连打；优先用已有本地库 / snapshot，不要新开探测脚本扫多日期多参数

## 禁止的默认行为

- 为「确认一下」对同一问题连打 `fixtures` / `leagues` / `odds` 多组参数
- 在用户未要求时，用 live `FootballFetcher` / httpx 探测官方接口
- 把空错误响应（plan / rateLimit）反复写入缓存当成功数据（见 fetcher 侧防护）

## 产品内同步

官方配额消耗仅两类：

1. **定时批次**（及已订阅管理员显式触发的完整批次）：未订阅每天 **00:05 UTC / 北京时间 08:05** 当天赛程、**07:00** 昨天和今天赛果、**10:55** 昨天/今天赛果与今天目录盘口、**22:00** 今天目录盘口轻刷，跳过积分榜与详情预拉、不请求未来；已订阅 07:00 回写昨天和今天赛果，10:55 增量维持 8 天赛程窗口，目录联赛明天只补缺盘并冻初盘、今天刷新即时盘，详情只预拉今天/明天。打开订阅自动开启密刷，关闭订阅同步关闭并禁用；密刷按每小时 25/55 分全天循环，只处理 `match_day=今天` 且仍在【比赛】的目录场次并重算日推，10:55 接在完整批次之后。已订阅但关闭密刷时使用未订阅时刻表，完整批次仍保持已订阅内容。管理员「立即同步」不限每日次数但每次消耗配额并受官方请求锁限制；「只更新赛果」与 07:00 定时同样回写昨天和今天终场比分
2. **显式运维 / 详情操作**：管理员可在运维页触发 `prematch_odds_sync`，前端只提交【比赛】当前筛选中比赛日为今天的场次；后端按 `fixtures.match_day=今天`、开赛时间仍在未来及 `leagues.is_catalog=true` 再过滤，明天、已开赛与目录外赛事排除。已有盘口刷新即时盘、缺盘口补齐。每个场次通常消耗一次请求，不拉赛程、赛果、积分榜或详情。已订阅时 `GET /fixtures/{id}/analysis` 只对今天的目录【比赛】按需补盘口；未订阅的普通用户只读本地。管理员显式 `POST /fixtures/{id}/odds/refresh` 也使用同一边界，其他场次不显示按钮。新增目录联赛时管理员可点「核对」走 `GET /admin/settings/leagues/{id}/lookup`：有 7 天缓存则不打官方，未命中才请求一次 `GET /leagues?id=`，不拉赛程盘口。已开赛且已有冻结快照的场次只补比分与状态，展示包一律读本地（开赛 12 小时内 60 秒缓存；12 小时~7 天仍未结算的按 30 分钟缓存补一次终场，不轮询）

列表页加载、F5、下拉刷新、联赛筛选都只读本地库；不得恢复公开 `POST /fixtures/sync` 或列表路径上的盘口补拉；详情预拉已收敛到订阅开关，不再增加平行开关。

中长期「用户 / VIP 额度」与公共池划分见仓库 **`docs/AUTH_VIP_QUOTA.md`**（设计稿；未落地前勿擅自加付费拦截逻辑）。
