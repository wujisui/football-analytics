---
description: 官方 API 配额：先文档后实测，禁止调试空耗
alwaysApply: true
---

# 官方 API 配额保护

本项目对接 API-Sports / RapidAPI，**免费日配额很紧**。Agent 调试不得把配额打光。

## 回答「API 能不能 / 怎么用」类问题

1. **先查文档**（官方 docs、仓库内 README / `providers.py` 注释 / 既有代码路径）
2. 文档已能说明的，**直接回答，禁止为验证而实打官方接口**
3. 仅当文档缺失、矛盾，或用户**明确要求**实测时，才可打官方 API
4. 若必须实测：先说明会消耗配额、预计请求次数；能 1 次说清的不要连打；优先用已有本地库 / snapshot，不要新开探测脚本扫多日期多参数

## 禁止的默认行为

- 为「确认一下」对同一问题连打 `fixtures` / `leagues` / `odds` 多组参数
- 在用户未要求时，用 live `FootballFetcher` / httpx 探测官方接口
- 把空错误响应（plan / rateLimit）反复写入缓存当成功数据（见 fetcher 侧防护）

## 产品内同步

官方配额消耗仅两类：

1. **定时批次**（及管理员当天唯一的显式完整批次）：未订阅每天 **00:05 UTC / 北京时间 08:05** 当天赛程、**11:00** 昨天赛果/今天赛程盘口、**22:00** 今天盘口轻刷，跳过积分榜与详情预拉、不请求未来；已订阅 11:00 回写近 4 天赛果、增量维持 8 天赛程窗口、只处理今天/明天盘口详情，其余轻刷今天未开赛热门盘口并重算日推（完整默认方案与 16:55…01:55 的完整密刷方案二选一，04/06/08/10 可独立叠加）。管理员「只更新赛果」是完整批次之外的按日比分回写，不占用当日完整批次额度
2. **显式运维 / 详情操作**：管理员可在运维页触发 `prematch_missing_odds_sync`，只扫描【比赛】默认两个当地比赛日窗口并逐场补拉缺少完整 1X2 的未开赛本地场次；每个候选通常消耗一次请求，不拉赛程、赛果、积分榜或详情。已订阅时 `GET /fixtures/{id}/analysis` 对未开赛热门缺包按需补拉；未订阅的普通用户只读本地。管理员也可显式 `POST /fixtures/{id}/odds/refresh` 单独更新任意一场未开赛盘口，不受热门联赛范围限制。新增目录联赛时管理员可点「核对」走 `GET /admin/settings/leagues/{id}/lookup`：有 7 天缓存则不打官方，未命中才请求一次 `GET /leagues?id=`，不拉赛程盘口。已开赛且已有冻结快照的场次只补比分与状态，展示包一律读本地（开赛 12 小时内 60 秒缓存；12 小时~7 天仍未结算的按 30 分钟缓存补一次终场，不轮询）

列表页加载、F5、下拉刷新、联赛筛选都只读本地库；不得恢复公开 `POST /fixtures/sync` 或列表路径上的盘口补拉；详情预拉已收敛到订阅开关，不再增加平行开关。

中长期「用户 / VIP 额度」与公共池划分见仓库 **`docs/AUTH_VIP_QUOTA.md`**（设计稿；未落地前勿擅自加付费拦截逻辑）。
