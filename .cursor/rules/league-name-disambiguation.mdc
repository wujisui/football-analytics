---
description: 联赛译名必须按 ID 与国家消歧，禁止仅凭通用英文名猜成热门联赛
alwaysApply: true
---

# 联赛名称消歧

联赛译名唯一真源是 `backend/app/services/league_names.py`。

## 固定优先级

1. 官方 `league_id` 映射（稳定 ID）
2. `英文名 + country` 精确映射
3. 仅对全球明确唯一的赛事名使用纯英文名映射
4. 信息不足时保留官方原文，禁止猜测

## 禁止

- 将 `Championship` 无条件译为“英冠”
- 将 `Bundesliga` 无条件译为“德甲”
- 将 `Premier League`、`Serie A`、`League One` 等通用名绑定到某一热门联赛
- 在前端增加补丁翻译或维护第二份联赛名称表

## 新增译名时

- 国内赛事默认加入 `名称|国家` 或 `_BY_ID`，不得直接加入纯名称表
- 纯名称映射只接受名称自身已明确国家/组织、不会跨国复用的赛事
- 为同名不同国至少增加一个回归测试
