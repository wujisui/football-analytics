---
description: Football Analytics 产品边界与进度依据
alwaysApply: true
---

# 产品定位

本仓库是**赛前分析**全栈项目（`backend/` + `frontend/`），不是实时比分站。

## 要做

- 开赛前准备并展示：赔率、历史交锋、近况、阵容、伤病、替补、胜平负分析
- **分析器与日推是两条轨道**：分析器的 1X2 按最可能结果推导（允许「胜/平」双选）。**双选只在市场自己也没选出热门时给**：去水后头名领先次名达 `_SINGLE_PICK_GAP` 即走单选路径，禁止用「热门不足五成」这类绝对阈值否决它（足球热门本就大多在 40%~50%，`weak_home/weak_away` 一度因此把 `m_gap` 近 0.2 的明确热门盘也拖成双选）。回测 178 场：扣掉「双选覆盖两个结果」的机械效应后，双选与单选超出市场覆盖度分别为 +1.5% 与 +2.5%，差异在噪声内。真源 `prediction.get_recommendation`，回归 `test_clear_favorite_below_half_still_gets_a_single_pick`、`test_weak_favorite_still_doubles_when_market_has_no_favorite`。分析器让球以真实主盘去水概率为基线；AH 模型至少 80 条样本且在最新时间留出集上同时以 log-loss、Brier 优于该基线时，才以「盘口 + 50% 模型偏差」保守修正，否则仍取盘口较高侧（同价持平）。不得从 1X2 或比分搬方向、不得强制翻边。该修正概率同时供详情和深盘日推，禁止日推二次收缩。日推从**独赢主/客胜 + 亚洲让球胜/负**中按风险调整回报单选，两者允许不同向，卡片上禁止混排。平局概率低、方差大，始终不进日推候选池（真源 `strategy.DAILY_PICK_OUTCOMES`），玩法展示开关不改变这条。
- **日推在命中率与回报间取平衡，不追低赔也不盲追高赔**：基础分为 `校准命中概率 × (赔率 - 1) ** PAYOUT_EXPONENT`，`PAYOUT_EXPONENT = 0.5` 的凹效用保留高水位收益奖励并压低长赔方差。**禁止靠压幂次来偏向命中率**：概率来自去水市场（`p ≈ 1/赔率`）时基础分极大值落在 `赔率 = 1/(1-e)`，压低 e 会整体抬高净赔率 < 1 的低赔候选并压平各候选原始分差，历史 EMA 与联赛软权重因此更容易反超，反而挤掉高概率的 1.9 档候选（实测 `e=1/3` 时 62% 概率、1.93 赔率、EV +0.197 的候选掉出四强）。**命中率偏好一律在打分之前表达，不进基础分**：基础分代入去水概率后正比于 `√(p(1-p))`、关于 0.5 对称，无法区分同一块让球盘的两侧（让胜 0.519 与让负 0.481 的概率项完全相等），两选一盘上真正决定排序的只剩抽水差，会系统性买进水位更高的低概率侧。因此同一块让球盘**只保留条件命中率更高的一侧**（两侧条件概率之和恒为 1，退半与走水已计入，等价于只买过半的那一侧），命中率同档时才交回基础分按水位决胜；真源 `pipeline._to_ah_picks`，回归 `test_shallow_board_never_buys_the_lower_probability_side`。**让球候选只取主盘，不要扩到其他档位**：已回测 395 场同口径对比，扩到全部浅盘档位后按分数选注 ROI 从 −0.24% 到 +0.62%（t=0.15，无差别）；改按命中率选档明确变差（命中率 61.5% 但 ROI −5.08%，高命中率被低水位吃光，正是「不追低赔」的实证）；「分数接近则贴 1X2 结论」看似最好（ROI +5.67%）但并列窗口取 0.005/0.01/0.02/0.03/0.05/0.10 时 ROI 在 +1.20%/−0.74%/+5.67%/+1.39%/−1.75%/−2.44% 之间正负横跳，是参数过拟合，不是真实效应。独赢仍走 `MIN_DAILY_CONFIDENCE` 下限，改这两个常量须同步 `test_risk_score_keeps_even_money_reachable` 与 `test_low_exponent_would_flatten_the_score_gap_across_odds`；最低置信度 40%，EV 逐场落库并作同分审计，不作正值门槛。1X2 模型当前跑不赢市场（`inference_mode=market_baseline`），AH 模型未同时胜过盘口双指标前也不启用，因此禁止描述为「正期望价值注」。浅盘 `|让球线| ≤ 0.5` 按 1X2 概率计入退半/走水；深盘读取 AH 推断层已经过质量门并完成 50% 偏差收缩的概率，再按玩法校准，不得二次收缩。**退半只罚一次**：退半的好处已计入条件命中率、代价已计入更低赔率，基础分不得再乘 `at_risk`，否则会系统性地把浅盘让球压到同向独赢之下。同一方向的独赢与浅盘必须在加历史权重前二选一：结算完全相同的 ±0.5 按实际水位取高者，其余浅盘按已计入退半/走水的基础分取高者，同分优先有本金保护的 AH；历史 EMA 与联赛×玩法软权重只乘到胜出的玩法上参与跨场排序，不得反向淘汰当场更优的兜底结构。**不设玩法配额**：跨场次只按综合分竞争，不允许低分候选为了「凑玩法分散」顶掉高分候选；只保留「同一场最多一个玩法」防止同场相关注占两个坑。
- **日推在 Top-N 截取前产出自洽三件套**：独赢入选时以主/客方向配不会输的真实让球侧；让球盘入选时以真实 `让胜/让负` 为主推并生成同侧胜负方向及比分。让 0 可推荐让0胜/让0负，打平走水退本。无法在真实盘口、大小球和双进结论下自洽的市场候选淘汰，同场另一玩法或后续场次补位；不得改造盘口线。三件套写入 `favorite_fixtures.auto_lean/auto_handicap_lean/auto_score_hint`，实际结算玩法与投注项写入 `auto_pick_snapshots.market/lean`，**不回写 `pre_match_data`**。真源 `recommendation.pipeline`、`recommendation.consistency.validate_pick_consistency`
- 详情「我的预测」盘口解释由后端 `market_analysis.py` 唯一生成：使用初盘 / 中盘 / 临场 / 即时盘，按采集时间去重，只比较同庄家、同玩法、同档位的水位与去水概率；主盘升降、同档共振、1X2、大小球、让球主盘去水倾向和计入亚盘返还后的期望收益分开说明。禁止跨档位/跨庄家拼走势或把报价变化臆断成资金流；前端只展示结构化结果，不保留第二套判断
- 数据写入本地库；前端只调本项目后端 `/api/v1`
- 【比赛】与【赛果】的归属以服务器当前 UTC 时间与开赛时刻为主：未到开赛时刻进【比赛】，已到开赛时刻进【赛果】，不依赖可能仍为 `pending` 的本地状态；**例外**：`postponed` 且原定开赛已过超过 1 天的场次移出【赛果】列表。比赛列表由后端 `/fixtures/today?scope=prematch` 在 SQL 中过滤，前端不二次判断。**【比赛】列表「今天/明天」分组、【关注】日期筛选、【赛果】日期查询/联赛统计与自动推荐分桶**统一读取后端持久化的场地当地比赛日：按 `venue.city` → 联赛国家 → 主队已知所在地 → UTC 兜底推导 IANA 时区，保存来源供审计；前端禁止从 `fixture_date` 二次换算。开赛边界真源：`results_capture.py`；比赛日真源：`match_day.py`
- 开赛后 / 结束后：预测快照冻结（审计字段不再改写）；**已开赛场次详情展示包只读本地，不再为补包/补盘打官方**（真源 `ttl_policy.should_stop_api_refresh`），这一趟对官方的唯一请求是比分 + 状态（单次；开赛 12 小时内短 TTL 跟比分，此后到 7 天内仍未结算的按 30 分钟 TTL 追终场，不轮询）；只有未开赛的热门联赛才走按点击补包
- 已入库赛程的终场比分由固定定时批次按日回写，供赛果页查询
- 未订阅的官方同步范围固定为**00:05 UTC（北京时间 08:05）只同步当天赛程 + 07:00/10:55 昨天与今天赛果 + 10:55 今天目录盘口 + 22:00 今天目录盘口**。08:05 只调用一次全球赛程接口，不拉盘口、积分榜或详情；07:00 回写昨天和今天终场比分；10:55/22:00 也跳过积分榜与详情预拉、不请求未来。盘口联赛范围统一为「热门联赛」配置页中的全部目录联赛（`leagues.is_catalog=true`），勾选热门只影响默认展示筛选。普通用户详情点击只读本地；管理员详情「更新盘口」与运维「更新盘口」只处理 `match_day=今天` 且仍在【比赛】的目录场次，明天、已开赛及目录外赛事由后端排除。已有盘口刷新即时盘、缺盘口补齐；不拉赛程、赛果、积分榜或详情。批次先结算昨天再拉今天，保证统计与机器学习标签闭环。管理员设置另可点「更新赛果」回写昨天和今天终场比分（与 07:00 定时范围相同），不拉盘口
- 已订阅视为至少 Pro（日配额 7500）：07:00 回写当前比赛日的昨天和今天赛果；10:55 跑每日定时完整批次；管理员「立即同步」等同再执行一次完整批次，不限每日次数，共用官方请求锁且每次消耗配额；未订阅时该按钮禁用。赛程在本地保留今天起 8 天，每天只请求窗口内缺失的未来日期。10:55 / 立即同步盘口只覆盖当前比赛日和明天：明天仅补缺盘并冻初盘，当前比赛日刷新即时盘；详情只预拉今天、明天。打开订阅自动开启密刷，关闭订阅同时关闭并禁用密刷；已订阅可手动关闭密刷。密刷从 11:25 起按每小时 25/55 分全天循环，只刷新 `match_day=今天` 且仍在【比赛】的目录盘口；10:55 在完整批次后再执行同刻密刷。已订阅但关闭密刷时采用 07:00/08:05/10:55/22:00 时刻表，但 10:55 仍按已订阅完整批次执行且「立即同步」仍可用。打开列表页、F5、下拉刷新、切换筛选只读本地库；无公开列表 sync、无 SSE、无前端轮询
- 「无前端轮询」特指不轮询比赛列表或官方 API。后台批次、运维「更新盘口」、更新赛果或热门勾选完成后递增持久化 `client_data_revision` 并 bump 前端全局 epoch。【比赛】/【赛果】在前台且未息屏时每 30 秒只读一次该本地版本，切到后台、息屏或离开这两页即停表；版本变化后只刷新当前页。运维按钮与详情更新盘口成功后直接 `notifyLocalDataChanged`。
- 每次**批量**同步盘口后（运维「更新盘口」、密刷、10:55 完整批次；`fixtures_sync` 在 `mode != results` 时），从**已有赛前盘口的未开赛场次**中，按后端持久化的**比赛所在地日历日**各自挑最多 4 场写入自动推荐池（`favorite_fixtures.source=auto`，只替换自动推荐）：独赢主/客胜与真实亚洲让球胜/负共同入池；概率先按玩法做时间留出 Platt 校准，浅盘按 1X2 概率计入返还，深盘 AH 概率向市场保守收缩；候选按 `概率 × 净赔率 ** PAYOUT_EXPONENT` 的风险调整回报分，再乘历史 EMA 与联赛×玩法软权重排序，EV 与赔率随行审计。完整候选池先过结果方向×让球×比分联合一致性闸，同一场只取一个玩法；玩法之间不设配额，四个坑位纯按综合分先后填。候选不足 4 场只告警、不硬塞；拒绝所有多选、平局单选与走水推荐。未来窗口有 N 个有赛日则最多约 `4×N` 场；同时写入 `auto_pick_snapshots`（开赛后冻结），赛果页准确率只学这条轨迹。质量星按同比赛日最终综合分分档。自动推荐不点亮星标、不进入【关注】，仅在【比赛】/【赛程】置顶并显示 `[荐]`/质量星；卡片整行改用日推三件套，分析器文案只留给未入选场次与详情「我的预测」
- **详情单场「更新盘口」不重算日推**：只拉本场官方盘口并 `_upsert_odds_and_recompute` 本场算法推荐。日推是同一批盘口的跨场排序，一场新水位对其余旧盘口没有可比性；禁止在 `POST /fixtures/{id}/odds/refresh` 里调用 `sync_daily_auto_favorites`。已带 `[荐]` 的三件套等到下一次批量才对齐
- 订阅开关是详情与调度的唯一边界；已删除「定时全量获取详情」平行开关。内部 `ENABLE_FREE_QUOTA` 仅作为旧部署兼容存储名，其 `false` 等价于已订阅
- 联赛目录、分类与热门勾选以数据库为唯一真源；`config/leagues.json` 仅在首次建库时导入种子。管理员在「我的 → 管理员设置 → 热门联赛」新增分类/联赛、修改中文名/国家/分类和热门状态。系统保护联赛仅五大联赛、荷甲、葡超、欧冠、中超：不可删除、不可改官方 ID。其余目录联赛可改正官方 ID，也可在密码确认后连同该联赛全部历史删除并重新添加。未勾选目录联赛进侧栏「其他」且只入库赛程、不定时拉盘；目录外额外白名单赛事仍由当日赛程自动进筛选框，不另建平行目录

## 不做

- 实时比分、滚球直播、开赛后持续轮询官方接口
- 把 API Key 写进前端或提交到 Git
- 用官方 Widgets 替代自有分析（Widgets 仅可选展示增强）

## 进度与规划

以根目录 `PROJECT_PLAN.md` 为准；改产品边界先改该文档第 1 节，再改代码。  
用户鉴权 / VIP / API 配额设计见 `docs/AUTH_VIP_QUOTA.md`（规划中）；未要求落地前不要擅自加付费拦截。
