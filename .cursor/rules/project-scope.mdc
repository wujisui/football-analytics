---
description: Football Analytics 产品边界与进度依据
alwaysApply: true
---

# 产品定位

本仓库是**赛前分析**全栈项目（`backend/` + `frontend/`），不是实时比分站。

## 要做

- 开赛前准备并展示：赔率、历史交锋、近况、阵容、伤病、替补、胜平负分析
- **推荐链路最终口径（2026-09-21 双轨版，覆盖本文件后续所有旧日推阈值/分层/补位/星级描述）**：
  **概率是双轨的，排序永远只用其中一条。** 每个方向候选同时携带两个数：所投那块盘的
  **去水价** `implied_probability`，以及**训练模型的影子输出** `model_probability`。
  排序概率 `probability_source` 按玩法门禁择一——该玩法模型在时间留出集上赢过市场
  才取 `model`，否则一律取 `market`。四个模型当前全部未过门禁，所以线上跑的是市场
  基线，**这是如实定位，不是缺陷**：09-06 起 600 场回放里市场轨 64 注命中 56.9%
  (33/58)、单位收益 +0.79、ROI +1.2%，而强制启用模型的影子轨同样 64 个坑只有 53.3%
  (32/60)、ROI −4.9%，模型自称平均 EV +6.2% 却跑输——门禁拦对了。
  **未过门禁的模型也必须输出并冻结影子概率**（`ml_predictor.shadow_probabilities`、
  `ah_predictor.shadow_cover_probability`、`predict_goals(ignore_deployable=True)`）。
  曾经「不可部署就不写概率」，而 Platt 只从有概率的记录训练，于是样本恒为 0、模型
  永远无法被验证——**这是自锁，禁止再退回去**。
  **校准器按 `玩法:概率来源` 分键**（`ah:market`、`ah:model`…，可再挂 `:方向`）：去水价
  与模型输出是两个不同的估计量，不得共用一个 Platt。**校准是可选增益不是准入**：没有
  通过验证的键就原样透传原始概率，冷启动照样出推荐。真源
  `probability_calibration.calibrate_probability`，回归
  `test_uncalibrated_key_passes_the_raw_probability_through`、
  `test_market_and_model_estimators_never_share_one_calibrator`。
  **每场参考**按“有 AH：AH→大小球→双进；无 AH：1X2→大小球→双进”严格降级选一个方向，
  前一玩法只在拿不到概率时才降级，不跨玩法比大小；展示方向与校准命中概率，悬停里给
  概率来源、EV 与方向一致性。**日推**只有三道闸：拿得到概率与赔率、过最低置信度
  （1X2/大小球/双进 0.40，AH 0.50）、方向不逆着强一致盘口；过闸后按
  `层级 → 校准命中概率 − 方向惩罚` 取每个比赛日 Top 4，下层不得凭分数越过上层。
  **星级按校准命中概率的绝对区间**：≥65%=5★、≥60%=4.5★、≥57%=4★、≥54%=3.5★、
  ≥51%=3★、≥48%=2.5★、≥45%=2★、≥42%=1.5★、其余 1★；不得按当天相对排名造五星。
  **EV 只落库审计，不排序、不设门槛**（理由见下一条），AH/四分盘仍按全赢、半赢、走水、
  半输、全输计算。前端必须如实标注「概率来源：盘口去水（市场基线）」，**禁止**把日推
  描述成正期望注。所有比赛的所有方向候选（含未入选）冻结两类概率、来源、校准器版本、
  赔率、结算分布、EV、方向修正与跳过原因，完赛后回填实际单位收益，长期反馈只喂
  `玩法:来源` Platt 与星级审计，不再保留 EMA/联赛软权重等第二套排名补丁。真源
  `recommendation.decision`、`recommendation.pipeline`，回放
  `backend/scripts/backtest_current_recommendation.py`（live 与 model_shadow 两条轨道）。
- **分析器与日推是两条轨道**：分析器的 1X2 按最可能结果推导（允许「主胜/和局」双选）。**双选只在市场自己也没选出热门时给**：去水后头名领先次名达 `_SINGLE_PICK_GAP` 即走单选路径，禁止用「热门不足五成」这类绝对阈值否决它（足球热门本就大多在 40%~50%，`weak_home/weak_away` 一度因此把 `m_gap` 近 0.2 的明确热门盘也拖成双选）。回测 178 场：扣掉「双选覆盖两个结果」的机械效应后，双选与单选超出市场覆盖度分别为 +1.5% 与 +2.5%，差异在噪声内。真源 `prediction.get_recommendation`，回归 `test_clear_favorite_below_half_still_gets_a_single_pick`、`test_weak_favorite_still_doubles_when_market_has_no_favorite`。**穿盘方向任何档位都不是胜负方向**：覆盖让球与赢得比赛是两个问题——受让方穿过 ±0.5 含「打平」，穿过 ±1.5 更与谁赢无关，把低水侧读成独赢等于系统性买进三路里概率最低的那一个。胜平负真源恒为去水 1X2 盘面，让球水位只在市场自己没选出热门时充当 `_ah_market_favorite` 平手裁决；**禁止**在 `get_recommendation` 里按亚盘方向短路。曾按「深盘才禁、浅盘照搬」的窄口径放行，2026-09-20 当日 63 场里 17 场逆着去水热门下注（含一场让 1.5 球盘去推市场仅给 10.6% 的客胜），胜平负 41.3%、比分 0/63；同窗口跟随热门 55.0% 对逆热门 33.3%，回滚后当日回到 46.0%（当天买热门的基准线是 44.4%）。深盘 `|让球线| ≥ 1` 另需让球侧统一取让球方（`bettable_side`），否则曼城让 1.5 会被读成「客胜 · 客+1.5 · 比分 0-4」。分析器与日推共用这一条真源，两轨不得在同一块盘上给出相反的让球侧。回归 `test_ah_board_never_overrides_the_1x2_board`、`test_deep_board_is_not_a_1x2_direction`、`test_deep_board_handicap_takes_the_giving_side`。**大小球方向同样以盘口为准**：`_resolve_ou_side` 有价差就取低水侧，完全同价才回退近况与模型启发式。不得再设「价差够大才看盘口」的闸——亚洲均衡总进球盘常态只差 2%（1.92 / 1.96），闸值 6% 永不触发，判断会全落到兜底启发式 `_side_from_probs`，而它的默认分支返回 `over`：2026-09-20 因此押了 51 大 : 6 小（87.3%，市场只有 54.0% 大盘更低），命中 24/57 与「恒买大」完全相同，还把目标总进球顶到 4、叠上「双进:是」后比分全是 3-1 / 4-0。回测 1203 场跟盘口 54.8% 对启发式 54.5%（噪声内），判大占比 51.0% 对 60.9%。真源 `backend/scripts/backtest_ou_side.py`，回归 `test_ou_side_follows_a_thin_market_gap`。**比分是三层条件叠乘的推导值（胜平负 × 大小球 × 双进），不是独立预测**：长期命中约 9% 已贴近单场最可能比分的天花板，上面任何一层错它必然归零，不要把它的低命中率当成独立 bug 去追。**判断命中率下滑是回归还是爆冷，先跑 `backend/scripts/replay_prediction_leans.py` 对基准线**，不要只看绝对值。**「库里上千条数据」不等于「日推样本上千条」**：分析器完场预测 1265 条，而日推快照仅 303 条（AH 188 / 1X2 106 / 大小球 9 / 双进 0），日推每轮只推 4 场，绝大多数比赛从未进池，两条轨道差一个数量级。按 80% 检验力、α=0.05、基线 54% 测算，检出 3 个百分点需每臂 4307 条，5 个百分点需 1543 条，而 AH 产出约 5.7 注/天——**阈值调参在本项目数据量下走不通，禁止再以「回测命中率更高」为由调门槛**。改门槛前必须跑 `backend/scripts/audit_sample_power.py` 看时间留出段：实测只看训练段会把 AH 门槛选到 58%（训练 66.7%），而它在留出段只有 40.0%；现行 53% 的依据是「两路市场不买低于五成的一侧」这条机制下界，不是回测增益。分析器让球以真实主盘去水概率为基线；AH 模型至少 80 条样本且在最新时间留出集上同时以 log-loss、Brier 优于该基线时，才以「盘口 + 50% 模型偏差」保守修正，否则仍取盘口较高侧（同价持平）。不得从 1X2 或比分搬方向、不得强制翻边；**比分反过来迁就让球**：比分只是由胜平负 + 大小 + 双进推出来的参考值，与真实让球行打架时，在同一胜负结果、同一大小球结论、同一双进形态里换一组不会让该让球侧输的比分（曼城 -1.5 配 2-0 而非 1-0），换不到才保留原比分——大小球优先级高于让球行。真源 `prediction._align_score_with_handicap`，回归 `test_reference_score_never_contradicts_the_handicap_row`。该修正概率同时供详情和深盘日推，禁止日推二次收缩。日推玩法固定按**亚洲让球 → 大小球 → 双进 → 无 AH 盘场次的独赢**分层；有有效 AH 盘时绝不生成独赢候选。深盘让球不因线深一律降级：优先用冻结 AH 推断概率，没有则用主盘去水概率，过 53% 最低信心且能自洽才推让球，否则才考虑大小球、双进。平局概率低、方差大，始终不进日推候选池（真源 `strategy.DAILY_PICK_OUTCOMES`）。让球一律按亚盘结算，没有整数盘三项口径。
- **日推层内按校准命中概率排序，赔率不进排序**：基础分就是 `校准命中概率` 本身（真源 `strategy.pick_ranking_score`），赔率只算进 `ev` 落库供审计与同分决胜。**禁止恢复 `概率 × 净赔率 ** e` 那类赔率加权分**：概率全部来自去水市场（`p ≈ 1/赔率`），该式会约掉成 `√(p(1-p))`，**极大值恰在 p = 0.5**，等于在每一层里系统性地挑最接近抛硬币的盘。实测 542 条浅盘让球按所投一侧赔率分桶，命中率单调降而旧打分单调升：≤1.80（水位差大）69.8% (37/53) 对平均分 0.4780，1.80~1.90 为 55.9% (246/440) 对 0.4846，1.90~1.95（近平水）49.0% (24/49) 对 0.4879；训练段 69.0/56.6/54.5%、留出段 72.7/54.4/37.5% 同向。取前 25% 时旧式 55.6%、纯概率 60.0%。旧规则「压低幂次会压平原始分差、让历史 EMA 反超」只在跨赔率档比较时成立，而 `_daily_pick_rank_key` 先按层排序、层内赔率挤在 1.8~1.95，同一批样本 e=0.5 的基础分极差只有 7.3%、e=0 反而有 15.0%——压平分差的恰恰是旧幂次。**命中率偏好一律在打分之前表达**：同一块让球盘只保留条件命中率更高的一侧（退半与走水已计入）；该规则只管浅盘 `|让球线| < 1`。**深盘 `|让球线| ≥ 1` 取与本场最可能结果同侧的一边**：受让侧赢在「输一球以内」，而卡片胜负方向只有主胜 / 客胜两格，装不下这层意思——曼城 -1.5 买客 +1.5 会被讲成「客胜、比分 1-3」；讲不圆的交给一致性闸淘汰后降级。盘口深度阈值只有 `ah_market_structure.DEEP_AH_LINE` 一处，对外两条具名规则：`side_speaks_for_result`（能否当胜负方向卖出去；让球方恒成立、受让方仅浅盘成立）定候选可选性，`outright_win_settles`（赢球能否保证这一侧不输盘；受让方恒成立、让球方只到一球盘）定非 AH 注的伴随让球行；**调用方禁止自带 `abs(line) > 1` 之类的手抄阈值**，两个问句对让球/受让恰好相反，压成一个判断会双向出错。真源 `ah_market_structure.bettable_side`、`decision.build_match_decision`（深盘受让侧 `tellable=False`、`skip_reason=deep_board_receiving_side`，仍冻结概率供审计），回归 `test_deep_board_receiving_side_is_never_sold_as_an_outright_win`、`test_one_depth_rule_serves_both_the_bet_and_the_companion_row`、 `test_ranking_never_peaks_at_the_coin_flip`、`test_shallow_board_ranks_by_probability_not_by_payout`、`test_deep_board_buys_the_side_the_card_can_tell`。**让球候选只取主盘，不扩到其他档位**；历史回测未证明扩盘有稳定收益。最低置信度 40% (1X2) / **50% (AH，二元盘的机制底线，禁止再往上加缓冲)**，EV 逐场落库并作同分审计，不作正值门槛。曾设 53%，依据是样本内扫出的「更高命中率」，实测站不住：同批样本留出段不设闸 54.0% (88/163)，加 0.53 闸降到 53.5% (23/43)，扔掉 74% 候选换负收益；根因是这个量没有区分度——概率过 0.55 的只有 6/542、过 0.58 的 1 条。改前复跑 `backend/scripts/backtest_ah_confidence.py`。1X2 模型当前跑不赢市场（`inference_mode=market_baseline`），AH 模型未同时胜过盘口双指标前也不启用，因此禁止描述为「正期望价值注」。浅盘 `|让球线| ≤ 0.5` 按 1X2 概率计入退半/走水；深盘优先读取 AH 推断层已经过质量门并完成 50% 偏差收缩的概率，没有冻结值时用主盘去水概率，再按玩法校准，不得二次收缩。线深本身不是降级条件。**退半只罚一次**：退半的好处已计入条件命中率、代价已计入更低赔率，基础分不得再乘 `at_risk`。有有效 AH 盘时独赢根本不进日推候选池，不再做浅盘 AH 与独赢二选一；无 AH 盘独赢只在 AH、大小球、双进之后兜底。历史 EMA 与联赛×玩法软权重只用于同层跨场排序，不得让下一层候选越级。只保留「同一场最多一个玩法」防止同场相关注占两个坑。
- **禁止把 `EV > 0` 设成候选门槛（2026-09-21 曾设、当日回滚）**：本项目四种玩法的概率都由**它自己那块盘**去水得来（`p = (1/赔率) / 超额`），于是 `EV = p × 赔率 − 1 = 1/超额 − 1` **恒为负**，负的幅度正好是庄家抽水。这不是「没找到价值」，而是该条件在这套概率口径下**数学上无法满足**。唯一能把它抬过零的是 Platt 校准截距，而四个校准器的 `deployable` 状态不同：`ah` / `1x2` 未通过留出检验被停用（校准退化为恒等，EV 永远为负，**整层候选恒空**），`ou` 截距 +0.136 把每块盘抬 3.4 个点（EV 恒为正，**恒通过**），`btts` 截距 +0.050 不够（仍被拒）。结果是日推在两天内塌成全是大小球：09-19 为 1x2=17/ah=3，09-22 为 ou=2/其余全 0。而那个 +0.136 并非验证出的优势——OU 校准器自身留出 Brier 只从 0.24878 改善到 0.24661。**要让 EV 闸有意义，概率必须来自独立于所投价格的估计**（跑赢市场的模型，或另一家庄家的公允价）；`inference_mode=market_baseline` 下不存在这种边际。EV 逐场落库供审计，不排序、不作门槛。真源 `decision.RecommendationCandidate.eligible_for_daily_pick`、`pipeline._to_pick`，回归 `test_negative_ev_still_produces_a_pick`、`test_ranking_uses_probability_not_payout`。
- **方向闸保留**：盘口方向由同一博彩公司初盘→中盘→临场→即时盘的让球线变化及初盘/即时盘同档位去水概率共振共同确定；算法方向逆着强一致市场方向时淘汰，逆着弱方向时标记“逆向推荐”并降权。入选理由必须明确市场方向、算法方向、一致性和 EV。真源 `market_analysis.handicap_direction_signal`。
- **日推在 Top-N 截取前产出自洽三件套**：让球盘入选时以实际投注侧为唯一真源，重新生成同侧胜负方向与比分（主+0.25 只能配主胜及主胜比分，客-0.25 同理）；比分逐条过真实亚洲盘结算，所投侧至少不能输。让 0 可推荐让0胜/让0负，打平走水退本。无法在真实盘口、大小球和双进结论下自洽的 AH 候选淘汰，同场按 AH→大小球→双进降级或由后续场次补位；不得改造盘口线。**让球行只是非 AH 注的伴随展示，不是那一注本身**：大小球 / 双进 / 无盘独赢入选时，浅盘让球行改成与胜负方向同侧；深于 1 球的盘口无法由单独主胜/客胜承诺穿盘，此时隐藏让球行，不得连实际投注项一起淘汰。三件套写入 `favorite_fixtures.auto_lean/auto_handicap_lean/auto_score_hint`，实际结算玩法与投注项写入 `auto_pick_snapshots.market/lean`，**不回写 `pre_match_data`**。真源 `pipeline._consistent_bundle`、`prediction.score_hint_for_consistent_bundle`，回归 `test_quarter_ball_pick_rebuilds_result_and_score_from_selected_side`、`test_away_quarter_ball_pick_uses_away_result_and_winning_score`、`test_impossible_deep_handicap_falls_back_and_hides_handicap_row`。
- 详情「我的预测」盘口解释由后端 `market_analysis.py` 唯一生成：使用初盘 / 中盘 / 临场 / 即时盘，按采集时间去重，只比较同庄家、同玩法、同档位的水位与去水概率；主盘升降、同档共振、1X2、大小球、让球主盘去水倾向和计入亚盘返还后的期望收益分开说明。禁止跨档位/跨庄家拼走势或把报价变化臆断成资金流；前端只展示结构化结果，不保留第二套判断
- 数据写入本地库；前端只调本项目后端 `/api/v1`
- 【比赛】与【赛果】的归属以服务器当前 UTC 时间与开赛时刻为主：未到开赛时刻进【比赛】，已到开赛时刻进【赛果】，不依赖可能仍为 `pending` 的本地状态；**例外**：`postponed` 且原定开赛已过超过 1 天的场次移出【赛果】列表。比赛列表由后端 `/fixtures/today?scope=prematch` 在 SQL 中过滤，前端不二次判断。**【比赛】列表「今天/明天」分组、【关注】日期筛选、【赛果】日期查询/联赛统计与自动推荐分桶**统一读取后端持久化的场地当地比赛日：按 `venue.city` → 联赛国家 → 主队已知所在地 → UTC 兜底推导 IANA 时区，保存来源供审计；前端禁止从 `fixture_date` 二次换算。开赛边界真源：`results_capture.py`；比赛日真源：`match_day.py`
- 开赛后 / 结束后：预测快照冻结（审计字段不再改写）；**已开赛场次详情展示包只读本地，不再为补包/补盘打官方**（真源 `ttl_policy.should_stop_api_refresh`），这一趟对官方的唯一请求是比分 + 状态（单次；开赛 12 小时内短 TTL 跟比分，此后到 7 天内仍未结算的按 30 分钟 TTL 追终场，不轮询）；只有未开赛的热门联赛才走按点击补包
- 已入库赛程的终场比分由固定定时批次按日回写，供赛果页查询
- 未订阅的官方同步范围固定为**00:05 UTC（北京时间 08:05）只同步当天赛程 + 07:00/10:55 昨天与今天赛果 + 10:55 今天目录盘口 + 22:00 今天目录盘口**。08:05 只调用一次全球赛程接口，不拉盘口、积分榜或详情；07:00 回写昨天和今天终场比分；10:55/22:00 也跳过积分榜与详情预拉、不请求未来。盘口联赛范围统一为「热门联赛」配置页中的全部目录联赛（`leagues.is_catalog=true`），勾选热门只影响默认展示筛选。普通用户详情点击只读本地。未订阅时管理员「立即同步」「更新赛果」与运维/详情「更新盘口」全部禁用，后端拒绝手动触发。已订阅时管理员详情「更新盘口」与运维「更新盘口」只处理 `match_day=今天` 且仍在【比赛】的目录场次，明天、已开赛及目录外赛事由后端排除。已有盘口刷新即时盘、缺盘口补齐；不拉赛程、赛果、积分榜或详情。批次先结算昨天再拉今天，保证统计与机器学习标签闭环
- 已订阅视为至少 Pro（日配额 7500）：07:00 回写当前比赛日的昨天和今天赛果；10:55 跑每日定时完整批次；管理员「立即同步」「更新赛果」「更新盘口」仅已订阅可用，立即同步不限每日次数，共用官方请求锁且每次消耗配额；未订阅时这些按钮全部禁用。赛程在本地保留今天起 8 天，每天只请求窗口内缺失的未来日期。10:55 / 立即同步盘口只覆盖当前比赛日和明天：明天仅补缺盘并冻初盘，当前比赛日刷新即时盘；详情只预拉今天、明天。打开订阅自动开启密刷，关闭订阅同时关闭并禁用密刷；已订阅可手动关闭密刷。密刷从 11:25 起按每小时 25/55 分全天循环，只刷新 `match_day=今天` 且仍在【比赛】的目录盘口；10:55 在完整批次后再执行同刻密刷。已订阅但关闭密刷时采用 07:00/08:05/10:55/22:00 时刻表，但 10:55 仍按已订阅完整批次执行且「立即同步」仍可用。打开列表页、F5、下拉刷新、切换筛选只读本地库；无公开列表 sync、无 SSE、无前端轮询
- 「无前端轮询」特指不轮询比赛列表或官方 API。后台批次、运维「更新盘口」、更新赛果或热门勾选完成后递增持久化 `client_data_revision` 并 bump 前端全局 epoch。【比赛】/【赛果】在前台且未息屏时每 30 秒只读一次该本地版本，切到后台、息屏或离开这两页即停表；版本变化后只刷新当前页。运维按钮与详情更新盘口成功后直接 `notifyLocalDataChanged`。
- 每次**批量**同步盘口后（运维「更新盘口」、密刷、10:55 完整批次；`fixtures_sync` 在 `mode != results` 时），从**已有赛前盘口的未开赛场次**中，按后端持久化的**比赛所在地日历日**写入自动推荐池（`favorite_fixtures.source=auto`，只替换自动推荐）。候选按 AH → 大小球 → 双进 → 无 AH 盘独赢分层；有有效 AH 盘的场次不生成独赢。概率先按玩法做时间留出 Platt 校准，浅盘按 1X2 概率计入返还，深盘 AH 概率向市场保守收缩；层内直接按校准命中概率，再乘历史 EMA 与联赛×玩法软权重排序，EV 与赔率只随行审计、不进排序。完整候选池先过结果方向×让球×比分联合一致性闸，同一场只取一个玩法；同层玩法之间不设配额。**每次重挑时每个比赛日恒定最多 4 场，任何情况都不放宽。**`MIN_MATCHES_FOR_FULL_QUOTA = 6` 管的是「几时允许少于 4 场」，不是「几时可以多于 4 场」：当日未开赛推荐池不低于 6 场就应当给满 4 场，选不满只告警、不拿未过质量或自洽闸的场次硬塞；池子不足 6 场时给不满 4 场属正常，不告警。禁止把「允许少推」实现成 `day_limit = len(day_picks)`。**这个 4 是展示上限，不是当日结算注数上限**：`sync_daily_auto_favorites` 的删除范围只覆盖未开赛场次，已开赛的 `auto_pick_snapshots` 一律保留待结算，而密刷每小时 :25/:55 重挑一次，所以一个比赛日累计冻结 8~20 注是正常现象（35 个比赛日里 29 个超过 4 注，峰值 20）。**别把它当成配额 bug 去「修」**；也别把赛果页那条曲线读成「当天 4 注的战绩」——它统计的是当日全部冻结注数，标签为「当日累计推荐」（真源 `resultsPageState.RESULTS_HIT_OPTIONS`）。实测滚动补位不拖累质量：前 4 注 54.9%、第 5–8 注 58.5%、第 9 注及以后 51.3%，均在噪声内。拒绝所有多选、平局单选与走水推荐。真源 `pipeline.select_daily_picks_by_match_day` 与 `pipeline.log_sync_summary`；回归 `test_one_goal_board_uses_market_probability_instead_of_moneyline`、`test_weak_deep_ah_falls_back_to_goals_not_moneyline`。未来窗口按各比赛日分别选取；同时写入 `auto_pick_snapshots`（开赛后冻结），赛果页准确率只学这条轨迹。质量星按同比赛日最终综合分分档。自动推荐不点亮星标、不进入【关注】，仅在【比赛】/【赛程】置顶并显示 `[荐]`/质量星；卡片整行改用日推三件套，分析器文案只留给未入选场次与详情「我的预测」
- 日推玩法按层级补位：**亚洲让球不足 4 场时依次用大小球、双方进球补位，最后才用无有效 AH 盘场次的独赢**。下一层不能凭分数越过上一层；「玩法之间不设配额」只指同一层内部。大小/双进同样使用对应盘口去水概率、分玩法 Platt 校准、历史软权重、同场单玩法限制与方向×真实让球×比分一致性闸；真源 `pipeline.MARKET_FALLBACK_TIER`，回归 `test_ou_then_btts_precede_board_free_moneyline`、`test_ou_displaces_board_free_moneyline`。
- **置信度门槛最终口径**：唯一的候选门槛是最低置信度——1X2 / 大小球 / 双进 `MIN_DAILY_CONFIDENCE = 0.40`，AH `MIN_AH_CONFIDENCE = 0.50`（两路盘不买二选一里低于五成的那一侧，是机制下界不是回测增益，**禁止再往上加缓冲**：曾设 0.53，留出段反而从 54.0% 掉到 53.5% 并扔掉 74% 候选）。真源 `decision.MIN_DAILY_CONFIDENCE` / `decision.MIN_AH_CONFIDENCE`。过闸后按 AH → 大小球 → 双进 → 无 AH 独赢分层，层内按校准命中概率排序。AH / 1X2 再过四阶段让球线 + 多档同档共振方向闸：逆强方向淘汰，逆弱方向降权并标记。`MIN_MATCHES_FOR_FULL_QUOTA = 6` 只用来决定「给不满 4 场要不要告警」，池子不足 6 场时少推不告警。不得恢复 `EV > 0` 闸（理由见上）。
- **详情单场「更新盘口」不重算日推**：只拉本场官方盘口并 `_upsert_odds_and_recompute` 本场算法推荐。日推是同一批盘口的跨场排序，一场新水位对其余旧盘口没有可比性；禁止在 `POST /fixtures/{id}/odds/refresh` 里调用 `sync_daily_auto_favorites`。已带 `[荐]` 的三件套等到下一次批量才对齐
- 订阅开关是详情与调度的唯一边界；已删除「定时全量获取详情」平行开关。内部 `ENABLE_FREE_QUOTA` 仅作为旧部署兼容存储名，其 `false` 等价于已订阅
- 联赛目录、分类与热门勾选以数据库为唯一真源；`config/leagues.json` 仅在首次建库时导入种子。管理员在「我的 → 管理员设置 → 热门联赛」新增分类/联赛、修改中文名/国家/分类和热门状态。系统保护联赛仅五大联赛、荷甲、葡超、欧冠、中超：不可删除、不可改官方 ID。其余目录联赛可改正官方 ID，也可在密码确认后连同该联赛全部历史删除并重新添加。未勾选目录联赛进侧栏「其他」且只入库赛程、不定时拉盘；目录外额外白名单赛事仍由当日赛程自动进筛选框，不另建平行目录。**默认勾选 = 热门 ∪ 当日有 `[荐]` 的联赛**：日推池按 `is_catalog` 选而默认勾选只认 `is_hot`，两套范围不一致会让落在未勾热门目录联赛上的 `[荐]` 被默认筛选整场藏掉，用户看到的推荐数少于实际选出数（09-01 推 4 场，其中德国杯不在热门，列表只剩 3 场）。带 `[荐]` 的联赛照旧归「其他」分组，只是默认勾上；隐藏的残留「让平」不撑开勾选。真源 `leagues.get_league_filter_options`，回归 `test_league_filter_options.py`

## 不做

- 实时比分、滚球直播、开赛后持续轮询官方接口
- 把 API Key 写进前端或提交到 Git
- 用官方 Widgets 替代自有分析（Widgets 仅可选展示增强）

## 进度与规划

以根目录 `PROJECT_PLAN.md` 为准；改产品边界先改该文档第 1 节，再改代码。  
用户鉴权 / VIP / API 配额设计见 `docs/AUTH_VIP_QUOTA.md`（规划中）；未要求落地前不要擅自加付费拦截。
