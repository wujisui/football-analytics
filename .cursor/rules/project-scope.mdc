---
description: Football Analytics 产品边界与进度依据
alwaysApply: true
---

# 产品定位

本仓库是**赛前分析**全栈项目（`backend/` + `frontend/`），不是实时比分站。

## 要做

- 开赛前准备并展示：赔率、历史交锋、近况、阵容、伤病、替补、胜平负分析
- **分析器与日推是两条轨道**：分析器按最可能结果推导（允许「胜/平」双选），日推按**校准置信度**单选，两者允许不同向，卡片上禁止混排。**日推只在主胜 / 客胜里单选**：亚盘庄家同时开独赢（主胜/和局/客胜）与让球盘，和局并非不可下注，但平局概率低、方差大，即使算出正 EV 也会拖垮整体中奖率，因此不进候选池（真源 `strategy.DAILY_PICK_OUTCOMES`），玩法开关不改变这条。
- **日推按「模型最有把握」选场，不按正期望选场**：1X2 模型当前跑不赢市场（`ml_predictor` 因验证 log-loss 差于市场基线而置为 `inference_mode=market_baseline`），校准概率等于市场去水概率，期望收益恒等于负的抽水，正期望在数学上不可能出现。EV 仍逐场算出并落库供审计，但不参与选优、不作门槛，否则推荐池恒为空。亚盘、大小球、双方进球三条线经回测同样无稳定市场边际（亚盘高分歧区间按时间切分后由正转负，典型过拟合），因此**禁止**以「正期望价值注」的口径描述日推。等某个玩法在时间留出上真正跑赢市场，再把 EV 提回门槛。普通详情仍由 `ah_predictor._agree_with_recommendation` 约束 `|让球线| ≤ 0.5`；**日推在 Top-N 截取前以自己的单选方向为唯一基准**产出自洽三件套：真实盘口上该方向不会输的让球侧（主胜配让胜、客胜配让负；让 0 同样是让球盘，赛果打平按走水退本、不算输，照常给方向），以及用 `prediction.score_hint_for_lean` 按该方向重算、沿用本场大小球与双方进球结论的比分候选。主胜遇深于主让 1 球、客胜遇深于客让 1 球、缺大小球结论等无法表达的场次不标荐，记录原因后从完整排序池继续补位；不得改造盘口线。三件套写入 `favorite_fixtures.auto_handicap_lean/auto_score_hint` 与 `auto_pick_snapshots.handicap_lean/score_hint`，**不回写 `pre_match_data`**，分析器那套原样留给未入选场次与详情「我的预测」。真源 `recommendation.consistency.validate_pick_consistency`
- 详情「我的预测」盘口解释由后端 `market_analysis.py` 唯一生成：使用初盘 / 中盘 / 临场 / 即时盘，按采集时间去重，只比较同庄家、同玩法、同档位的水位与去水概率；主盘升降、同档共振、1X2、大小球和计入亚盘返还后的期望收益分开说明。禁止跨档位/跨庄家拼走势或把报价变化臆断成资金流；前端只展示结构化结果，不保留第二套判断
- 数据写入本地库；前端只调本项目后端 `/api/v1`
- 【比赛】与【赛果】的归属以服务器当前 UTC 时间与开赛时刻为主：未到开赛时刻进【比赛】，已到开赛时刻进【赛果】，不依赖可能仍为 `pending` 的本地状态；**例外**：`postponed` 且原定开赛已过超过 1 天的场次移出【赛果】列表。比赛列表由后端 `/fixtures/today?scope=prematch` 在 SQL 中过滤，前端不二次判断。**【比赛】列表「今天/明天」分组、【赛果】日期查询/联赛统计与自动推荐分桶**统一读取后端持久化的场地当地比赛日：按 `venue.city` → 联赛国家 → 主队已知所在地 → UTC 兜底推导 IANA 时区，保存来源供审计；前端禁止从 `fixture_date` 二次换算。开赛边界真源：`results_capture.py`；比赛日真源：`match_day.py`
- 开赛后 / 结束后：预测快照冻结（审计字段不再改写）；**已开赛场次详情展示包只读本地，不再为补包/补盘打官方**（真源 `ttl_policy.should_stop_api_refresh`），这一趟对官方的唯一请求是比分 + 状态（单次；开赛 12 小时内短 TTL 跟比分，此后到 7 天内仍未结算的按 30 分钟 TTL 追终场，不轮询）；只有未开赛的热门联赛才走按点击补包
- 已入库赛程的终场比分由固定定时批次按日回写，供赛果页查询
- 未订阅的官方同步范围固定为**00:05 UTC（北京时间 08:05）只同步当天赛程 + 11:00 昨天赛果/今天比赛/热门盘口 + 22:00 今天热门盘口**。08:05 只调用一次全球赛程接口，不拉盘口、积分榜或详情；11:00/22:00 也跳过积分榜与详情预拉、不请求未来。普通用户详情点击只读本地；管理员可在任意未开赛场次详情显式点「更新盘口」单独请求本场盘口，不受热门联赛范围限制，也可在运维页点「批量更新盘口」：前端只提交【比赛】当前筛选已勾选联赛对应的未开赛场次，已有盘口刷新即时盘、缺盘口补齐，未勾选的「其他」赛事不进入名单；不拉赛程、赛果、积分榜或详情。批次先结算昨天再拉今天，保证统计与机器学习标签闭环。管理员设置另可点「只更新赛果」按日回写昨天与今天的终场比分，不占用「立即同步」当日额度、不拉盘口
- 已订阅视为至少 Pro（日配额 7500）：11:00 跑每日定时完整批次；管理员「立即同步」不限每日次数，新增热门联赛后可再次同步，但共用官方请求锁且每次消耗配额；未订阅时该按钮禁用。近 4 天赛果按日回写；赛程在本地保留今天起 8 天，但每天只请求窗口内缺失的未来日期，正常只新增末端一天。11:00 / 立即同步盘口覆盖今天及未来三天：未来三天缺盘补齐并冻初盘，今天刷新即时盘（整份盘口只取一家庄，初盘可由次级庄兜底但主庄开盘后替换，开赛后不再改写）；详情预拉只覆盖今天/明天热门未开赛缺包，打开详情可按需补包。盘口轻刷在两套独立完整方案间二选一：默认方案为 02:00、11:55、14:00、16:00、18:00、20:00、21:00 至 00:00 每半小时；密刷方案为 02:00、11:55、14:00、16:00、16:55、17:25 … 01:55；04/06/08/10 由早间开关独立叠加。未订阅只保留 08:05/11:00/22:00。管理员「只更新赛果」仍可按近 4 天回写比分。打开列表页、F5、下拉刷新、切换筛选只读本地库；无公开列表 sync、无 SSE、无前端轮询
- 「无前端轮询」特指不轮询比赛列表或官方 API。后台批次、批量盘口、赛果回写或热门勾选完成后递增持久化 `client_data_revision` 并 bump 前端全局 epoch。【比赛】/【赛果】在前台且未息屏时每 30 秒只读一次该本地版本，切到后台、息屏或离开这两页即停表；版本变化后只刷新当前页。运维按钮与详情更新盘口成功后直接 `notifyLocalDataChanged`。
- 每次定时同步盘口后，从**已有赛前盘口的未开赛场次**中，按后端持久化的**比赛所在地日历日**各自挑最多 4 场写入自动推荐池（`favorite_fixtures.source=auto`，只替换自动推荐）：候选概率先按玩法经过冻结历史的时间留出 Platt 校准（最近 20% 只验证；Brier 与 log-loss 未同时改善则该玩法保持原概率）；`|让球线| ≤ 0.5` 的盘口按 1X2 概率估值并计入返还（±0.25 平局退半、平手盘平局全退），其中 `±0.5` 与胜平负同事件时共用概率并按实际报价择优，其他候选把高于市场基线的模型概率向去水公平概率保守收缩；候选按**校准置信度**排序（`DailyRecommendationPick.score` 以置信度为基数，再乘历史激励的 EMA 与联赛×玩法软权重），EV 与赔率只随行落库供审计。完整候选池排序后先做 1X2×让球×已有比分联合一致性闸，淘汰项记录原因并继续取后续候选，争取在比赛数不少于 6 时补足 4 场；候选不足 4 场时只告警、不硬塞。拒绝胜/平、让胜/负等多选，也拒绝平局单选；精确比分只作详情提示，不参与自动主推；未来窗口有 N 个有赛日则最多约 `4×N` 场，禁止把整周两百场压成全局 Top4；同时写入 `auto_pick_snapshots`（开赛后不再改写），赛果页「每日推荐」准确率只学这条冻结轨迹。挑场前每个自然日最多刷新一次激励态（每日推荐 EMA 奖惩 + 预测包联赛×玩法软权重，落库 `app_settings`）；历史反馈只调整综合分，不设置历史分位淘汰门槛，候选按综合分取每个比赛日最多 4 场。入选后再算 `quality_rating`：同一比赛日内综合分最高的一场锚定 5 星，其余每降低一个得分档位降 0.5 星（半星粒度，同分同星），无需等待历史样本；前端在【比赛】卡片让球行用 `n-rate` 只读展示质量星。自动推荐不点亮星标、不进入【关注】，仅在【比赛】/【赛程】置顶并显示 `[荐]`/质量星，且被日推选中的卡片整行改用日推三件套（`auto_lean` / `auto_handicap_lean` / `auto_score_hint`，真源 `useFavoriteFixtures.autoFavoritePick`），分析器的双选文案留在未入选场次与详情「我的预测」；【关注】只展示用户点亮星标并经收藏接口写入数据库的 `source=manual` 记录
- 订阅开关是详情与调度的唯一边界；已删除「定时全量获取详情」平行开关。内部 `ENABLE_FREE_QUOTA` 仅作为旧部署兼容存储名，其 `false` 等价于已订阅
- 联赛目录、分类与热门勾选以数据库为唯一真源；`config/leagues.json` 仅在首次建库时导入种子。管理员在「我的 → 管理员设置 → 热门联赛」新增分类/联赛、修改中文名/国家/分类和热门状态。系统保护联赛仅五大联赛、荷甲、葡超、欧冠、中超：不可删除、不可改官方 ID。其余目录联赛可改正官方 ID，也可在密码确认后连同该联赛全部历史删除并重新添加。未勾选目录联赛进侧栏「其他」且只入库赛程、不定时拉盘；目录外额外白名单赛事仍由当日赛程自动进筛选框，不另建平行目录

## 不做

- 实时比分、滚球直播、开赛后持续轮询官方接口
- 把 API Key 写进前端或提交到 Git
- 用官方 Widgets 替代自有分析（Widgets 仅可选展示增强）

## 进度与规划

以根目录 `PROJECT_PLAN.md` 为准；改产品边界先改该文档第 1 节，再改代码。  
用户鉴权 / VIP / API 配额设计见 `docs/AUTH_VIP_QUOTA.md`（规划中）；未要求落地前不要擅自加付费拦截。
