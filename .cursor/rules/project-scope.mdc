---
description: Football Analytics 产品边界与进度依据
alwaysApply: true
---

# 产品定位

本仓库是**赛前分析**全栈项目（`backend/` + `frontend/`），不是实时比分站。

## 要做

- 开赛前准备并展示：赔率、历史交锋、近况、阵容、伤病、替补、胜平负分析
- 数据写入本地库；前端只调本项目后端 `/api/v1`
- 【比赛】与【赛果】的归属以服务器当前 UTC 时间与开赛时刻为主：未到开赛时刻进【比赛】，已到开赛时刻进【赛果】，不依赖可能仍为 `pending` 的本地状态；**例外**：`postponed` 且原定开赛已过超过 1 天的场次移出【赛果】列表。比赛列表由后端 `/fixtures/today?scope=prematch` 在 SQL 中过滤，前端不二次判断。**【比赛】列表「今天/明天」分组、【赛果】日期查询/联赛统计与自动推荐分桶**统一读取后端持久化的场地当地比赛日：按 `venue.city` → 联赛国家 → 主队已知所在地 → UTC 兜底推导 IANA 时区，保存来源供审计；前端禁止从 `fixture_date` 二次换算。开赛边界真源：`results_capture.py`；比赛日真源：`match_day.py`
- 开赛后 / 结束后：预测快照冻结（审计字段不再改写）；**已开赛场次详情展示包只读本地，不再为补包/补盘打官方**（真源 `ttl_policy.should_stop_api_refresh`），这一趟对官方的唯一请求是比分 + 状态（单次；开赛 12 小时内短 TTL 跟比分，此后到 7 天内仍未结算的按 30 分钟 TTL 追终场，不轮询）；只有未开赛的热门联赛才走按点击补包
- 已入库赛程的终场比分由固定定时批次按日回写，供赛果页查询
- 未订阅的官方同步范围固定为**00:05 UTC（北京时间 08:05）只同步当天赛程 + 11:00 昨天赛果/今天比赛/热门盘口 + 22:00 今天热门盘口**。08:05 只调用一次全球赛程接口，不拉盘口、积分榜或详情；11:00/22:00 也跳过积分榜与详情预拉、不请求未来。普通用户详情点击只读本地；管理员可在任意未开赛场次详情显式点「更新盘口」单独请求本场盘口，不受热门联赛范围限制，也可在运维页点「补齐比赛盘口」，只扫描【比赛】默认两个当地比赛日窗口并逐场补拉缺少完整 1X2 的未开赛本地场次，不拉赛程、赛果、积分榜或详情。批次先结算昨天再拉今天，保证统计与机器学习标签闭环。管理员设置另可点「只更新赛果」按日回写昨天与今天的终场比分，不占用「立即同步」当日额度、不拉盘口
- 已订阅视为至少 Pro（日配额 7500）：11:00（或当天尚未成功时的管理员「立即同步」）跑每日唯一完整批次；成功日写入 `app_settings.last_full_sync_day` 并禁用按钮。近 4 天赛果按日回写；赛程在本地保留今天起 8 天，但每天只请求窗口内缺失的未来日期，正常只新增末端一天。盘口与详情只处理今天、明天：明天首次可用盘口冻结为初盘，成为今天后刷新即时盘（整份盘口只取一家庄，初盘可由次级庄兜底但主庄开盘后替换，开赛后不再改写）；详情预拉只覆盖今天/明天热门未开赛缺包，打开详情可按需补包。11:55、02/14/16/18/20 只刷新今天未开赛热门盘口并重算日推；21:00 至 24:00（00:00）每隔半小时再轻刷；04/06/08/10 由默认开启的开关控制。未订阅只保留 08:05/11:00/22:00。管理员「只更新赛果」在完整批次成功后仍可按近 4 天回写比分。打开列表页、F5、下拉刷新、切换筛选只读本地库；无公开列表 sync、无 SSE、无前端轮询
- 每次定时同步盘口后，从**已有赛前盘口的未开赛场次**中，按后端持久化的**比赛所在地日历日**各自挑最多 4 场写入自动推荐池（`favorite_fixtures.source=auto`，只替换自动推荐）：候选概率先按玩法经过冻结历史的时间留出 Platt 校准（最近 20% 只验证；Brier 与 log-loss 未同时改善则该玩法保持原概率），再用校准概率计算期望收益；校准只改排序、不减少数量。拒绝胜/平、让胜/负等多选；精确比分只作详情提示，不参与自动主推；未来窗口有 N 个有赛日则最多约 `4×N` 场，禁止把整周两百场压成全局 Top4；同时写入 `auto_pick_snapshots`（开赛后不再改写），赛果页「每日推荐」准确率只学这条冻结轨迹。挑场前每个自然日最多刷新一次激励态（每日推荐 EMA 奖惩 + 预测包联赛×玩法软权重，落库 `app_settings`）；历史反馈只调整综合分，不设置历史分位淘汰门槛，候选按综合分取每个比赛日最多 4 场。入选后再算 `quality_rating`：同一比赛日内综合分最高的一场锚定 5 星，其余每降低一个得分档位降 0.5 星（半星粒度，同分同星），无需等待历史样本；前端在【比赛】卡片让球行用 `n-rate` 只读展示质量星。自动推荐不点亮星标、不进入【关注】，仅在【比赛】/【赛程】置顶并显示 `[荐]`/质量星；【关注】只展示用户点亮星标并经收藏接口写入数据库的 `source=manual` 记录
- 订阅开关是详情与调度的唯一边界；已删除「定时全量获取详情」平行开关。内部 `ENABLE_FREE_QUOTA` 仅作为旧部署兼容存储名，其 `false` 等价于已订阅
- 联赛目录、分类与热门勾选以数据库为唯一真源；`config/leagues.json` 仅在首次建库时导入受保护种子。管理员在「我的 → 管理员设置 → 热门联赛」新增分类/联赛、修改中文名/国家/分类和热门状态；种子联赛不可删除、不可改官方 ID，后台新增联赛可改正官方 ID，也可在密码确认后连同该联赛全部历史删除并重新添加。未勾选目录联赛进侧栏「其他」且只入库赛程、不定时拉盘；目录外额外白名单赛事仍由当日赛程自动进筛选框，不另建平行目录

## 不做

- 实时比分、滚球直播、开赛后持续轮询官方接口
- 把 API Key 写进前端或提交到 Git
- 用官方 Widgets 替代自有分析（Widgets 仅可选展示增强）

## 进度与规划

以根目录 `PROJECT_PLAN.md` 为准；改产品边界先改该文档第 1 节，再改代码。  
用户鉴权 / VIP / API 配额设计见 `docs/AUTH_VIP_QUOTA.md`（规划中）；未要求落地前不要擅自加付费拦截。
