---
description: Football Analytics 产品边界与进度依据
alwaysApply: true
---

# 产品定位

本仓库是**赛前分析**全栈项目（`backend/` + `frontend/`），不是实时比分站。

## 要做

- 开赛前准备并展示：赔率、历史交锋、近况、阵容、伤病、替补、胜平负分析
- 数据写入本地库；前端只调本项目后端 `/api/v1`
- 【比赛】与【赛果】的归属只比较服务器当前 UTC 时间和开赛时刻：未到开赛时刻进【比赛】，已到开赛时刻进【赛果】，不依赖可能仍为 `pending` 的本地状态；比赛列表由后端 `/fixtures/today?scope=prematch` 在 SQL 中过滤，前端不二次判断。边界真源：`backend/app/services/results_capture.py`
- 开赛后 / 结束后：预测快照冻结（审计字段不再改写）；详情缺展示包时可按点击补拉并落库；未完场比赛打开详情时补一次即时比分（单次请求 + 短 TTL，不轮询）
- 已入库赛程的终场比分由固定定时批次按日回写，供赛果页查询
- 官方写入分两条路径：**定时批次**按 `SCHEDULER_TIMEZONE` 每天 **00:00、06:00、11:00、16:00、19:00、22:00** 同步赛程窗口、盘口、赛果，并对窗口内有比赛的联赛按「联赛+赛季」每天各拉一次积分榜（同日已有快照则跳过）；**用户打开详情**（`GET /fixtures/{id}/analysis`）时可按需补拉阵容/伤病/交锋/近况/简报等并写入 `pre_match_data`（积分榜优先读联赛快照）。打开列表页、F5、下拉刷新、切换筛选只读本地库；无公开手动 sync、无 SSE、无前端轮询
- 每次定时同步盘口后，从 `config/leagues.json` 热门联赛未开赛场次中，按 **UTC 比赛日** 各自挑最多 4 场写入「关注」（`favorite_fixtures.source=auto`，只替换自动关注）：拒绝胜/平、让胜/负等多选；精确比分只作详情提示，不参与自动主推；未来窗口有 N 个有赛日则最多约 `4×N` 场，禁止把整周两百场压成全局 Top4；同时写入 `auto_pick_snapshots`（开赛后不再改写），赛果页「每日推荐」准确率只学这条冻结轨迹。挑场前每个自然日最多刷新一次激励态（每日推荐 EMA 奖惩 + 预测包联赛×玩法软权重，落库 `app_settings`）；历史反馈只调整综合分，不设置历史分位淘汰门槛，候选按综合分取每个比赛日最多 4 场。入选后再算 `quality_rating`：同一比赛日内综合分最高的一场锚定 5 星，其余每降低一个得分档位降 0.5 星（半星粒度，同分同星），无需等待历史样本；前端在【比赛】卡片让球行用 `n-rate` 只读展示，关注星标与 `[荐]` 标签统一金色、不表达质量
- 定时任务同步未来日期条窗口（默认今天起 8 天）全量赛程；**不**做窗口内全量详情包预拉（默认关；管理员可在「我的 → 管理员设置」开关，落库 `app_settings`，无需改代码）
- 联赛目录只维护 `config/leagues.json`（默认勾选）；其余联赛由当日本地赛程自动进筛选框，不另建次级目录文件

## 不做

- 实时比分、滚球直播、开赛后持续轮询官方接口
- 把 API Key 写进前端或提交到 Git
- 用官方 Widgets 替代自有分析（Widgets 仅可选展示增强）

## 进度与规划

以根目录 `PROJECT_PLAN.md` 为准；改产品边界先改该文档第 1 节，再改代码。  
用户鉴权 / VIP / API 配额设计见 `docs/AUTH_VIP_QUOTA.md`（规划中）；未要求落地前不要擅自加付费拦截。
